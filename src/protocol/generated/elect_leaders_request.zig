// Auto-generated from ElectLeadersRequest.json
// API Key: 43, Type: request
// Valid Versions: 0-2, Flexible Versions: 2+
//
// DO NOT EDIT - generated by codegen_v2.py

const std = @import("std");
const ser = @import("../serialization.zig");
const Allocator = std.mem.Allocator;

pub const ElectLeadersRequest = struct {
    pub const TopicPartitions = struct {
        /// The name of a topic.
        /// Versions: 0+
        topic: ?[]const u8 = null,
        /// The partitions of this topic whose leader should be elected.
        /// Versions: 0+
        partitions: []const i32 = &.{},

        pub fn serialize(self: *const TopicPartitions, buf: []u8, pos: *usize, version: i16) void {
            if (version >= 2) {
                ser.writeCompactString(buf, pos, self.topic);
            } else {
                ser.writeString(buf, pos, self.topic);
            }
            if (version >= 2) {
                ser.writeCompactArrayLen(buf, pos, self.partitions.len);
            } else {
                ser.writeArrayLen(buf, pos, self.partitions.len);
            }
            for (self.partitions) |item| {
                ser.writeI32(buf, pos, item);
            }
            if (version >= 2) ser.writeEmptyTaggedFields(buf, pos);
        }

        pub fn deserialize(alloc: Allocator, buf: []const u8, pos: *usize, version: i16) !TopicPartitions {
            var result = TopicPartitions{};
            result.topic = if (version >= 2)
                try ser.readCompactString(buf, pos)
            else
                try ser.readString(buf, pos);
            const partitions_len: usize = if (version >= 2)
                (try ser.readCompactArrayLen(buf, pos)) orelse 0
            else
                (try ser.readArrayLen(buf, pos)) orelse 0;
            if (partitions_len > 0) {
                const partitions_items = try alloc.alloc(i32, partitions_len);
                for (partitions_items) |*item| {
                    item.* = ser.readI32(buf, pos);
                }
                result.partitions = partitions_items;
            }
            if (version >= 2) try ser.skipTaggedFields(buf, pos);
            return result;
        }

        pub fn calcSize(self: *const TopicPartitions, version: i16) usize {
            var size: usize = 0;
            if (version >= 2) {
                size += ser.compactStringSize(self.topic);
            } else {
                size += ser.stringSize(self.topic);
            }
            if (version >= 2) {
                size += ser.unsignedVarintSize(self.partitions.len + 1);
            } else {
                size += 4;
            }
            size += self.partitions.len * 4;
            if (version >= 2) size += 1;
            return size;
        }
    };

    /// Type of elections to conduct for the partition. A value of '0' elects the preferred replica. A value of '1' elects the first live replica if there are no in-sync replica.
    /// Versions: 1+
    election_type: i8 = 0,
    /// The topic partitions to elect leaders.
    /// Versions: 0+
    topic_partitions: ?[]const TopicPartitions = null,
    /// The time in ms to wait for the election to complete.
    /// Versions: 0+
    timeout_ms: i32 = 60000,

    pub fn serialize(self: *const ElectLeadersRequest, buf: []u8, pos: *usize, version: i16) void {
        if (version >= 1) {
            ser.writeI8(buf, pos, self.election_type);
        }
        if (self.topic_partitions) |topic_partitions| {
            if (version >= 2) {
                ser.writeCompactArrayLen(buf, pos, topic_partitions.len);
            } else {
                ser.writeArrayLen(buf, pos, topic_partitions.len);
            }
            for (topic_partitions) |item| {
                item.serialize(buf, pos, version);
            }
        } else {
            if (version >= 2) {
                ser.writeCompactArrayLen(buf, pos, null);
            } else {
                ser.writeArrayLen(buf, pos, null);
            }
        }
        ser.writeI32(buf, pos, self.timeout_ms);
        if (version >= 2) ser.writeEmptyTaggedFields(buf, pos);
    }

    pub fn deserialize(alloc: Allocator, buf: []const u8, pos: *usize, version: i16) !ElectLeadersRequest {
        var result = ElectLeadersRequest{};
        if (version >= 1) {
            result.election_type = ser.readI8(buf, pos);
        }
        const topic_partitions_len: ?usize = if (version >= 2)
            try ser.readCompactArrayLen(buf, pos)
        else
            try ser.readArrayLen(buf, pos);
        if (topic_partitions_len) |len| {
            if (len == 0) {
                result.topic_partitions = &.{};
            } else {
                const topic_partitions_items = try alloc.alloc(TopicPartitions, len);
                for (topic_partitions_items) |*item| {
                    item.* = try TopicPartitions.deserialize(alloc, buf, pos, version);
                }
                result.topic_partitions = topic_partitions_items;
            }
        } else {
            result.topic_partitions = null;
        }
        result.timeout_ms = ser.readI32(buf, pos);
        if (version >= 2) try ser.skipTaggedFields(buf, pos);
        return result;
    }

    pub fn calcSize(self: *const ElectLeadersRequest, version: i16) usize {
        var size: usize = 0;
        if (version >= 1) {
            size += 1;
        }
        if (self.topic_partitions) |topic_partitions| {
            if (version >= 2) {
                size += ser.unsignedVarintSize(topic_partitions.len + 1);
            } else {
                size += 4;
            }
            for (topic_partitions) |item| {
                size += item.calcSize(version);
            }
        } else if (version >= 2) {
            size += 1;
        } else {
            size += 4;
        }
        size += 4;
        if (version >= 2) size += 1;
        return size;
    }
};
