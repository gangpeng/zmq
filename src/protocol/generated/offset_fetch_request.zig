// Auto-generated from OffsetFetchRequest.json
// API Key: 9, Type: request
// Valid Versions: 0-9, Flexible Versions: 6+
//
// DO NOT EDIT - generated by codegen_v2.py

const std = @import("std");
const ser = @import("../serialization.zig");
const Allocator = std.mem.Allocator;

pub const OffsetFetchRequest = struct {
    pub const OffsetFetchRequestTopic = struct {
        /// The topic name.
        /// Versions: 0-7
        name: ?[]const u8 = null,
        /// The partition indexes we would like to fetch offsets for.
        /// Versions: 0-7
        partition_indexes: []const i32 = &.{},

        pub fn serialize(self: *const OffsetFetchRequestTopic, buf: []u8, pos: *usize, version: i16) void {
            if (version >= 6) {
                ser.writeCompactString(buf, pos, self.name);
            } else {
                ser.writeString(buf, pos, self.name);
            }
            if (version >= 6) {
                ser.writeCompactArrayLen(buf, pos, self.partition_indexes.len);
            } else {
                ser.writeArrayLen(buf, pos, self.partition_indexes.len);
            }
            for (self.partition_indexes) |item| {
                ser.writeI32(buf, pos, item);
            }
            if (version >= 6) ser.writeEmptyTaggedFields(buf, pos);
        }

        pub fn deserialize(_: Allocator, buf: []const u8, pos: *usize, version: i16) !OffsetFetchRequestTopic {
            var result = OffsetFetchRequestTopic{};
            result.name = if (version >= 6)
                try ser.readCompactString(buf, pos)
            else
                try ser.readString(buf, pos);
            const partition_indexes_len: usize = if (version >= 6)
                (try ser.readCompactArrayLen(buf, pos)) orelse 0
            else
                (try ser.readArrayLen(buf, pos)) orelse 0;
            if (partition_indexes_len > 0) {
                pos.* += partition_indexes_len * 4;
            }
            if (version >= 6) try ser.skipTaggedFields(buf, pos);
            return result;
        }

        pub fn calcSize(self: *const OffsetFetchRequestTopic, version: i16) usize {
            var size: usize = 0;
            if (version >= 6) {
                size += ser.compactStringSize(self.name);
            } else {
                size += ser.stringSize(self.name);
            }
            if (version >= 6) {
                size += ser.unsignedVarintSize(self.partition_indexes.len + 1);
            } else {
                size += 4;
            }
            size += self.partition_indexes.len * 4;
            if (version >= 6) size += 1;
            return size;
        }
    };

    pub const OffsetFetchRequestGroup = struct {
        pub const OffsetFetchRequestTopics = struct {
            /// The topic name.
            /// Versions: 8+
            name: ?[]const u8 = null,
            /// The partition indexes we would like to fetch offsets for.
            /// Versions: 8+
            partition_indexes: []const i32 = &.{},

            pub fn serialize(self: *const OffsetFetchRequestTopics, buf: []u8, pos: *usize, version: i16) void {
                if (version >= 8) {
                    if (version >= 6) {
                        ser.writeCompactString(buf, pos, self.name);
                    } else {
                        ser.writeString(buf, pos, self.name);
                    }
                }
                if (version >= 8) {
                    if (version >= 6) {
                        ser.writeCompactArrayLen(buf, pos, self.partition_indexes.len);
                    } else {
                        ser.writeArrayLen(buf, pos, self.partition_indexes.len);
                    }
                    for (self.partition_indexes) |item| {
                        ser.writeI32(buf, pos, item);
                    }
                }
                if (version >= 6) ser.writeEmptyTaggedFields(buf, pos);
            }

            pub fn deserialize(_: Allocator, buf: []const u8, pos: *usize, version: i16) !OffsetFetchRequestTopics {
                var result = OffsetFetchRequestTopics{};
                if (version >= 8) {
                    result.name = if (version >= 6)
                        try ser.readCompactString(buf, pos)
                    else
                        try ser.readString(buf, pos);
                }
                if (version >= 8) {
                    const partition_indexes_len: usize = if (version >= 6)
                        (try ser.readCompactArrayLen(buf, pos)) orelse 0
                    else
                        (try ser.readArrayLen(buf, pos)) orelse 0;
                    if (partition_indexes_len > 0) {
                        pos.* += partition_indexes_len * 4;
                    }
                }
                if (version >= 6) try ser.skipTaggedFields(buf, pos);
                return result;
            }

            pub fn calcSize(self: *const OffsetFetchRequestTopics, version: i16) usize {
                var size: usize = 0;
                if (version >= 8) {
                    if (version >= 6) {
                        size += ser.compactStringSize(self.name);
                    } else {
                        size += ser.stringSize(self.name);
                    }
                }
                if (version >= 8) {
                    if (version >= 6) {
                        size += ser.unsignedVarintSize(self.partition_indexes.len + 1);
                    } else {
                        size += 4;
                    }
                    size += self.partition_indexes.len * 4;
                }
                if (version >= 6) size += 1;
                return size;
            }
        };

        /// The group ID.
        /// Versions: 8+
        group_id: ?[]const u8 = null,
        /// The member ID assigned by the group coordinator if using the new consumer protocol (KIP-848).
        /// Versions: 9+
        member_id: ?[]const u8 = null,
        /// The member epoch if using the new consumer protocol (KIP-848).
        /// Versions: 9+
        member_epoch: i32 = -1,
        /// Each topic we would like to fetch offsets for, or null to fetch offsets for all topics.
        /// Versions: 8+
        topics: []const OffsetFetchRequestTopics = &.{},

        pub fn serialize(self: *const OffsetFetchRequestGroup, buf: []u8, pos: *usize, version: i16) void {
            if (version >= 8) {
                if (version >= 6) {
                    ser.writeCompactString(buf, pos, self.group_id);
                } else {
                    ser.writeString(buf, pos, self.group_id);
                }
            }
            if (version >= 9) {
                if (version >= 6) {
                    ser.writeCompactString(buf, pos, self.member_id);
                } else {
                    ser.writeString(buf, pos, self.member_id);
                }
            }
            if (version >= 9) {
                ser.writeI32(buf, pos, self.member_epoch);
            }
            if (version >= 8) {
                if (version >= 6) {
                    ser.writeCompactArrayLen(buf, pos, self.topics.len);
                } else {
                    ser.writeArrayLen(buf, pos, self.topics.len);
                }
                for (self.topics) |item| {
                    item.serialize(buf, pos, version);
                }
            }
            if (version >= 6) ser.writeEmptyTaggedFields(buf, pos);
        }

        pub fn deserialize(alloc: Allocator, buf: []const u8, pos: *usize, version: i16) !OffsetFetchRequestGroup {
            var result = OffsetFetchRequestGroup{};
            if (version >= 8) {
                result.group_id = if (version >= 6)
                    try ser.readCompactString(buf, pos)
                else
                    try ser.readString(buf, pos);
            }
            if (version >= 9) {
                result.member_id = if (version >= 6)
                    try ser.readCompactString(buf, pos)
                else
                    try ser.readString(buf, pos);
            }
            if (version >= 9) {
                result.member_epoch = ser.readI32(buf, pos);
            }
            if (version >= 8) {
                const topics_len: usize = if (version >= 6)
                    (try ser.readCompactArrayLen(buf, pos)) orelse 0
                else
                    (try ser.readArrayLen(buf, pos)) orelse 0;
                if (topics_len > 0) {
                    const topics_items = try alloc.alloc(OffsetFetchRequestTopics, topics_len);
                    for (topics_items) |*item| {
                        item.* = try OffsetFetchRequestTopics.deserialize(alloc, buf, pos, version);
                    }
                    result.topics = topics_items;
                }
            }
            if (version >= 6) try ser.skipTaggedFields(buf, pos);
            return result;
        }

        pub fn calcSize(self: *const OffsetFetchRequestGroup, version: i16) usize {
            var size: usize = 0;
            if (version >= 8) {
                if (version >= 6) {
                    size += ser.compactStringSize(self.group_id);
                } else {
                    size += ser.stringSize(self.group_id);
                }
            }
            if (version >= 9) {
                if (version >= 6) {
                    size += ser.compactStringSize(self.member_id);
                } else {
                    size += ser.stringSize(self.member_id);
                }
            }
            if (version >= 9) {
                size += 4;
            }
            if (version >= 8) {
                if (version >= 6) {
                    size += ser.unsignedVarintSize(self.topics.len + 1);
                } else {
                    size += 4;
                }
                for (self.topics) |item| {
                    size += item.calcSize(version);
                }
            }
            if (version >= 6) size += 1;
            return size;
        }
    };

    /// The group to fetch offsets for.
    /// Versions: 0-7
    group_id: ?[]const u8 = null,
    /// Each topic we would like to fetch offsets for, or null to fetch offsets for all topics.
    /// Versions: 0-7
    topics: []const OffsetFetchRequestTopic = &.{},
    /// Each group we would like to fetch offsets for
    /// Versions: 8+
    groups: []const OffsetFetchRequestGroup = &.{},
    /// Whether broker should hold on returning unstable offsets but set a retriable error code for the partitions.
    /// Versions: 7+
    require_stable: bool = false,

    pub fn serialize(self: *const OffsetFetchRequest, buf: []u8, pos: *usize, version: i16) void {
        if (version >= 6) {
            ser.writeCompactString(buf, pos, self.group_id);
        } else {
            ser.writeString(buf, pos, self.group_id);
        }
        if (version >= 6) {
            ser.writeCompactArrayLen(buf, pos, self.topics.len);
        } else {
            ser.writeArrayLen(buf, pos, self.topics.len);
        }
        for (self.topics) |item| {
            item.serialize(buf, pos, version);
        }
        if (version >= 8) {
            if (version >= 6) {
                ser.writeCompactArrayLen(buf, pos, self.groups.len);
            } else {
                ser.writeArrayLen(buf, pos, self.groups.len);
            }
            for (self.groups) |item| {
                item.serialize(buf, pos, version);
            }
        }
        if (version >= 7) {
            ser.writeBool(buf, pos, self.require_stable);
        }
        if (version >= 6) ser.writeEmptyTaggedFields(buf, pos);
    }

    pub fn deserialize(alloc: Allocator, buf: []const u8, pos: *usize, version: i16) !OffsetFetchRequest {
        var result = OffsetFetchRequest{};
        result.group_id = if (version >= 6)
            try ser.readCompactString(buf, pos)
        else
            try ser.readString(buf, pos);
        const topics_len: usize = if (version >= 6)
            (try ser.readCompactArrayLen(buf, pos)) orelse 0
        else
            (try ser.readArrayLen(buf, pos)) orelse 0;
        if (topics_len > 0) {
            const topics_items = try alloc.alloc(OffsetFetchRequestTopic, topics_len);
            for (topics_items) |*item| {
                item.* = try OffsetFetchRequestTopic.deserialize(alloc, buf, pos, version);
            }
            result.topics = topics_items;
        }
        if (version >= 8) {
            const groups_len: usize = if (version >= 6)
                (try ser.readCompactArrayLen(buf, pos)) orelse 0
            else
                (try ser.readArrayLen(buf, pos)) orelse 0;
            if (groups_len > 0) {
                const groups_items = try alloc.alloc(OffsetFetchRequestGroup, groups_len);
                for (groups_items) |*item| {
                    item.* = try OffsetFetchRequestGroup.deserialize(alloc, buf, pos, version);
                }
                result.groups = groups_items;
            }
        }
        if (version >= 7) {
            result.require_stable = try ser.readBool(buf, pos);
        }
        if (version >= 6) try ser.skipTaggedFields(buf, pos);
        return result;
    }

    pub fn calcSize(self: *const OffsetFetchRequest, version: i16) usize {
        var size: usize = 0;
        if (version >= 6) {
            size += ser.compactStringSize(self.group_id);
        } else {
            size += ser.stringSize(self.group_id);
        }
        if (version >= 6) {
            size += ser.unsignedVarintSize(self.topics.len + 1);
        } else {
            size += 4;
        }
        for (self.topics) |item| {
            size += item.calcSize(version);
        }
        if (version >= 8) {
            if (version >= 6) {
                size += ser.unsignedVarintSize(self.groups.len + 1);
            } else {
                size += 4;
            }
            for (self.groups) |item| {
                size += item.calcSize(version);
            }
        }
        if (version >= 7) {
            size += 1;
        }
        if (version >= 6) size += 1;
        return size;
    }
};
