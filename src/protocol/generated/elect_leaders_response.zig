// Auto-generated from ElectLeadersResponse.json
// API Key: 43, Type: response
// Valid Versions: 0-2, Flexible Versions: 2+
//
// DO NOT EDIT - generated by codegen_v2.py

const std = @import("std");
const ser = @import("../serialization.zig");
const Allocator = std.mem.Allocator;

pub const ElectLeadersResponse = struct {
    pub const ReplicaElectionResult = struct {
        pub const PartitionResult = struct {
            /// The partition id
            /// Versions: 0+
            partition_id: i32 = 0,
            /// The result error, or zero if there was no error.
            /// Versions: 0+
            error_code: i16 = 0,
            /// The result message, or null if there was no error.
            /// Versions: 0+
            error_message: ?[]const u8 = null,

            pub fn serialize(self: *const PartitionResult, buf: []u8, pos: *usize, version: i16) void {
                ser.writeI32(buf, pos, self.partition_id);
                ser.writeI16(buf, pos, self.error_code);
                if (version >= 2) {
                    ser.writeCompactString(buf, pos, self.error_message);
                } else {
                    ser.writeString(buf, pos, self.error_message);
                }
                if (version >= 2) ser.writeEmptyTaggedFields(buf, pos);
            }

            pub fn deserialize(_: Allocator, buf: []const u8, pos: *usize, version: i16) !PartitionResult {
                var result = PartitionResult{};
                result.partition_id = ser.readI32(buf, pos);
                result.error_code = ser.readI16(buf, pos);
                result.error_message = if (version >= 2)
                    try ser.readCompactString(buf, pos)
                else
                    try ser.readString(buf, pos);
                if (version >= 2) try ser.skipTaggedFields(buf, pos);
                return result;
            }

            pub fn calcSize(self: *const PartitionResult, version: i16) usize {
                var size: usize = 0;
                size += 4;
                size += 2;
                if (version >= 2) {
                    size += ser.compactStringSize(self.error_message);
                } else {
                    size += ser.stringSize(self.error_message);
                }
                if (version >= 2) size += 1;
                return size;
            }
        };

        /// The topic name
        /// Versions: 0+
        topic: ?[]const u8 = null,
        /// The results for each partition
        /// Versions: 0+
        partition_result: []const PartitionResult = &.{},

        pub fn serialize(self: *const ReplicaElectionResult, buf: []u8, pos: *usize, version: i16) void {
            if (version >= 2) {
                ser.writeCompactString(buf, pos, self.topic);
            } else {
                ser.writeString(buf, pos, self.topic);
            }
            if (version >= 2) {
                ser.writeCompactArrayLen(buf, pos, self.partition_result.len);
            } else {
                ser.writeArrayLen(buf, pos, self.partition_result.len);
            }
            for (self.partition_result) |item| {
                item.serialize(buf, pos, version);
            }
            if (version >= 2) ser.writeEmptyTaggedFields(buf, pos);
        }

        pub fn deserialize(alloc: Allocator, buf: []const u8, pos: *usize, version: i16) !ReplicaElectionResult {
            var result = ReplicaElectionResult{};
            result.topic = if (version >= 2)
                try ser.readCompactString(buf, pos)
            else
                try ser.readString(buf, pos);
            const partition_result_len: usize = if (version >= 2)
                (try ser.readCompactArrayLen(buf, pos)) orelse 0
            else
                (try ser.readArrayLen(buf, pos)) orelse 0;
            if (partition_result_len > 0) {
                const partition_result_items = try alloc.alloc(PartitionResult, partition_result_len);
                for (partition_result_items) |*item| {
                    item.* = try PartitionResult.deserialize(alloc, buf, pos, version);
                }
                result.partition_result = partition_result_items;
            }
            if (version >= 2) try ser.skipTaggedFields(buf, pos);
            return result;
        }

        pub fn calcSize(self: *const ReplicaElectionResult, version: i16) usize {
            var size: usize = 0;
            if (version >= 2) {
                size += ser.compactStringSize(self.topic);
            } else {
                size += ser.stringSize(self.topic);
            }
            if (version >= 2) {
                size += ser.unsignedVarintSize(self.partition_result.len + 1);
            } else {
                size += 4;
            }
            for (self.partition_result) |item| {
                size += item.calcSize(version);
            }
            if (version >= 2) size += 1;
            return size;
        }
    };

    /// The duration in milliseconds for which the request was throttled due to a quota violation, or zero if the request did not violate any quota.
    /// Versions: 0+
    throttle_time_ms: i32 = 0,
    /// The top level response error code.
    /// Versions: 1+
    error_code: i16 = 0,
    /// The election results, or an empty array if the requester did not have permission and the request asks for all partitions.
    /// Versions: 0+
    replica_election_results: []const ReplicaElectionResult = &.{},

    pub fn serialize(self: *const ElectLeadersResponse, buf: []u8, pos: *usize, version: i16) void {
        ser.writeI32(buf, pos, self.throttle_time_ms);
        if (version >= 1) {
            ser.writeI16(buf, pos, self.error_code);
        }
        if (version >= 2) {
            ser.writeCompactArrayLen(buf, pos, self.replica_election_results.len);
        } else {
            ser.writeArrayLen(buf, pos, self.replica_election_results.len);
        }
        for (self.replica_election_results) |item| {
            item.serialize(buf, pos, version);
        }
        if (version >= 2) ser.writeEmptyTaggedFields(buf, pos);
    }

    pub fn deserialize(alloc: Allocator, buf: []const u8, pos: *usize, version: i16) !ElectLeadersResponse {
        var result = ElectLeadersResponse{};
        result.throttle_time_ms = ser.readI32(buf, pos);
        if (version >= 1) {
            result.error_code = ser.readI16(buf, pos);
        }
        const replica_election_results_len: usize = if (version >= 2)
            (try ser.readCompactArrayLen(buf, pos)) orelse 0
        else
            (try ser.readArrayLen(buf, pos)) orelse 0;
        if (replica_election_results_len > 0) {
            const replica_election_results_items = try alloc.alloc(ReplicaElectionResult, replica_election_results_len);
            for (replica_election_results_items) |*item| {
                item.* = try ReplicaElectionResult.deserialize(alloc, buf, pos, version);
            }
            result.replica_election_results = replica_election_results_items;
        }
        if (version >= 2) try ser.skipTaggedFields(buf, pos);
        return result;
    }

    pub fn calcSize(self: *const ElectLeadersResponse, version: i16) usize {
        var size: usize = 0;
        size += 4;
        if (version >= 1) {
            size += 2;
        }
        if (version >= 2) {
            size += ser.unsignedVarintSize(self.replica_election_results.len + 1);
        } else {
            size += 4;
        }
        for (self.replica_election_results) |item| {
            size += item.calcSize(version);
        }
        if (version >= 2) size += 1;
        return size;
    }
};
