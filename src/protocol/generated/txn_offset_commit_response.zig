// Auto-generated from TxnOffsetCommitResponse.json
// API Key: 28, Type: response
// Valid Versions: 0-4, Flexible Versions: 3+
//
// DO NOT EDIT - generated by codegen_v2.py

const std = @import("std");
const ser = @import("../serialization.zig");
const Allocator = std.mem.Allocator;

pub const TxnOffsetCommitResponse = struct {
    pub const TxnOffsetCommitResponseTopic = struct {
        pub const TxnOffsetCommitResponsePartition = struct {
            /// The partition index.
            /// Versions: 0+
            partition_index: i32 = 0,
            /// The error code, or 0 if there was no error.
            /// Versions: 0+
            error_code: i16 = 0,

            pub fn serialize(self: *const TxnOffsetCommitResponsePartition, buf: []u8, pos: *usize, version: i16) void {
                ser.writeI32(buf, pos, self.partition_index);
                ser.writeI16(buf, pos, self.error_code);
                if (version >= 3) ser.writeEmptyTaggedFields(buf, pos);
            }

            pub fn deserialize(_: Allocator, buf: []const u8, pos: *usize, version: i16) !TxnOffsetCommitResponsePartition {
                var result = TxnOffsetCommitResponsePartition{};
                result.partition_index = ser.readI32(buf, pos);
                result.error_code = ser.readI16(buf, pos);
                if (version >= 3) try ser.skipTaggedFields(buf, pos);
                return result;
            }

            pub fn calcSize(_: *const TxnOffsetCommitResponsePartition, version: i16) usize {
                var size: usize = 0;
                size += 4;
                size += 2;
                if (version >= 3) size += 1;
                return size;
            }
        };

        /// The topic name.
        /// Versions: 0+
        name: ?[]const u8 = null,
        /// The responses for each partition in the topic.
        /// Versions: 0+
        partitions: []const TxnOffsetCommitResponsePartition = &.{},

        pub fn serialize(self: *const TxnOffsetCommitResponseTopic, buf: []u8, pos: *usize, version: i16) void {
            if (version >= 3) {
                ser.writeCompactString(buf, pos, self.name);
            } else {
                ser.writeString(buf, pos, self.name);
            }
            if (version >= 3) {
                ser.writeCompactArrayLen(buf, pos, self.partitions.len);
            } else {
                ser.writeArrayLen(buf, pos, self.partitions.len);
            }
            for (self.partitions) |item| {
                item.serialize(buf, pos, version);
            }
            if (version >= 3) ser.writeEmptyTaggedFields(buf, pos);
        }

        pub fn deserialize(alloc: Allocator, buf: []const u8, pos: *usize, version: i16) !TxnOffsetCommitResponseTopic {
            var result = TxnOffsetCommitResponseTopic{};
            result.name = if (version >= 3)
                try ser.readCompactString(buf, pos)
            else
                try ser.readString(buf, pos);
            const partitions_len: usize = if (version >= 3)
                (try ser.readCompactArrayLen(buf, pos)) orelse 0
            else
                (try ser.readArrayLen(buf, pos)) orelse 0;
            if (partitions_len > 0) {
                const partitions_items = try alloc.alloc(TxnOffsetCommitResponsePartition, partitions_len);
                for (partitions_items) |*item| {
                    item.* = try TxnOffsetCommitResponsePartition.deserialize(alloc, buf, pos, version);
                }
                result.partitions = partitions_items;
            }
            if (version >= 3) try ser.skipTaggedFields(buf, pos);
            return result;
        }

        pub fn calcSize(self: *const TxnOffsetCommitResponseTopic, version: i16) usize {
            var size: usize = 0;
            if (version >= 3) {
                size += ser.compactStringSize(self.name);
            } else {
                size += ser.stringSize(self.name);
            }
            if (version >= 3) {
                size += ser.unsignedVarintSize(self.partitions.len + 1);
            } else {
                size += 4;
            }
            for (self.partitions) |item| {
                size += item.calcSize(version);
            }
            if (version >= 3) size += 1;
            return size;
        }
    };

    /// The duration in milliseconds for which the request was throttled due to a quota violation, or zero if the request did not violate any quota.
    /// Versions: 0+
    throttle_time_ms: i32 = 0,
    /// The responses for each topic.
    /// Versions: 0+
    topics: []const TxnOffsetCommitResponseTopic = &.{},

    pub fn serialize(self: *const TxnOffsetCommitResponse, buf: []u8, pos: *usize, version: i16) void {
        ser.writeI32(buf, pos, self.throttle_time_ms);
        if (version >= 3) {
            ser.writeCompactArrayLen(buf, pos, self.topics.len);
        } else {
            ser.writeArrayLen(buf, pos, self.topics.len);
        }
        for (self.topics) |item| {
            item.serialize(buf, pos, version);
        }
        if (version >= 3) ser.writeEmptyTaggedFields(buf, pos);
    }

    pub fn deserialize(alloc: Allocator, buf: []const u8, pos: *usize, version: i16) !TxnOffsetCommitResponse {
        var result = TxnOffsetCommitResponse{};
        result.throttle_time_ms = ser.readI32(buf, pos);
        const topics_len: usize = if (version >= 3)
            (try ser.readCompactArrayLen(buf, pos)) orelse 0
        else
            (try ser.readArrayLen(buf, pos)) orelse 0;
        if (topics_len > 0) {
            const topics_items = try alloc.alloc(TxnOffsetCommitResponseTopic, topics_len);
            for (topics_items) |*item| {
                item.* = try TxnOffsetCommitResponseTopic.deserialize(alloc, buf, pos, version);
            }
            result.topics = topics_items;
        }
        if (version >= 3) try ser.skipTaggedFields(buf, pos);
        return result;
    }

    pub fn calcSize(self: *const TxnOffsetCommitResponse, version: i16) usize {
        var size: usize = 0;
        size += 4;
        if (version >= 3) {
            size += ser.unsignedVarintSize(self.topics.len + 1);
        } else {
            size += 4;
        }
        for (self.topics) |item| {
            size += item.calcSize(version);
        }
        if (version >= 3) size += 1;
        return size;
    }
};
