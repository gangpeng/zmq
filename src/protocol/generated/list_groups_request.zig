// Auto-generated from ListGroupsRequest.json
// API Key: 16, Type: request
// Valid Versions: 0-5, Flexible Versions: 3+
//
// DO NOT EDIT - generated by codegen_v2.py

const std = @import("std");
const ser = @import("../serialization.zig");
const Allocator = std.mem.Allocator;

pub const ListGroupsRequest = struct {
    /// The states of the groups we want to list. If empty, all groups are returned with their state.
    /// Versions: 4+
    states_filter: []const ?[]const u8 = &.{},
    /// The types of the groups we want to list. If empty, all groups are returned with their type.
    /// Versions: 5+
    types_filter: []const ?[]const u8 = &.{},

    pub fn serialize(self: *const ListGroupsRequest, buf: []u8, pos: *usize, version: i16) void {
        if (version >= 4) {
            if (version >= 3) {
                ser.writeCompactArrayLen(buf, pos, self.states_filter.len);
            } else {
                ser.writeArrayLen(buf, pos, self.states_filter.len);
            }
            for (self.states_filter) |item| {
                if (version >= 3) {
                    ser.writeCompactString(buf, pos, item);
                } else {
                    ser.writeString(buf, pos, item);
                }
            }
        }
        if (version >= 5) {
            if (version >= 3) {
                ser.writeCompactArrayLen(buf, pos, self.types_filter.len);
            } else {
                ser.writeArrayLen(buf, pos, self.types_filter.len);
            }
            for (self.types_filter) |item| {
                if (version >= 3) {
                    ser.writeCompactString(buf, pos, item);
                } else {
                    ser.writeString(buf, pos, item);
                }
            }
        }
        if (version >= 3) ser.writeEmptyTaggedFields(buf, pos);
    }

    pub fn deserialize(alloc: Allocator, buf: []const u8, pos: *usize, version: i16) !ListGroupsRequest {
        var result = ListGroupsRequest{};
        if (version >= 4) {
            const states_filter_len: usize = if (version >= 3)
                (try ser.readCompactArrayLen(buf, pos)) orelse 0
            else
                (try ser.readArrayLen(buf, pos)) orelse 0;
            if (states_filter_len > 0) {
                const states_filter_items = try alloc.alloc(?[]const u8, states_filter_len);
                for (states_filter_items) |*item| {
                    item.* = if (version >= 3)
                        try ser.readCompactString(buf, pos)
                    else
                        try ser.readString(buf, pos);
                }
                result.states_filter = states_filter_items;
            }
        }
        if (version >= 5) {
            const types_filter_len: usize = if (version >= 3)
                (try ser.readCompactArrayLen(buf, pos)) orelse 0
            else
                (try ser.readArrayLen(buf, pos)) orelse 0;
            if (types_filter_len > 0) {
                const types_filter_items = try alloc.alloc(?[]const u8, types_filter_len);
                for (types_filter_items) |*item| {
                    item.* = if (version >= 3)
                        try ser.readCompactString(buf, pos)
                    else
                        try ser.readString(buf, pos);
                }
                result.types_filter = types_filter_items;
            }
        }
        if (version >= 3) try ser.skipTaggedFields(buf, pos);
        return result;
    }

    pub fn calcSize(self: *const ListGroupsRequest, version: i16) usize {
        var size: usize = 0;
        if (version >= 4) {
            if (version >= 3) {
                size += ser.unsignedVarintSize(self.states_filter.len + 1);
            } else {
                size += 4;
            }
            for (self.states_filter) |item| {
                if (version >= 3) {
                    size += ser.compactStringSize(item);
                } else {
                    size += ser.stringSize(item);
                }
            }
        }
        if (version >= 5) {
            if (version >= 3) {
                size += ser.unsignedVarintSize(self.types_filter.len + 1);
            } else {
                size += 4;
            }
            for (self.types_filter) |item| {
                if (version >= 3) {
                    size += ser.compactStringSize(item);
                } else {
                    size += ser.stringSize(item);
                }
            }
        }
        if (version >= 3) size += 1;
        return size;
    }
};
