// Auto-generated from StopReplicaRequest.json
// API Key: 5, Type: request
// Valid Versions: 0-4, Flexible Versions: 2+
//
// DO NOT EDIT - generated by codegen_v2.py

const std = @import("std");
const ser = @import("../serialization.zig");
const Allocator = std.mem.Allocator;

pub const StopReplicaRequest = struct {
    pub const StopReplicaPartitionV0 = struct {
        /// The topic name.
        /// Versions: 0
        topic_name: ?[]const u8 = null,
        /// The partition index.
        /// Versions: 0
        partition_index: i32 = 0,

        pub fn serialize(self: *const StopReplicaPartitionV0, buf: []u8, pos: *usize, version: i16) void {
            if (version == 0) {
                ser.writeString(buf, pos, self.topic_name);
                ser.writeI32(buf, pos, self.partition_index);
            }
        }

        pub fn deserialize(_: Allocator, buf: []const u8, pos: *usize, version: i16) !StopReplicaPartitionV0 {
            var result = StopReplicaPartitionV0{};
            if (version == 0) {
                result.topic_name = try ser.readString(buf, pos);
                result.partition_index = ser.readI32(buf, pos);
            }
            return result;
        }

        pub fn calcSize(self: *const StopReplicaPartitionV0, version: i16) usize {
            var size: usize = 0;
            if (version == 0) {
                size += ser.stringSize(self.topic_name);
                size += 4;
            }
            return size;
        }
    };

    pub const StopReplicaTopicV1 = struct {
        /// The topic name.
        /// Versions: 1-2
        name: ?[]const u8 = null,
        /// The partition indexes.
        /// Versions: 1-2
        partition_indexes: []const i32 = &.{},

        pub fn serialize(self: *const StopReplicaTopicV1, buf: []u8, pos: *usize, version: i16) void {
            if (version >= 1 and version <= 2) {
                if (version >= 2) {
                    ser.writeCompactString(buf, pos, self.name);
                } else {
                    ser.writeString(buf, pos, self.name);
                }
            }
            if (version >= 1 and version <= 2) {
                if (version >= 2) {
                    ser.writeCompactArrayLen(buf, pos, self.partition_indexes.len);
                } else {
                    ser.writeArrayLen(buf, pos, self.partition_indexes.len);
                }
                for (self.partition_indexes) |item| {
                    ser.writeI32(buf, pos, item);
                }
            }
            if (version >= 2) ser.writeEmptyTaggedFields(buf, pos);
        }

        pub fn deserialize(alloc: Allocator, buf: []const u8, pos: *usize, version: i16) !StopReplicaTopicV1 {
            var result = StopReplicaTopicV1{};
            if (version >= 1 and version <= 2) {
                result.name = if (version >= 2)
                    try ser.readCompactString(buf, pos)
                else
                    try ser.readString(buf, pos);
            }
            if (version >= 1 and version <= 2) {
                const partition_indexes_len: usize = if (version >= 2)
                    (try ser.readCompactArrayLen(buf, pos)) orelse 0
                else
                    (try ser.readArrayLen(buf, pos)) orelse 0;
                if (partition_indexes_len > 0) {
                    const partition_indexes_items = try alloc.alloc(i32, partition_indexes_len);
                    for (partition_indexes_items) |*item| {
                        item.* = ser.readI32(buf, pos);
                    }
                    result.partition_indexes = partition_indexes_items;
                }
            }
            if (version >= 2) try ser.skipTaggedFields(buf, pos);
            return result;
        }

        pub fn calcSize(self: *const StopReplicaTopicV1, version: i16) usize {
            var size: usize = 0;
            if (version >= 1 and version <= 2) {
                if (version >= 2) {
                    size += ser.compactStringSize(self.name);
                } else {
                    size += ser.stringSize(self.name);
                }
            }
            if (version >= 1 and version <= 2) {
                if (version >= 2) {
                    size += ser.unsignedVarintSize(self.partition_indexes.len + 1);
                } else {
                    size += 4;
                }
                size += self.partition_indexes.len * 4;
            }
            if (version >= 2) size += 1;
            return size;
        }
    };

    pub const StopReplicaTopicState = struct {
        pub const StopReplicaPartitionState = struct {
            /// The partition index.
            /// Versions: 3+
            partition_index: i32 = 0,
            /// The leader epoch.
            /// Versions: 3+
            leader_epoch: i32 = -1,
            /// Whether this partition should be deleted.
            /// Versions: 3+
            delete_partition: bool = false,

            pub fn serialize(self: *const StopReplicaPartitionState, buf: []u8, pos: *usize, version: i16) void {
                if (version >= 3) {
                    ser.writeI32(buf, pos, self.partition_index);
                }
                if (version >= 3) {
                    ser.writeI32(buf, pos, self.leader_epoch);
                }
                if (version >= 3) {
                    ser.writeBool(buf, pos, self.delete_partition);
                }
                if (version >= 2) ser.writeEmptyTaggedFields(buf, pos);
            }

            pub fn deserialize(_: Allocator, buf: []const u8, pos: *usize, version: i16) !StopReplicaPartitionState {
                var result = StopReplicaPartitionState{};
                if (version >= 3) {
                    result.partition_index = ser.readI32(buf, pos);
                }
                if (version >= 3) {
                    result.leader_epoch = ser.readI32(buf, pos);
                }
                if (version >= 3) {
                    result.delete_partition = try ser.readBool(buf, pos);
                }
                if (version >= 2) try ser.skipTaggedFields(buf, pos);
                return result;
            }

            pub fn calcSize(_: *const StopReplicaPartitionState, version: i16) usize {
                var size: usize = 0;
                if (version >= 3) {
                    size += 4;
                }
                if (version >= 3) {
                    size += 4;
                }
                if (version >= 3) {
                    size += 1;
                }
                if (version >= 2) size += 1;
                return size;
            }
        };

        /// The topic name.
        /// Versions: 3+
        topic_name: ?[]const u8 = null,
        /// The state of each partition
        /// Versions: 3+
        partition_states: []const StopReplicaPartitionState = &.{},

        pub fn serialize(self: *const StopReplicaTopicState, buf: []u8, pos: *usize, version: i16) void {
            if (version >= 3) {
                if (version >= 2) {
                    ser.writeCompactString(buf, pos, self.topic_name);
                } else {
                    ser.writeString(buf, pos, self.topic_name);
                }
            }
            if (version >= 3) {
                if (version >= 2) {
                    ser.writeCompactArrayLen(buf, pos, self.partition_states.len);
                } else {
                    ser.writeArrayLen(buf, pos, self.partition_states.len);
                }
                for (self.partition_states) |item| {
                    item.serialize(buf, pos, version);
                }
            }
            if (version >= 2) ser.writeEmptyTaggedFields(buf, pos);
        }

        pub fn deserialize(alloc: Allocator, buf: []const u8, pos: *usize, version: i16) !StopReplicaTopicState {
            var result = StopReplicaTopicState{};
            if (version >= 3) {
                result.topic_name = if (version >= 2)
                    try ser.readCompactString(buf, pos)
                else
                    try ser.readString(buf, pos);
            }
            if (version >= 3) {
                const partition_states_len: usize = if (version >= 2)
                    (try ser.readCompactArrayLen(buf, pos)) orelse 0
                else
                    (try ser.readArrayLen(buf, pos)) orelse 0;
                if (partition_states_len > 0) {
                    const partition_states_items = try alloc.alloc(StopReplicaPartitionState, partition_states_len);
                    for (partition_states_items) |*item| {
                        item.* = try StopReplicaPartitionState.deserialize(alloc, buf, pos, version);
                    }
                    result.partition_states = partition_states_items;
                }
            }
            if (version >= 2) try ser.skipTaggedFields(buf, pos);
            return result;
        }

        pub fn calcSize(self: *const StopReplicaTopicState, version: i16) usize {
            var size: usize = 0;
            if (version >= 3) {
                if (version >= 2) {
                    size += ser.compactStringSize(self.topic_name);
                } else {
                    size += ser.stringSize(self.topic_name);
                }
            }
            if (version >= 3) {
                if (version >= 2) {
                    size += ser.unsignedVarintSize(self.partition_states.len + 1);
                } else {
                    size += 4;
                }
                for (self.partition_states) |item| {
                    size += item.calcSize(version);
                }
            }
            if (version >= 2) size += 1;
            return size;
        }
    };

    /// The controller id.
    /// Versions: 0+
    controller_id: i32 = 0,
    /// If KRaft controller id is used during migration. See KIP-866
    /// Versions: 4+
    is_k_raft_controller: bool = false,
    /// The controller epoch.
    /// Versions: 0+
    controller_epoch: i32 = 0,
    /// The broker epoch.
    /// Versions: 1+
    broker_epoch: i64 = -1,
    /// Whether these partitions should be deleted.
    /// Versions: 0-2
    delete_partitions: bool = false,
    /// The partitions to stop.
    /// Versions: 0
    ungrouped_partitions: []const StopReplicaPartitionV0 = &.{},
    /// The topics to stop.
    /// Versions: 1-2
    topics: []const StopReplicaTopicV1 = &.{},
    /// Each topic.
    /// Versions: 3+
    topic_states: []const StopReplicaTopicState = &.{},

    pub fn serialize(self: *const StopReplicaRequest, buf: []u8, pos: *usize, version: i16) void {
        ser.writeI32(buf, pos, self.controller_id);
        if (version >= 4) {
            ser.writeBool(buf, pos, self.is_k_raft_controller);
        }
        ser.writeI32(buf, pos, self.controller_epoch);
        if (version >= 1) {
            ser.writeI64(buf, pos, self.broker_epoch);
        }
        if (version <= 2) {
            ser.writeBool(buf, pos, self.delete_partitions);
        }
        if (version == 0) {
            ser.writeArrayLen(buf, pos, self.ungrouped_partitions.len);
            for (self.ungrouped_partitions) |item| {
                item.serialize(buf, pos, version);
            }
        }
        if (version >= 1 and version <= 2) {
            if (version >= 2) {
                ser.writeCompactArrayLen(buf, pos, self.topics.len);
            } else {
                ser.writeArrayLen(buf, pos, self.topics.len);
            }
            for (self.topics) |item| {
                item.serialize(buf, pos, version);
            }
        }
        if (version >= 3) {
            if (version >= 2) {
                ser.writeCompactArrayLen(buf, pos, self.topic_states.len);
            } else {
                ser.writeArrayLen(buf, pos, self.topic_states.len);
            }
            for (self.topic_states) |item| {
                item.serialize(buf, pos, version);
            }
        }
        if (version >= 2) ser.writeEmptyTaggedFields(buf, pos);
    }

    pub fn deserialize(alloc: Allocator, buf: []const u8, pos: *usize, version: i16) !StopReplicaRequest {
        var result = StopReplicaRequest{};
        result.controller_id = ser.readI32(buf, pos);
        if (version >= 4) {
            result.is_k_raft_controller = try ser.readBool(buf, pos);
        }
        result.controller_epoch = ser.readI32(buf, pos);
        if (version >= 1) {
            result.broker_epoch = ser.readI64(buf, pos);
        }
        if (version <= 2) {
            result.delete_partitions = try ser.readBool(buf, pos);
        }
        if (version == 0) {
            const ungrouped_partitions_len: usize = (try ser.readArrayLen(buf, pos)) orelse 0;
            if (ungrouped_partitions_len > 0) {
                const ungrouped_partitions_items = try alloc.alloc(StopReplicaPartitionV0, ungrouped_partitions_len);
                for (ungrouped_partitions_items) |*item| {
                    item.* = try StopReplicaPartitionV0.deserialize(alloc, buf, pos, version);
                }
                result.ungrouped_partitions = ungrouped_partitions_items;
            }
        }
        if (version >= 1 and version <= 2) {
            const topics_len: usize = if (version >= 2)
                (try ser.readCompactArrayLen(buf, pos)) orelse 0
            else
                (try ser.readArrayLen(buf, pos)) orelse 0;
            if (topics_len > 0) {
                const topics_items = try alloc.alloc(StopReplicaTopicV1, topics_len);
                for (topics_items) |*item| {
                    item.* = try StopReplicaTopicV1.deserialize(alloc, buf, pos, version);
                }
                result.topics = topics_items;
            }
        }
        if (version >= 3) {
            const topic_states_len: usize = if (version >= 2)
                (try ser.readCompactArrayLen(buf, pos)) orelse 0
            else
                (try ser.readArrayLen(buf, pos)) orelse 0;
            if (topic_states_len > 0) {
                const topic_states_items = try alloc.alloc(StopReplicaTopicState, topic_states_len);
                for (topic_states_items) |*item| {
                    item.* = try StopReplicaTopicState.deserialize(alloc, buf, pos, version);
                }
                result.topic_states = topic_states_items;
            }
        }
        if (version >= 2) try ser.skipTaggedFields(buf, pos);
        return result;
    }

    pub fn calcSize(self: *const StopReplicaRequest, version: i16) usize {
        var size: usize = 0;
        size += 4;
        if (version >= 4) {
            size += 1;
        }
        size += 4;
        if (version >= 1) {
            size += 8;
        }
        if (version <= 2) {
            size += 1;
        }
        if (version == 0) {
            size += 4;
            for (self.ungrouped_partitions) |item| {
                size += item.calcSize(version);
            }
        }
        if (version >= 1 and version <= 2) {
            if (version >= 2) {
                size += ser.unsignedVarintSize(self.topics.len + 1);
            } else {
                size += 4;
            }
            for (self.topics) |item| {
                size += item.calcSize(version);
            }
        }
        if (version >= 3) {
            if (version >= 2) {
                size += ser.unsignedVarintSize(self.topic_states.len + 1);
            } else {
                size += 4;
            }
            for (self.topic_states) |item| {
                size += item.calcSize(version);
            }
        }
        if (version >= 2) size += 1;
        return size;
    }
};
