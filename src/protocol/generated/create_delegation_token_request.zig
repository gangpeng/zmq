// Auto-generated from CreateDelegationTokenRequest.json
// API Key: 38, Type: request
// Valid Versions: 0-3, Flexible Versions: 2+
//
// DO NOT EDIT - generated by codegen_v2.py

const std = @import("std");
const ser = @import("../serialization.zig");
const Allocator = std.mem.Allocator;

pub const CreateDelegationTokenRequest = struct {
    pub const CreatableRenewers = struct {
        /// The type of the Kafka principal.
        /// Versions: 0+
        principal_type: ?[]const u8 = null,
        /// The name of the Kafka principal.
        /// Versions: 0+
        principal_name: ?[]const u8 = null,

        pub fn serialize(self: *const CreatableRenewers, buf: []u8, pos: *usize, version: i16) void {
            if (version >= 2) {
                ser.writeCompactString(buf, pos, self.principal_type);
            } else {
                ser.writeString(buf, pos, self.principal_type);
            }
            if (version >= 2) {
                ser.writeCompactString(buf, pos, self.principal_name);
            } else {
                ser.writeString(buf, pos, self.principal_name);
            }
            if (version >= 2) ser.writeEmptyTaggedFields(buf, pos);
        }

        pub fn deserialize(_: Allocator, buf: []const u8, pos: *usize, version: i16) !CreatableRenewers {
            var result = CreatableRenewers{};
            result.principal_type = if (version >= 2)
                try ser.readCompactString(buf, pos)
            else
                try ser.readString(buf, pos);
            result.principal_name = if (version >= 2)
                try ser.readCompactString(buf, pos)
            else
                try ser.readString(buf, pos);
            if (version >= 2) try ser.skipTaggedFields(buf, pos);
            return result;
        }

        pub fn calcSize(self: *const CreatableRenewers, version: i16) usize {
            var size: usize = 0;
            if (version >= 2) {
                size += ser.compactStringSize(self.principal_type);
            } else {
                size += ser.stringSize(self.principal_type);
            }
            if (version >= 2) {
                size += ser.compactStringSize(self.principal_name);
            } else {
                size += ser.stringSize(self.principal_name);
            }
            if (version >= 2) size += 1;
            return size;
        }
    };

    /// The principal type of the owner of the token. If it's null it defaults to the token request principal.
    /// Versions: 3+
    owner_principal_type: ?[]const u8 = null,
    /// The principal name of the owner of the token. If it's null it defaults to the token request principal.
    /// Versions: 3+
    owner_principal_name: ?[]const u8 = null,
    /// A list of those who are allowed to renew this token before it expires.
    /// Versions: 0+
    renewers: []const CreatableRenewers = &.{},
    /// The maximum lifetime of the token in milliseconds, or -1 to use the server side default.
    /// Versions: 0+
    max_lifetime_ms: i64 = 0,

    pub fn serialize(self: *const CreateDelegationTokenRequest, buf: []u8, pos: *usize, version: i16) void {
        if (version >= 3) {
            if (version >= 2) {
                ser.writeCompactString(buf, pos, self.owner_principal_type);
            } else {
                ser.writeString(buf, pos, self.owner_principal_type);
            }
        }
        if (version >= 3) {
            if (version >= 2) {
                ser.writeCompactString(buf, pos, self.owner_principal_name);
            } else {
                ser.writeString(buf, pos, self.owner_principal_name);
            }
        }
        if (version >= 2) {
            ser.writeCompactArrayLen(buf, pos, self.renewers.len);
        } else {
            ser.writeArrayLen(buf, pos, self.renewers.len);
        }
        for (self.renewers) |item| {
            item.serialize(buf, pos, version);
        }
        ser.writeI64(buf, pos, self.max_lifetime_ms);
        if (version >= 2) ser.writeEmptyTaggedFields(buf, pos);
    }

    pub fn deserialize(alloc: Allocator, buf: []const u8, pos: *usize, version: i16) !CreateDelegationTokenRequest {
        var result = CreateDelegationTokenRequest{};
        if (version >= 3) {
            result.owner_principal_type = if (version >= 2)
                try ser.readCompactString(buf, pos)
            else
                try ser.readString(buf, pos);
        }
        if (version >= 3) {
            result.owner_principal_name = if (version >= 2)
                try ser.readCompactString(buf, pos)
            else
                try ser.readString(buf, pos);
        }
        const renewers_len: usize = if (version >= 2)
            (try ser.readCompactArrayLen(buf, pos)) orelse 0
        else
            (try ser.readArrayLen(buf, pos)) orelse 0;
        if (renewers_len > 0) {
            const renewers_items = try alloc.alloc(CreatableRenewers, renewers_len);
            for (renewers_items) |*item| {
                item.* = try CreatableRenewers.deserialize(alloc, buf, pos, version);
            }
            result.renewers = renewers_items;
        }
        result.max_lifetime_ms = ser.readI64(buf, pos);
        if (version >= 2) try ser.skipTaggedFields(buf, pos);
        return result;
    }

    pub fn calcSize(self: *const CreateDelegationTokenRequest, version: i16) usize {
        var size: usize = 0;
        if (version >= 3) {
            if (version >= 2) {
                size += ser.compactStringSize(self.owner_principal_type);
            } else {
                size += ser.stringSize(self.owner_principal_type);
            }
        }
        if (version >= 3) {
            if (version >= 2) {
                size += ser.compactStringSize(self.owner_principal_name);
            } else {
                size += ser.stringSize(self.owner_principal_name);
            }
        }
        if (version >= 2) {
            size += ser.unsignedVarintSize(self.renewers.len + 1);
        } else {
            size += 4;
        }
        for (self.renewers) |item| {
            size += item.calcSize(version);
        }
        size += 8;
        if (version >= 2) size += 1;
        return size;
    }
};
