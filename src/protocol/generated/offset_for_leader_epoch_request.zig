// Auto-generated from OffsetForLeaderEpochRequest.json
// API Key: 23, Type: request
// Valid Versions: 0-4, Flexible Versions: 4+
//
// DO NOT EDIT - generated by codegen_v2.py

const std = @import("std");
const ser = @import("../serialization.zig");
const Allocator = std.mem.Allocator;

pub const OffsetForLeaderEpochRequest = struct {
    pub const OffsetForLeaderTopic = struct {
        pub const OffsetForLeaderPartition = struct {
            /// The partition index.
            /// Versions: 0+
            partition: i32 = 0,
            /// An epoch used to fence consumers/replicas with old metadata. If the epoch provided by the client is larger than the current epoch known to the broker, then the UNKNOWN_LEADER_EPOCH error code will be returned. If the provided epoch is smaller, then the FENCED_LEADER_EPOCH error code will be returned.
            /// Versions: 2+
            current_leader_epoch: i32 = -1,
            /// The epoch to look up an offset for.
            /// Versions: 0+
            leader_epoch: i32 = 0,

            pub fn serialize(self: *const OffsetForLeaderPartition, buf: []u8, pos: *usize, version: i16) void {
                ser.writeI32(buf, pos, self.partition);
                if (version >= 2) {
                    ser.writeI32(buf, pos, self.current_leader_epoch);
                }
                ser.writeI32(buf, pos, self.leader_epoch);
                if (version >= 4) ser.writeEmptyTaggedFields(buf, pos);
            }

            pub fn deserialize(_: Allocator, buf: []const u8, pos: *usize, version: i16) !OffsetForLeaderPartition {
                var result = OffsetForLeaderPartition{};
                result.partition = ser.readI32(buf, pos);
                if (version >= 2) {
                    result.current_leader_epoch = ser.readI32(buf, pos);
                }
                result.leader_epoch = ser.readI32(buf, pos);
                if (version >= 4) try ser.skipTaggedFields(buf, pos);
                return result;
            }

            pub fn calcSize(_: *const OffsetForLeaderPartition, version: i16) usize {
                var size: usize = 0;
                size += 4;
                if (version >= 2) {
                    size += 4;
                }
                size += 4;
                if (version >= 4) size += 1;
                return size;
            }
        };

        /// The topic name.
        /// Versions: 0+
        topic: ?[]const u8 = null,
        /// Each partition to get offsets for.
        /// Versions: 0+
        partitions: []const OffsetForLeaderPartition = &.{},

        pub fn serialize(self: *const OffsetForLeaderTopic, buf: []u8, pos: *usize, version: i16) void {
            if (version >= 4) {
                ser.writeCompactString(buf, pos, self.topic);
            } else {
                ser.writeString(buf, pos, self.topic);
            }
            if (version >= 4) {
                ser.writeCompactArrayLen(buf, pos, self.partitions.len);
            } else {
                ser.writeArrayLen(buf, pos, self.partitions.len);
            }
            for (self.partitions) |item| {
                item.serialize(buf, pos, version);
            }
            if (version >= 4) ser.writeEmptyTaggedFields(buf, pos);
        }

        pub fn deserialize(alloc: Allocator, buf: []const u8, pos: *usize, version: i16) !OffsetForLeaderTopic {
            var result = OffsetForLeaderTopic{};
            result.topic = if (version >= 4)
                try ser.readCompactString(buf, pos)
            else
                try ser.readString(buf, pos);
            const partitions_len: usize = if (version >= 4)
                (try ser.readCompactArrayLen(buf, pos)) orelse 0
            else
                (try ser.readArrayLen(buf, pos)) orelse 0;
            if (partitions_len > 0) {
                const partitions_items = try alloc.alloc(OffsetForLeaderPartition, partitions_len);
                for (partitions_items) |*item| {
                    item.* = try OffsetForLeaderPartition.deserialize(alloc, buf, pos, version);
                }
                result.partitions = partitions_items;
            }
            if (version >= 4) try ser.skipTaggedFields(buf, pos);
            return result;
        }

        pub fn calcSize(self: *const OffsetForLeaderTopic, version: i16) usize {
            var size: usize = 0;
            if (version >= 4) {
                size += ser.compactStringSize(self.topic);
            } else {
                size += ser.stringSize(self.topic);
            }
            if (version >= 4) {
                size += ser.unsignedVarintSize(self.partitions.len + 1);
            } else {
                size += 4;
            }
            for (self.partitions) |item| {
                size += item.calcSize(version);
            }
            if (version >= 4) size += 1;
            return size;
        }
    };

    /// The broker ID of the follower, of -1 if this request is from a consumer.
    /// Versions: 3+
    replica_id: i32 = -2,
    /// Each topic to get offsets for.
    /// Versions: 0+
    topics: []const OffsetForLeaderTopic = &.{},

    pub fn serialize(self: *const OffsetForLeaderEpochRequest, buf: []u8, pos: *usize, version: i16) void {
        if (version >= 3) {
            ser.writeI32(buf, pos, self.replica_id);
        }
        if (version >= 4) {
            ser.writeCompactArrayLen(buf, pos, self.topics.len);
        } else {
            ser.writeArrayLen(buf, pos, self.topics.len);
        }
        for (self.topics) |item| {
            item.serialize(buf, pos, version);
        }
        if (version >= 4) ser.writeEmptyTaggedFields(buf, pos);
    }

    pub fn deserialize(alloc: Allocator, buf: []const u8, pos: *usize, version: i16) !OffsetForLeaderEpochRequest {
        var result = OffsetForLeaderEpochRequest{};
        if (version >= 3) {
            result.replica_id = ser.readI32(buf, pos);
        }
        const topics_len: usize = if (version >= 4)
            (try ser.readCompactArrayLen(buf, pos)) orelse 0
        else
            (try ser.readArrayLen(buf, pos)) orelse 0;
        if (topics_len > 0) {
            const topics_items = try alloc.alloc(OffsetForLeaderTopic, topics_len);
            for (topics_items) |*item| {
                item.* = try OffsetForLeaderTopic.deserialize(alloc, buf, pos, version);
            }
            result.topics = topics_items;
        }
        if (version >= 4) try ser.skipTaggedFields(buf, pos);
        return result;
    }

    pub fn calcSize(self: *const OffsetForLeaderEpochRequest, version: i16) usize {
        var size: usize = 0;
        if (version >= 3) {
            size += 4;
        }
        if (version >= 4) {
            size += ser.unsignedVarintSize(self.topics.len + 1);
        } else {
            size += 4;
        }
        for (self.topics) |item| {
            size += item.calcSize(version);
        }
        if (version >= 4) size += 1;
        return size;
    }
};
