// Auto-generated from DescribeDelegationTokenResponse.json
// API Key: 41, Type: response
// Valid Versions: 0-3, Flexible Versions: 2+
//
// DO NOT EDIT - generated by codegen_v2.py

const std = @import("std");
const ser = @import("../serialization.zig");
const Allocator = std.mem.Allocator;

pub const DescribeDelegationTokenResponse = struct {
    pub const DescribedDelegationToken = struct {
        pub const DescribedDelegationTokenRenewer = struct {
            /// The renewer principal type
            /// Versions: 0+
            principal_type: ?[]const u8 = null,
            /// The renewer principal name
            /// Versions: 0+
            principal_name: ?[]const u8 = null,

            pub fn serialize(self: *const DescribedDelegationTokenRenewer, buf: []u8, pos: *usize, version: i16) void {
                if (version >= 2) {
                    ser.writeCompactString(buf, pos, self.principal_type);
                } else {
                    ser.writeString(buf, pos, self.principal_type);
                }
                if (version >= 2) {
                    ser.writeCompactString(buf, pos, self.principal_name);
                } else {
                    ser.writeString(buf, pos, self.principal_name);
                }
                if (version >= 2) ser.writeEmptyTaggedFields(buf, pos);
            }

            pub fn deserialize(_: Allocator, buf: []const u8, pos: *usize, version: i16) !DescribedDelegationTokenRenewer {
                var result = DescribedDelegationTokenRenewer{};
                result.principal_type = if (version >= 2)
                    try ser.readCompactString(buf, pos)
                else
                    try ser.readString(buf, pos);
                result.principal_name = if (version >= 2)
                    try ser.readCompactString(buf, pos)
                else
                    try ser.readString(buf, pos);
                if (version >= 2) try ser.skipTaggedFields(buf, pos);
                return result;
            }

            pub fn calcSize(self: *const DescribedDelegationTokenRenewer, version: i16) usize {
                var size: usize = 0;
                if (version >= 2) {
                    size += ser.compactStringSize(self.principal_type);
                } else {
                    size += ser.stringSize(self.principal_type);
                }
                if (version >= 2) {
                    size += ser.compactStringSize(self.principal_name);
                } else {
                    size += ser.stringSize(self.principal_name);
                }
                if (version >= 2) size += 1;
                return size;
            }
        };

        /// The token principal type.
        /// Versions: 0+
        principal_type: ?[]const u8 = null,
        /// The token principal name.
        /// Versions: 0+
        principal_name: ?[]const u8 = null,
        /// The principal type of the requester of the token.
        /// Versions: 3+
        token_requester_principal_type: ?[]const u8 = null,
        /// The principal type of the requester of the token.
        /// Versions: 3+
        token_requester_principal_name: ?[]const u8 = null,
        /// The token issue timestamp in milliseconds.
        /// Versions: 0+
        issue_timestamp: i64 = 0,
        /// The token expiry timestamp in milliseconds.
        /// Versions: 0+
        expiry_timestamp: i64 = 0,
        /// The token maximum timestamp length in milliseconds.
        /// Versions: 0+
        max_timestamp: i64 = 0,
        /// The token ID.
        /// Versions: 0+
        token_id: ?[]const u8 = null,
        /// The token HMAC.
        /// Versions: 0+
        hmac: ?[]const u8 = null,
        /// Those who are able to renew this token before it expires.
        /// Versions: 0+
        renewers: []const DescribedDelegationTokenRenewer = &.{},

        pub fn serialize(self: *const DescribedDelegationToken, buf: []u8, pos: *usize, version: i16) void {
            if (version >= 2) {
                ser.writeCompactString(buf, pos, self.principal_type);
            } else {
                ser.writeString(buf, pos, self.principal_type);
            }
            if (version >= 2) {
                ser.writeCompactString(buf, pos, self.principal_name);
            } else {
                ser.writeString(buf, pos, self.principal_name);
            }
            if (version >= 3) {
                if (version >= 2) {
                    ser.writeCompactString(buf, pos, self.token_requester_principal_type);
                } else {
                    ser.writeString(buf, pos, self.token_requester_principal_type);
                }
            }
            if (version >= 3) {
                if (version >= 2) {
                    ser.writeCompactString(buf, pos, self.token_requester_principal_name);
                } else {
                    ser.writeString(buf, pos, self.token_requester_principal_name);
                }
            }
            ser.writeI64(buf, pos, self.issue_timestamp);
            ser.writeI64(buf, pos, self.expiry_timestamp);
            ser.writeI64(buf, pos, self.max_timestamp);
            if (version >= 2) {
                ser.writeCompactString(buf, pos, self.token_id);
            } else {
                ser.writeString(buf, pos, self.token_id);
            }
            if (version >= 2) {
                ser.writeCompactBytes(buf, pos, self.hmac);
            } else {
                ser.writeBytesBuf(buf, pos, self.hmac);
            }
            if (version >= 2) {
                ser.writeCompactArrayLen(buf, pos, self.renewers.len);
            } else {
                ser.writeArrayLen(buf, pos, self.renewers.len);
            }
            for (self.renewers) |item| {
                item.serialize(buf, pos, version);
            }
            if (version >= 2) ser.writeEmptyTaggedFields(buf, pos);
        }

        pub fn deserialize(alloc: Allocator, buf: []const u8, pos: *usize, version: i16) !DescribedDelegationToken {
            var result = DescribedDelegationToken{};
            result.principal_type = if (version >= 2)
                try ser.readCompactString(buf, pos)
            else
                try ser.readString(buf, pos);
            result.principal_name = if (version >= 2)
                try ser.readCompactString(buf, pos)
            else
                try ser.readString(buf, pos);
            if (version >= 3) {
                result.token_requester_principal_type = if (version >= 2)
                    try ser.readCompactString(buf, pos)
                else
                    try ser.readString(buf, pos);
            }
            if (version >= 3) {
                result.token_requester_principal_name = if (version >= 2)
                    try ser.readCompactString(buf, pos)
                else
                    try ser.readString(buf, pos);
            }
            result.issue_timestamp = ser.readI64(buf, pos);
            result.expiry_timestamp = ser.readI64(buf, pos);
            result.max_timestamp = ser.readI64(buf, pos);
            result.token_id = if (version >= 2)
                try ser.readCompactString(buf, pos)
            else
                try ser.readString(buf, pos);
            result.hmac = if (version >= 2)
                try ser.readCompactBytes(buf, pos)
            else
                try ser.readBytes(buf, pos);
            const renewers_len: usize = if (version >= 2)
                (try ser.readCompactArrayLen(buf, pos)) orelse 0
            else
                (try ser.readArrayLen(buf, pos)) orelse 0;
            if (renewers_len > 0) {
                const renewers_items = try alloc.alloc(DescribedDelegationTokenRenewer, renewers_len);
                for (renewers_items) |*item| {
                    item.* = try DescribedDelegationTokenRenewer.deserialize(alloc, buf, pos, version);
                }
                result.renewers = renewers_items;
            }
            if (version >= 2) try ser.skipTaggedFields(buf, pos);
            return result;
        }

        pub fn calcSize(self: *const DescribedDelegationToken, version: i16) usize {
            var size: usize = 0;
            if (version >= 2) {
                size += ser.compactStringSize(self.principal_type);
            } else {
                size += ser.stringSize(self.principal_type);
            }
            if (version >= 2) {
                size += ser.compactStringSize(self.principal_name);
            } else {
                size += ser.stringSize(self.principal_name);
            }
            if (version >= 3) {
                if (version >= 2) {
                    size += ser.compactStringSize(self.token_requester_principal_type);
                } else {
                    size += ser.stringSize(self.token_requester_principal_type);
                }
            }
            if (version >= 3) {
                if (version >= 2) {
                    size += ser.compactStringSize(self.token_requester_principal_name);
                } else {
                    size += ser.stringSize(self.token_requester_principal_name);
                }
            }
            size += 8;
            size += 8;
            size += 8;
            if (version >= 2) {
                size += ser.compactStringSize(self.token_id);
            } else {
                size += ser.stringSize(self.token_id);
            }
            if (version >= 2) {
                size += ser.compactBytesSize(self.hmac);
            } else {
                size += ser.bytesSize(self.hmac);
            }
            if (version >= 2) {
                size += ser.unsignedVarintSize(self.renewers.len + 1);
            } else {
                size += 4;
            }
            for (self.renewers) |item| {
                size += item.calcSize(version);
            }
            if (version >= 2) size += 1;
            return size;
        }
    };

    /// The error code, or 0 if there was no error.
    /// Versions: 0+
    error_code: i16 = 0,
    /// The tokens.
    /// Versions: 0+
    tokens: []const DescribedDelegationToken = &.{},
    /// The duration in milliseconds for which the request was throttled due to a quota violation, or zero if the request did not violate any quota.
    /// Versions: 0+
    throttle_time_ms: i32 = 0,

    pub fn serialize(self: *const DescribeDelegationTokenResponse, buf: []u8, pos: *usize, version: i16) void {
        ser.writeI16(buf, pos, self.error_code);
        if (version >= 2) {
            ser.writeCompactArrayLen(buf, pos, self.tokens.len);
        } else {
            ser.writeArrayLen(buf, pos, self.tokens.len);
        }
        for (self.tokens) |item| {
            item.serialize(buf, pos, version);
        }
        ser.writeI32(buf, pos, self.throttle_time_ms);
        if (version >= 2) ser.writeEmptyTaggedFields(buf, pos);
    }

    pub fn deserialize(alloc: Allocator, buf: []const u8, pos: *usize, version: i16) !DescribeDelegationTokenResponse {
        var result = DescribeDelegationTokenResponse{};
        result.error_code = ser.readI16(buf, pos);
        const tokens_len: usize = if (version >= 2)
            (try ser.readCompactArrayLen(buf, pos)) orelse 0
        else
            (try ser.readArrayLen(buf, pos)) orelse 0;
        if (tokens_len > 0) {
            const tokens_items = try alloc.alloc(DescribedDelegationToken, tokens_len);
            for (tokens_items) |*item| {
                item.* = try DescribedDelegationToken.deserialize(alloc, buf, pos, version);
            }
            result.tokens = tokens_items;
        }
        result.throttle_time_ms = ser.readI32(buf, pos);
        if (version >= 2) try ser.skipTaggedFields(buf, pos);
        return result;
    }

    pub fn calcSize(self: *const DescribeDelegationTokenResponse, version: i16) usize {
        var size: usize = 0;
        size += 2;
        if (version >= 2) {
            size += ser.unsignedVarintSize(self.tokens.len + 1);
        } else {
            size += 4;
        }
        for (self.tokens) |item| {
            size += item.calcSize(version);
        }
        size += 4;
        if (version >= 2) size += 1;
        return size;
    }
};
