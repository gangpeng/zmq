// Auto-generated from ListTransactionsRequest.json
// API Key: 66, Type: request
// Valid Versions: 0-1, Flexible Versions: 0+
//
// DO NOT EDIT - generated by codegen_v2.py

const std = @import("std");
const ser = @import("../serialization.zig");
const Allocator = std.mem.Allocator;

pub const ListTransactionsRequest = struct {
    /// The transaction states to filter by: if empty, all transactions are returned; if non-empty, then only transactions matching one of the filtered states will be returned
    /// Versions: 0+
    state_filters: []const ?[]const u8 = &.{},
    /// The producerIds to filter by: if empty, all transactions will be returned; if non-empty, only transactions which match one of the filtered producerIds will be returned
    /// Versions: 0+
    producer_id_filters: []const i64 = &.{},
    /// Duration (in millis) to filter by: if < 0, all transactions will be returned; otherwise, only transactions running longer than this duration will be returned
    /// Versions: 1+
    duration_filter: i64 = -1,

    pub fn serialize(self: *const ListTransactionsRequest, buf: []u8, pos: *usize, version: i16) void {
        ser.writeCompactArrayLen(buf, pos, self.state_filters.len);
        for (self.state_filters) |item| {
            ser.writeCompactString(buf, pos, item);
        }
        ser.writeCompactArrayLen(buf, pos, self.producer_id_filters.len);
        for (self.producer_id_filters) |item| {
            ser.writeI64(buf, pos, item);
        }
        if (version >= 1) {
            ser.writeI64(buf, pos, self.duration_filter);
        }
        ser.writeEmptyTaggedFields(buf, pos);
    }

    pub fn deserialize(alloc: Allocator, buf: []const u8, pos: *usize, version: i16) !ListTransactionsRequest {
        var result = ListTransactionsRequest{};
        const state_filters_len: usize = (try ser.readCompactArrayLen(buf, pos)) orelse 0;
        if (state_filters_len > 0) {
            const state_filters_items = try alloc.alloc(?[]const u8, state_filters_len);
            for (state_filters_items) |*item| {
                item.* = try ser.readCompactString(buf, pos);
            }
            result.state_filters = state_filters_items;
        }
        const producer_id_filters_len: usize = (try ser.readCompactArrayLen(buf, pos)) orelse 0;
        if (producer_id_filters_len > 0) {
            const producer_id_filters_items = try alloc.alloc(i64, producer_id_filters_len);
            for (producer_id_filters_items) |*item| {
                item.* = ser.readI64(buf, pos);
            }
            result.producer_id_filters = producer_id_filters_items;
        }
        if (version >= 1) {
            result.duration_filter = ser.readI64(buf, pos);
        }
        try ser.skipTaggedFields(buf, pos);
        return result;
    }

    pub fn calcSize(self: *const ListTransactionsRequest, version: i16) usize {
        var size: usize = 0;
        size += ser.unsignedVarintSize(self.state_filters.len + 1);
        for (self.state_filters) |item| {
            size += ser.compactStringSize(item);
        }
        size += ser.unsignedVarintSize(self.producer_id_filters.len + 1);
        size += self.producer_id_filters.len * 8;
        if (version >= 1) {
            size += 8;
        }
        size += 1;
        return size;
    }
};
