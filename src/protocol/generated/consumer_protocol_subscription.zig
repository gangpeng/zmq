// Auto-generated from ConsumerProtocolSubscription.json
// API Key: None, Type: data
// Valid Versions: 0-3, Flexible Versions: none
//
// DO NOT EDIT - generated by codegen_v2.py

const std = @import("std");
const ser = @import("../serialization.zig");
const Allocator = std.mem.Allocator;

pub const ConsumerProtocolSubscription = struct {
    pub const TopicPartition = struct {
        /// 
        /// Versions: 1+
        topic: ?[]const u8 = null,
        /// 
        /// Versions: 1+
        partitions: []const i32 = &.{},

        pub fn serialize(self: *const TopicPartition, buf: []u8, pos: *usize, version: i16) void {
            if (version >= 1) {
                ser.writeString(buf, pos, self.topic);
            }
            if (version >= 1) {
                ser.writeArrayLen(buf, pos, self.partitions.len);
                for (self.partitions) |item| {
                    ser.writeI32(buf, pos, item);
                }
            }
        }

        pub fn deserialize(alloc: Allocator, buf: []const u8, pos: *usize, version: i16) !TopicPartition {
            var result = TopicPartition{};
            if (version >= 1) {
                result.topic = try ser.readString(buf, pos);
            }
            if (version >= 1) {
                const partitions_len: usize = (try ser.readArrayLen(buf, pos)) orelse 0;
                if (partitions_len > 0) {
                    const partitions_items = try alloc.alloc(i32, partitions_len);
                    for (partitions_items) |*item| {
                        item.* = ser.readI32(buf, pos);
                    }
                    result.partitions = partitions_items;
                }
            }
            return result;
        }

        pub fn calcSize(self: *const TopicPartition, version: i16) usize {
            var size: usize = 0;
            if (version >= 1) {
                size += ser.stringSize(self.topic);
            }
            if (version >= 1) {
                size += 4;
                size += self.partitions.len * 4;
            }
            return size;
        }
    };

    /// 
    /// Versions: 0+
    topics: []const ?[]const u8 = &.{},
    /// 
    /// Versions: 0+
    user_data: ?[]const u8 = null,
    /// 
    /// Versions: 1+
    owned_partitions: []const TopicPartition = &.{},
    /// 
    /// Versions: 2+
    generation_id: i32 = -1,
    /// 
    /// Versions: 3+
    rack_id: ?[]const u8 = null,

    pub fn serialize(self: *const ConsumerProtocolSubscription, buf: []u8, pos: *usize, version: i16) void {
        ser.writeArrayLen(buf, pos, self.topics.len);
        for (self.topics) |item| {
            ser.writeString(buf, pos, item);
        }
        ser.writeBytesBuf(buf, pos, self.user_data);
        if (version >= 1) {
            ser.writeArrayLen(buf, pos, self.owned_partitions.len);
            for (self.owned_partitions) |item| {
                item.serialize(buf, pos, version);
            }
        }
        if (version >= 2) {
            ser.writeI32(buf, pos, self.generation_id);
        }
        if (version >= 3) {
            ser.writeString(buf, pos, self.rack_id);
        }
    }

    pub fn deserialize(alloc: Allocator, buf: []const u8, pos: *usize, version: i16) !ConsumerProtocolSubscription {
        var result = ConsumerProtocolSubscription{};
        const topics_len: usize = (try ser.readArrayLen(buf, pos)) orelse 0;
        if (topics_len > 0) {
            const topics_items = try alloc.alloc(?[]const u8, topics_len);
            for (topics_items) |*item| {
                item.* = try ser.readString(buf, pos);
            }
            result.topics = topics_items;
        }
        result.user_data = try ser.readBytes(buf, pos);
        if (version >= 1) {
            const owned_partitions_len: usize = (try ser.readArrayLen(buf, pos)) orelse 0;
            if (owned_partitions_len > 0) {
                const owned_partitions_items = try alloc.alloc(TopicPartition, owned_partitions_len);
                for (owned_partitions_items) |*item| {
                    item.* = try TopicPartition.deserialize(alloc, buf, pos, version);
                }
                result.owned_partitions = owned_partitions_items;
            }
        }
        if (version >= 2) {
            result.generation_id = ser.readI32(buf, pos);
        }
        if (version >= 3) {
            result.rack_id = try ser.readString(buf, pos);
        }
        return result;
    }

    pub fn calcSize(self: *const ConsumerProtocolSubscription, version: i16) usize {
        var size: usize = 0;
        size += 4;
        for (self.topics) |item| {
            size += ser.stringSize(item);
        }
        size += ser.bytesSize(self.user_data);
        if (version >= 1) {
            size += 4;
            for (self.owned_partitions) |item| {
                size += item.calcSize(version);
            }
        }
        if (version >= 2) {
            size += 4;
        }
        if (version >= 3) {
            size += ser.stringSize(self.rack_id);
        }
        return size;
    }
};
