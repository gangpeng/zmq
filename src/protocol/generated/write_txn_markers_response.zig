// Auto-generated from WriteTxnMarkersResponse.json
// API Key: 27, Type: response
// Valid Versions: 0-1, Flexible Versions: 1+
//
// DO NOT EDIT - generated by codegen_v2.py

const std = @import("std");
const ser = @import("../serialization.zig");
const Allocator = std.mem.Allocator;

pub const WriteTxnMarkersResponse = struct {
    pub const WritableTxnMarkerResult = struct {
        pub const WritableTxnMarkerTopicResult = struct {
            pub const WritableTxnMarkerPartitionResult = struct {
                /// The partition index.
                /// Versions: 0+
                partition_index: i32 = 0,
                /// The error code, or 0 if there was no error.
                /// Versions: 0+
                error_code: i16 = 0,

                pub fn serialize(self: *const WritableTxnMarkerPartitionResult, buf: []u8, pos: *usize, version: i16) void {
                    ser.writeI32(buf, pos, self.partition_index);
                    ser.writeI16(buf, pos, self.error_code);
                    if (version >= 1) ser.writeEmptyTaggedFields(buf, pos);
                }

                pub fn deserialize(_: Allocator, buf: []const u8, pos: *usize, version: i16) !WritableTxnMarkerPartitionResult {
                    var result = WritableTxnMarkerPartitionResult{};
                    result.partition_index = ser.readI32(buf, pos);
                    result.error_code = ser.readI16(buf, pos);
                    if (version >= 1) try ser.skipTaggedFields(buf, pos);
                    return result;
                }

                pub fn calcSize(_: *const WritableTxnMarkerPartitionResult, version: i16) usize {
                    var size: usize = 0;
                    size += 4;
                    size += 2;
                    if (version >= 1) size += 1;
                    return size;
                }
            };

            /// The topic name.
            /// Versions: 0+
            name: ?[]const u8 = null,
            /// The results by partition.
            /// Versions: 0+
            partitions: []const WritableTxnMarkerPartitionResult = &.{},

            pub fn serialize(self: *const WritableTxnMarkerTopicResult, buf: []u8, pos: *usize, version: i16) void {
                if (version >= 1) {
                    ser.writeCompactString(buf, pos, self.name);
                } else {
                    ser.writeString(buf, pos, self.name);
                }
                if (version >= 1) {
                    ser.writeCompactArrayLen(buf, pos, self.partitions.len);
                } else {
                    ser.writeArrayLen(buf, pos, self.partitions.len);
                }
                for (self.partitions) |item| {
                    item.serialize(buf, pos, version);
                }
                if (version >= 1) ser.writeEmptyTaggedFields(buf, pos);
            }

            pub fn deserialize(alloc: Allocator, buf: []const u8, pos: *usize, version: i16) !WritableTxnMarkerTopicResult {
                var result = WritableTxnMarkerTopicResult{};
                result.name = if (version >= 1)
                    try ser.readCompactString(buf, pos)
                else
                    try ser.readString(buf, pos);
                const partitions_len: usize = if (version >= 1)
                    (try ser.readCompactArrayLen(buf, pos)) orelse 0
                else
                    (try ser.readArrayLen(buf, pos)) orelse 0;
                if (partitions_len > 0) {
                    const partitions_items = try alloc.alloc(WritableTxnMarkerPartitionResult, partitions_len);
                    for (partitions_items) |*item| {
                        item.* = try WritableTxnMarkerPartitionResult.deserialize(alloc, buf, pos, version);
                    }
                    result.partitions = partitions_items;
                }
                if (version >= 1) try ser.skipTaggedFields(buf, pos);
                return result;
            }

            pub fn calcSize(self: *const WritableTxnMarkerTopicResult, version: i16) usize {
                var size: usize = 0;
                if (version >= 1) {
                    size += ser.compactStringSize(self.name);
                } else {
                    size += ser.stringSize(self.name);
                }
                if (version >= 1) {
                    size += ser.unsignedVarintSize(self.partitions.len + 1);
                } else {
                    size += 4;
                }
                for (self.partitions) |item| {
                    size += item.calcSize(version);
                }
                if (version >= 1) size += 1;
                return size;
            }
        };

        /// The current producer ID in use by the transactional ID.
        /// Versions: 0+
        producer_id: i64 = 0,
        /// The results by topic.
        /// Versions: 0+
        topics: []const WritableTxnMarkerTopicResult = &.{},

        pub fn serialize(self: *const WritableTxnMarkerResult, buf: []u8, pos: *usize, version: i16) void {
            ser.writeI64(buf, pos, self.producer_id);
            if (version >= 1) {
                ser.writeCompactArrayLen(buf, pos, self.topics.len);
            } else {
                ser.writeArrayLen(buf, pos, self.topics.len);
            }
            for (self.topics) |item| {
                item.serialize(buf, pos, version);
            }
            if (version >= 1) ser.writeEmptyTaggedFields(buf, pos);
        }

        pub fn deserialize(alloc: Allocator, buf: []const u8, pos: *usize, version: i16) !WritableTxnMarkerResult {
            var result = WritableTxnMarkerResult{};
            result.producer_id = ser.readI64(buf, pos);
            const topics_len: usize = if (version >= 1)
                (try ser.readCompactArrayLen(buf, pos)) orelse 0
            else
                (try ser.readArrayLen(buf, pos)) orelse 0;
            if (topics_len > 0) {
                const topics_items = try alloc.alloc(WritableTxnMarkerTopicResult, topics_len);
                for (topics_items) |*item| {
                    item.* = try WritableTxnMarkerTopicResult.deserialize(alloc, buf, pos, version);
                }
                result.topics = topics_items;
            }
            if (version >= 1) try ser.skipTaggedFields(buf, pos);
            return result;
        }

        pub fn calcSize(self: *const WritableTxnMarkerResult, version: i16) usize {
            var size: usize = 0;
            size += 8;
            if (version >= 1) {
                size += ser.unsignedVarintSize(self.topics.len + 1);
            } else {
                size += 4;
            }
            for (self.topics) |item| {
                size += item.calcSize(version);
            }
            if (version >= 1) size += 1;
            return size;
        }
    };

    /// The results for writing makers.
    /// Versions: 0+
    markers: []const WritableTxnMarkerResult = &.{},

    pub fn serialize(self: *const WriteTxnMarkersResponse, buf: []u8, pos: *usize, version: i16) void {
        if (version >= 1) {
            ser.writeCompactArrayLen(buf, pos, self.markers.len);
        } else {
            ser.writeArrayLen(buf, pos, self.markers.len);
        }
        for (self.markers) |item| {
            item.serialize(buf, pos, version);
        }
        if (version >= 1) ser.writeEmptyTaggedFields(buf, pos);
    }

    pub fn deserialize(alloc: Allocator, buf: []const u8, pos: *usize, version: i16) !WriteTxnMarkersResponse {
        var result = WriteTxnMarkersResponse{};
        const markers_len: usize = if (version >= 1)
            (try ser.readCompactArrayLen(buf, pos)) orelse 0
        else
            (try ser.readArrayLen(buf, pos)) orelse 0;
        if (markers_len > 0) {
            const markers_items = try alloc.alloc(WritableTxnMarkerResult, markers_len);
            for (markers_items) |*item| {
                item.* = try WritableTxnMarkerResult.deserialize(alloc, buf, pos, version);
            }
            result.markers = markers_items;
        }
        if (version >= 1) try ser.skipTaggedFields(buf, pos);
        return result;
    }

    pub fn calcSize(self: *const WriteTxnMarkersResponse, version: i16) usize {
        var size: usize = 0;
        if (version >= 1) {
            size += ser.unsignedVarintSize(self.markers.len + 1);
        } else {
            size += 4;
        }
        for (self.markers) |item| {
            size += item.calcSize(version);
        }
        if (version >= 1) size += 1;
        return size;
    }
};
