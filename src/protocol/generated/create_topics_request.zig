// Auto-generated from CreateTopicsRequest.json
// API Key: 19, Type: request
// Valid Versions: 0-7, Flexible Versions: 5+
//
// DO NOT EDIT - generated by codegen_v2.py

const std = @import("std");
const ser = @import("../serialization.zig");
const Allocator = std.mem.Allocator;

pub const CreateTopicsRequest = struct {
    pub const CreatableTopic = struct {
        pub const CreatableReplicaAssignment = struct {
            /// The partition index.
            /// Versions: 0+
            partition_index: i32 = 0,
            /// The brokers to place the partition on.
            /// Versions: 0+
            broker_ids: []const i32 = &.{},

            pub fn serialize(self: *const CreatableReplicaAssignment, buf: []u8, pos: *usize, version: i16) void {
                ser.writeI32(buf, pos, self.partition_index);
                if (version >= 5) {
                    ser.writeCompactArrayLen(buf, pos, self.broker_ids.len);
                } else {
                    ser.writeArrayLen(buf, pos, self.broker_ids.len);
                }
                for (self.broker_ids) |item| {
                    ser.writeI32(buf, pos, item);
                }
                if (version >= 5) ser.writeEmptyTaggedFields(buf, pos);
            }

            pub fn deserialize(_: Allocator, buf: []const u8, pos: *usize, version: i16) !CreatableReplicaAssignment {
                var result = CreatableReplicaAssignment{};
                result.partition_index = ser.readI32(buf, pos);
                const broker_ids_len: usize = if (version >= 5)
                    (try ser.readCompactArrayLen(buf, pos)) orelse 0
                else
                    (try ser.readArrayLen(buf, pos)) orelse 0;
                if (broker_ids_len > 0) {
                    pos.* += broker_ids_len * 4;
                }
                if (version >= 5) try ser.skipTaggedFields(buf, pos);
                return result;
            }

            pub fn calcSize(self: *const CreatableReplicaAssignment, version: i16) usize {
                var size: usize = 0;
                size += 4;
                if (version >= 5) {
                    size += ser.unsignedVarintSize(self.broker_ids.len + 1);
                } else {
                    size += 4;
                }
                size += self.broker_ids.len * 4;
                if (version >= 5) size += 1;
                return size;
            }
        };

        pub const CreatableTopicConfig = struct {
            /// The configuration name.
            /// Versions: 0+
            name: ?[]const u8 = null,
            /// The configuration value.
            /// Versions: 0+
            value: ?[]const u8 = null,

            pub fn serialize(self: *const CreatableTopicConfig, buf: []u8, pos: *usize, version: i16) void {
                if (version >= 5) {
                    ser.writeCompactString(buf, pos, self.name);
                } else {
                    ser.writeString(buf, pos, self.name);
                }
                if (version >= 5) {
                    ser.writeCompactString(buf, pos, self.value);
                } else {
                    ser.writeString(buf, pos, self.value);
                }
                if (version >= 5) ser.writeEmptyTaggedFields(buf, pos);
            }

            pub fn deserialize(_: Allocator, buf: []const u8, pos: *usize, version: i16) !CreatableTopicConfig {
                var result = CreatableTopicConfig{};
                result.name = if (version >= 5)
                    try ser.readCompactString(buf, pos)
                else
                    try ser.readString(buf, pos);
                result.value = if (version >= 5)
                    try ser.readCompactString(buf, pos)
                else
                    try ser.readString(buf, pos);
                if (version >= 5) try ser.skipTaggedFields(buf, pos);
                return result;
            }

            pub fn calcSize(self: *const CreatableTopicConfig, version: i16) usize {
                var size: usize = 0;
                if (version >= 5) {
                    size += ser.compactStringSize(self.name);
                } else {
                    size += ser.stringSize(self.name);
                }
                if (version >= 5) {
                    size += ser.compactStringSize(self.value);
                } else {
                    size += ser.stringSize(self.value);
                }
                if (version >= 5) size += 1;
                return size;
            }
        };

        /// The topic name.
        /// Versions: 0+
        name: ?[]const u8 = null,
        /// The number of partitions to create in the topic, or -1 if we are either specifying a manual partition assignment or using the default partitions.
        /// Versions: 0+
        num_partitions: i32 = 0,
        /// The number of replicas to create for each partition in the topic, or -1 if we are either specifying a manual partition assignment or using the default replication factor.
        /// Versions: 0+
        replication_factor: i16 = 0,
        /// The manual partition assignment, or the empty array if we are using automatic assignment.
        /// Versions: 0+
        assignments: []const CreatableReplicaAssignment = &.{},
        /// The custom topic configurations to set.
        /// Versions: 0+
        configs: []const CreatableTopicConfig = &.{},

        pub fn serialize(self: *const CreatableTopic, buf: []u8, pos: *usize, version: i16) void {
            if (version >= 5) {
                ser.writeCompactString(buf, pos, self.name);
            } else {
                ser.writeString(buf, pos, self.name);
            }
            ser.writeI32(buf, pos, self.num_partitions);
            ser.writeI16(buf, pos, self.replication_factor);
            if (version >= 5) {
                ser.writeCompactArrayLen(buf, pos, self.assignments.len);
            } else {
                ser.writeArrayLen(buf, pos, self.assignments.len);
            }
            for (self.assignments) |item| {
                item.serialize(buf, pos, version);
            }
            if (version >= 5) {
                ser.writeCompactArrayLen(buf, pos, self.configs.len);
            } else {
                ser.writeArrayLen(buf, pos, self.configs.len);
            }
            for (self.configs) |item| {
                item.serialize(buf, pos, version);
            }
            if (version >= 5) ser.writeEmptyTaggedFields(buf, pos);
        }

        pub fn deserialize(alloc: Allocator, buf: []const u8, pos: *usize, version: i16) !CreatableTopic {
            var result = CreatableTopic{};
            result.name = if (version >= 5)
                try ser.readCompactString(buf, pos)
            else
                try ser.readString(buf, pos);
            result.num_partitions = ser.readI32(buf, pos);
            result.replication_factor = ser.readI16(buf, pos);
            const assignments_len: usize = if (version >= 5)
                (try ser.readCompactArrayLen(buf, pos)) orelse 0
            else
                (try ser.readArrayLen(buf, pos)) orelse 0;
            if (assignments_len > 0) {
                const assignments_items = try alloc.alloc(CreatableReplicaAssignment, assignments_len);
                for (assignments_items) |*item| {
                    item.* = try CreatableReplicaAssignment.deserialize(alloc, buf, pos, version);
                }
                result.assignments = assignments_items;
            }
            const configs_len: usize = if (version >= 5)
                (try ser.readCompactArrayLen(buf, pos)) orelse 0
            else
                (try ser.readArrayLen(buf, pos)) orelse 0;
            if (configs_len > 0) {
                const configs_items = try alloc.alloc(CreatableTopicConfig, configs_len);
                for (configs_items) |*item| {
                    item.* = try CreatableTopicConfig.deserialize(alloc, buf, pos, version);
                }
                result.configs = configs_items;
            }
            if (version >= 5) try ser.skipTaggedFields(buf, pos);
            return result;
        }

        pub fn calcSize(self: *const CreatableTopic, version: i16) usize {
            var size: usize = 0;
            if (version >= 5) {
                size += ser.compactStringSize(self.name);
            } else {
                size += ser.stringSize(self.name);
            }
            size += 4;
            size += 2;
            if (version >= 5) {
                size += ser.unsignedVarintSize(self.assignments.len + 1);
            } else {
                size += 4;
            }
            for (self.assignments) |item| {
                size += item.calcSize(version);
            }
            if (version >= 5) {
                size += ser.unsignedVarintSize(self.configs.len + 1);
            } else {
                size += 4;
            }
            for (self.configs) |item| {
                size += item.calcSize(version);
            }
            if (version >= 5) size += 1;
            return size;
        }
    };

    /// The topics to create.
    /// Versions: 0+
    topics: []const CreatableTopic = &.{},
    /// How long to wait in milliseconds before timing out the request.
    /// Versions: 0+
    timeout_ms: i32 = 60000,
    /// If true, check that the topics can be created as specified, but don't create anything.
    /// Versions: 1+
    validate_only: bool = false,

    pub fn serialize(self: *const CreateTopicsRequest, buf: []u8, pos: *usize, version: i16) void {
        if (version >= 5) {
            ser.writeCompactArrayLen(buf, pos, self.topics.len);
        } else {
            ser.writeArrayLen(buf, pos, self.topics.len);
        }
        for (self.topics) |item| {
            item.serialize(buf, pos, version);
        }
        ser.writeI32(buf, pos, self.timeout_ms);
        if (version >= 1) {
            ser.writeBool(buf, pos, self.validate_only);
        }
        if (version >= 5) ser.writeEmptyTaggedFields(buf, pos);
    }

    pub fn deserialize(alloc: Allocator, buf: []const u8, pos: *usize, version: i16) !CreateTopicsRequest {
        var result = CreateTopicsRequest{};
        const topics_len: usize = if (version >= 5)
            (try ser.readCompactArrayLen(buf, pos)) orelse 0
        else
            (try ser.readArrayLen(buf, pos)) orelse 0;
        if (topics_len > 0) {
            const topics_items = try alloc.alloc(CreatableTopic, topics_len);
            for (topics_items) |*item| {
                item.* = try CreatableTopic.deserialize(alloc, buf, pos, version);
            }
            result.topics = topics_items;
        }
        result.timeout_ms = ser.readI32(buf, pos);
        if (version >= 1) {
            result.validate_only = try ser.readBool(buf, pos);
        }
        if (version >= 5) try ser.skipTaggedFields(buf, pos);
        return result;
    }

    pub fn calcSize(self: *const CreateTopicsRequest, version: i16) usize {
        var size: usize = 0;
        if (version >= 5) {
            size += ser.unsignedVarintSize(self.topics.len + 1);
        } else {
            size += 4;
        }
        for (self.topics) |item| {
            size += item.calcSize(version);
        }
        size += 4;
        if (version >= 1) {
            size += 1;
        }
        if (version >= 5) size += 1;
        return size;
    }
};
