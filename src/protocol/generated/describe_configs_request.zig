// Auto-generated from DescribeConfigsRequest.json
// API Key: 32, Type: request
// Valid Versions: 0-4, Flexible Versions: 4+
//
// DO NOT EDIT - generated by codegen_v2.py

const std = @import("std");
const ser = @import("../serialization.zig");
const Allocator = std.mem.Allocator;

pub const DescribeConfigsRequest = struct {
    pub const DescribeConfigsResource = struct {
        /// The resource type.
        /// Versions: 0+
        resource_type: i8 = 0,
        /// The resource name.
        /// Versions: 0+
        resource_name: ?[]const u8 = null,
        /// The configuration keys to list, or null to list all configuration keys.
        /// Versions: 0+
        configuration_keys: []const ?[]const u8 = &.{},

        pub fn serialize(self: *const DescribeConfigsResource, buf: []u8, pos: *usize, version: i16) void {
            ser.writeI8(buf, pos, self.resource_type);
            if (version >= 4) {
                ser.writeCompactString(buf, pos, self.resource_name);
            } else {
                ser.writeString(buf, pos, self.resource_name);
            }
            if (version >= 4) {
                ser.writeCompactArrayLen(buf, pos, self.configuration_keys.len);
            } else {
                ser.writeArrayLen(buf, pos, self.configuration_keys.len);
            }
            for (self.configuration_keys) |item| {
                if (version >= 4) {
                    ser.writeCompactString(buf, pos, item);
                } else {
                    ser.writeString(buf, pos, item);
                }
            }
            if (version >= 4) ser.writeEmptyTaggedFields(buf, pos);
        }

        pub fn deserialize(_: Allocator, buf: []const u8, pos: *usize, version: i16) !DescribeConfigsResource {
            var result = DescribeConfigsResource{};
            result.resource_type = ser.readI8(buf, pos);
            result.resource_name = if (version >= 4)
                try ser.readCompactString(buf, pos)
            else
                try ser.readString(buf, pos);
            const configuration_keys_len: usize = if (version >= 4)
                (try ser.readCompactArrayLen(buf, pos)) orelse 0
            else
                (try ser.readArrayLen(buf, pos)) orelse 0;
            for (0..configuration_keys_len) |_| {
                if (version >= 4) {
                    _ = try ser.readCompactString(buf, pos);
                } else {
                    _ = try ser.readString(buf, pos);
                }
            }
            if (version >= 4) try ser.skipTaggedFields(buf, pos);
            return result;
        }

        pub fn calcSize(self: *const DescribeConfigsResource, version: i16) usize {
            var size: usize = 0;
            size += 1;
            if (version >= 4) {
                size += ser.compactStringSize(self.resource_name);
            } else {
                size += ser.stringSize(self.resource_name);
            }
            if (version >= 4) {
                size += ser.unsignedVarintSize(self.configuration_keys.len + 1);
            } else {
                size += 4;
            }
            for (self.configuration_keys) |item| {
                if (version >= 4) {
                    size += ser.compactStringSize(item);
                } else {
                    size += ser.stringSize(item);
                }
            }
            if (version >= 4) size += 1;
            return size;
        }
    };

    /// The resources whose configurations we want to describe.
    /// Versions: 0+
    resources: []const DescribeConfigsResource = &.{},
    /// True if we should include all synonyms.
    /// Versions: 1+
    include_synonyms: bool = false,
    /// True if we should include configuration documentation.
    /// Versions: 3+
    include_documentation: bool = false,

    pub fn serialize(self: *const DescribeConfigsRequest, buf: []u8, pos: *usize, version: i16) void {
        if (version >= 4) {
            ser.writeCompactArrayLen(buf, pos, self.resources.len);
        } else {
            ser.writeArrayLen(buf, pos, self.resources.len);
        }
        for (self.resources) |item| {
            item.serialize(buf, pos, version);
        }
        if (version >= 1) {
            ser.writeBool(buf, pos, self.include_synonyms);
        }
        if (version >= 3) {
            ser.writeBool(buf, pos, self.include_documentation);
        }
        if (version >= 4) ser.writeEmptyTaggedFields(buf, pos);
    }

    pub fn deserialize(alloc: Allocator, buf: []const u8, pos: *usize, version: i16) !DescribeConfigsRequest {
        var result = DescribeConfigsRequest{};
        const resources_len: usize = if (version >= 4)
            (try ser.readCompactArrayLen(buf, pos)) orelse 0
        else
            (try ser.readArrayLen(buf, pos)) orelse 0;
        if (resources_len > 0) {
            const resources_items = try alloc.alloc(DescribeConfigsResource, resources_len);
            for (resources_items) |*item| {
                item.* = try DescribeConfigsResource.deserialize(alloc, buf, pos, version);
            }
            result.resources = resources_items;
        }
        if (version >= 1) {
            result.include_synonyms = try ser.readBool(buf, pos);
        }
        if (version >= 3) {
            result.include_documentation = try ser.readBool(buf, pos);
        }
        if (version >= 4) try ser.skipTaggedFields(buf, pos);
        return result;
    }

    pub fn calcSize(self: *const DescribeConfigsRequest, version: i16) usize {
        var size: usize = 0;
        if (version >= 4) {
            size += ser.unsignedVarintSize(self.resources.len + 1);
        } else {
            size += 4;
        }
        for (self.resources) |item| {
            size += item.calcSize(version);
        }
        if (version >= 1) {
            size += 1;
        }
        if (version >= 3) {
            size += 1;
        }
        if (version >= 4) size += 1;
        return size;
    }
};
