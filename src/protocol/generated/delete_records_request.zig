// Auto-generated from DeleteRecordsRequest.json
// API Key: 21, Type: request
// Valid Versions: 0-2, Flexible Versions: 2+
//
// DO NOT EDIT - generated by codegen_v2.py

const std = @import("std");
const ser = @import("../serialization.zig");
const Allocator = std.mem.Allocator;

pub const DeleteRecordsRequest = struct {
    pub const DeleteRecordsTopic = struct {
        pub const DeleteRecordsPartition = struct {
            /// The partition index.
            /// Versions: 0+
            partition_index: i32 = 0,
            /// The deletion offset.
            /// Versions: 0+
            offset: i64 = 0,

            pub fn serialize(self: *const DeleteRecordsPartition, buf: []u8, pos: *usize, version: i16) void {
                ser.writeI32(buf, pos, self.partition_index);
                ser.writeI64(buf, pos, self.offset);
                if (version >= 2) ser.writeEmptyTaggedFields(buf, pos);
            }

            pub fn deserialize(_: Allocator, buf: []const u8, pos: *usize, version: i16) !DeleteRecordsPartition {
                var result = DeleteRecordsPartition{};
                result.partition_index = ser.readI32(buf, pos);
                result.offset = ser.readI64(buf, pos);
                if (version >= 2) try ser.skipTaggedFields(buf, pos);
                return result;
            }

            pub fn calcSize(_: *const DeleteRecordsPartition, version: i16) usize {
                var size: usize = 0;
                size += 4;
                size += 8;
                if (version >= 2) size += 1;
                return size;
            }
        };

        /// The topic name.
        /// Versions: 0+
        name: ?[]const u8 = null,
        /// Each partition that we want to delete records from.
        /// Versions: 0+
        partitions: []const DeleteRecordsPartition = &.{},

        pub fn serialize(self: *const DeleteRecordsTopic, buf: []u8, pos: *usize, version: i16) void {
            if (version >= 2) {
                ser.writeCompactString(buf, pos, self.name);
            } else {
                ser.writeString(buf, pos, self.name);
            }
            if (version >= 2) {
                ser.writeCompactArrayLen(buf, pos, self.partitions.len);
            } else {
                ser.writeArrayLen(buf, pos, self.partitions.len);
            }
            for (self.partitions) |item| {
                item.serialize(buf, pos, version);
            }
            if (version >= 2) ser.writeEmptyTaggedFields(buf, pos);
        }

        pub fn deserialize(alloc: Allocator, buf: []const u8, pos: *usize, version: i16) !DeleteRecordsTopic {
            var result = DeleteRecordsTopic{};
            result.name = if (version >= 2)
                try ser.readCompactString(buf, pos)
            else
                try ser.readString(buf, pos);
            const partitions_len: usize = if (version >= 2)
                (try ser.readCompactArrayLen(buf, pos)) orelse 0
            else
                (try ser.readArrayLen(buf, pos)) orelse 0;
            if (partitions_len > 0) {
                const partitions_items = try alloc.alloc(DeleteRecordsPartition, partitions_len);
                for (partitions_items) |*item| {
                    item.* = try DeleteRecordsPartition.deserialize(alloc, buf, pos, version);
                }
                result.partitions = partitions_items;
            }
            if (version >= 2) try ser.skipTaggedFields(buf, pos);
            return result;
        }

        pub fn calcSize(self: *const DeleteRecordsTopic, version: i16) usize {
            var size: usize = 0;
            if (version >= 2) {
                size += ser.compactStringSize(self.name);
            } else {
                size += ser.stringSize(self.name);
            }
            if (version >= 2) {
                size += ser.unsignedVarintSize(self.partitions.len + 1);
            } else {
                size += 4;
            }
            for (self.partitions) |item| {
                size += item.calcSize(version);
            }
            if (version >= 2) size += 1;
            return size;
        }
    };

    /// Each topic that we want to delete records from.
    /// Versions: 0+
    topics: []const DeleteRecordsTopic = &.{},
    /// How long to wait for the deletion to complete, in milliseconds.
    /// Versions: 0+
    timeout_ms: i32 = 0,

    pub fn serialize(self: *const DeleteRecordsRequest, buf: []u8, pos: *usize, version: i16) void {
        if (version >= 2) {
            ser.writeCompactArrayLen(buf, pos, self.topics.len);
        } else {
            ser.writeArrayLen(buf, pos, self.topics.len);
        }
        for (self.topics) |item| {
            item.serialize(buf, pos, version);
        }
        ser.writeI32(buf, pos, self.timeout_ms);
        if (version >= 2) ser.writeEmptyTaggedFields(buf, pos);
    }

    pub fn deserialize(alloc: Allocator, buf: []const u8, pos: *usize, version: i16) !DeleteRecordsRequest {
        var result = DeleteRecordsRequest{};
        const topics_len: usize = if (version >= 2)
            (try ser.readCompactArrayLen(buf, pos)) orelse 0
        else
            (try ser.readArrayLen(buf, pos)) orelse 0;
        if (topics_len > 0) {
            const topics_items = try alloc.alloc(DeleteRecordsTopic, topics_len);
            for (topics_items) |*item| {
                item.* = try DeleteRecordsTopic.deserialize(alloc, buf, pos, version);
            }
            result.topics = topics_items;
        }
        result.timeout_ms = ser.readI32(buf, pos);
        if (version >= 2) try ser.skipTaggedFields(buf, pos);
        return result;
    }

    pub fn calcSize(self: *const DeleteRecordsRequest, version: i16) usize {
        var size: usize = 0;
        if (version >= 2) {
            size += ser.unsignedVarintSize(self.topics.len + 1);
        } else {
            size += 4;
        }
        for (self.topics) |item| {
            size += item.calcSize(version);
        }
        size += 4;
        if (version >= 2) size += 1;
        return size;
    }
};
