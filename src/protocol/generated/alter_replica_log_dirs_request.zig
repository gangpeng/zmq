// Auto-generated from AlterReplicaLogDirsRequest.json
// API Key: 34, Type: request
// Valid Versions: 0-2, Flexible Versions: 2+
//
// DO NOT EDIT - generated by codegen_v2.py

const std = @import("std");
const ser = @import("../serialization.zig");
const Allocator = std.mem.Allocator;

pub const AlterReplicaLogDirsRequest = struct {
    pub const AlterReplicaLogDir = struct {
        pub const AlterReplicaLogDirTopic = struct {
            /// The topic name.
            /// Versions: 0+
            name: ?[]const u8 = null,
            /// The partition indexes.
            /// Versions: 0+
            partitions: []const i32 = &.{},

            pub fn serialize(self: *const AlterReplicaLogDirTopic, buf: []u8, pos: *usize, version: i16) void {
                if (version >= 2) {
                    ser.writeCompactString(buf, pos, self.name);
                } else {
                    ser.writeString(buf, pos, self.name);
                }
                if (version >= 2) {
                    ser.writeCompactArrayLen(buf, pos, self.partitions.len);
                } else {
                    ser.writeArrayLen(buf, pos, self.partitions.len);
                }
                for (self.partitions) |item| {
                    ser.writeI32(buf, pos, item);
                }
                if (version >= 2) ser.writeEmptyTaggedFields(buf, pos);
            }

            pub fn deserialize(alloc: Allocator, buf: []const u8, pos: *usize, version: i16) !AlterReplicaLogDirTopic {
                var result = AlterReplicaLogDirTopic{};
                result.name = if (version >= 2)
                    try ser.readCompactString(buf, pos)
                else
                    try ser.readString(buf, pos);
                const partitions_len: usize = if (version >= 2)
                    (try ser.readCompactArrayLen(buf, pos)) orelse 0
                else
                    (try ser.readArrayLen(buf, pos)) orelse 0;
                if (partitions_len > 0) {
                    const partitions_items = try alloc.alloc(i32, partitions_len);
                    for (partitions_items) |*item| {
                        item.* = ser.readI32(buf, pos);
                    }
                    result.partitions = partitions_items;
                }
                if (version >= 2) try ser.skipTaggedFields(buf, pos);
                return result;
            }

            pub fn calcSize(self: *const AlterReplicaLogDirTopic, version: i16) usize {
                var size: usize = 0;
                if (version >= 2) {
                    size += ser.compactStringSize(self.name);
                } else {
                    size += ser.stringSize(self.name);
                }
                if (version >= 2) {
                    size += ser.unsignedVarintSize(self.partitions.len + 1);
                } else {
                    size += 4;
                }
                size += self.partitions.len * 4;
                if (version >= 2) size += 1;
                return size;
            }
        };

        /// The absolute directory path.
        /// Versions: 0+
        path: ?[]const u8 = null,
        /// The topics to add to the directory.
        /// Versions: 0+
        topics: []const AlterReplicaLogDirTopic = &.{},

        pub fn serialize(self: *const AlterReplicaLogDir, buf: []u8, pos: *usize, version: i16) void {
            if (version >= 2) {
                ser.writeCompactString(buf, pos, self.path);
            } else {
                ser.writeString(buf, pos, self.path);
            }
            if (version >= 2) {
                ser.writeCompactArrayLen(buf, pos, self.topics.len);
            } else {
                ser.writeArrayLen(buf, pos, self.topics.len);
            }
            for (self.topics) |item| {
                item.serialize(buf, pos, version);
            }
            if (version >= 2) ser.writeEmptyTaggedFields(buf, pos);
        }

        pub fn deserialize(alloc: Allocator, buf: []const u8, pos: *usize, version: i16) !AlterReplicaLogDir {
            var result = AlterReplicaLogDir{};
            result.path = if (version >= 2)
                try ser.readCompactString(buf, pos)
            else
                try ser.readString(buf, pos);
            const topics_len: usize = if (version >= 2)
                (try ser.readCompactArrayLen(buf, pos)) orelse 0
            else
                (try ser.readArrayLen(buf, pos)) orelse 0;
            if (topics_len > 0) {
                const topics_items = try alloc.alloc(AlterReplicaLogDirTopic, topics_len);
                for (topics_items) |*item| {
                    item.* = try AlterReplicaLogDirTopic.deserialize(alloc, buf, pos, version);
                }
                result.topics = topics_items;
            }
            if (version >= 2) try ser.skipTaggedFields(buf, pos);
            return result;
        }

        pub fn calcSize(self: *const AlterReplicaLogDir, version: i16) usize {
            var size: usize = 0;
            if (version >= 2) {
                size += ser.compactStringSize(self.path);
            } else {
                size += ser.stringSize(self.path);
            }
            if (version >= 2) {
                size += ser.unsignedVarintSize(self.topics.len + 1);
            } else {
                size += 4;
            }
            for (self.topics) |item| {
                size += item.calcSize(version);
            }
            if (version >= 2) size += 1;
            return size;
        }
    };

    /// The alterations to make for each directory.
    /// Versions: 0+
    dirs: []const AlterReplicaLogDir = &.{},

    pub fn serialize(self: *const AlterReplicaLogDirsRequest, buf: []u8, pos: *usize, version: i16) void {
        if (version >= 2) {
            ser.writeCompactArrayLen(buf, pos, self.dirs.len);
        } else {
            ser.writeArrayLen(buf, pos, self.dirs.len);
        }
        for (self.dirs) |item| {
            item.serialize(buf, pos, version);
        }
        if (version >= 2) ser.writeEmptyTaggedFields(buf, pos);
    }

    pub fn deserialize(alloc: Allocator, buf: []const u8, pos: *usize, version: i16) !AlterReplicaLogDirsRequest {
        var result = AlterReplicaLogDirsRequest{};
        const dirs_len: usize = if (version >= 2)
            (try ser.readCompactArrayLen(buf, pos)) orelse 0
        else
            (try ser.readArrayLen(buf, pos)) orelse 0;
        if (dirs_len > 0) {
            const dirs_items = try alloc.alloc(AlterReplicaLogDir, dirs_len);
            for (dirs_items) |*item| {
                item.* = try AlterReplicaLogDir.deserialize(alloc, buf, pos, version);
            }
            result.dirs = dirs_items;
        }
        if (version >= 2) try ser.skipTaggedFields(buf, pos);
        return result;
    }

    pub fn calcSize(self: *const AlterReplicaLogDirsRequest, version: i16) usize {
        var size: usize = 0;
        if (version >= 2) {
            size += ser.unsignedVarintSize(self.dirs.len + 1);
        } else {
            size += 4;
        }
        for (self.dirs) |item| {
            size += item.calcSize(version);
        }
        if (version >= 2) size += 1;
        return size;
    }
};
