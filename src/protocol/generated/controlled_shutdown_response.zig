// Auto-generated from ControlledShutdownResponse.json
// API Key: 7, Type: response
// Valid Versions: 0-3, Flexible Versions: 3+
//
// DO NOT EDIT - generated by codegen_v2.py

const std = @import("std");
const ser = @import("../serialization.zig");
const Allocator = std.mem.Allocator;

pub const ControlledShutdownResponse = struct {
    pub const RemainingPartition = struct {
        /// The name of the topic.
        /// Versions: 0+
        topic_name: ?[]const u8 = null,
        /// The index of the partition.
        /// Versions: 0+
        partition_index: i32 = 0,

        pub fn serialize(self: *const RemainingPartition, buf: []u8, pos: *usize, version: i16) void {
            if (version >= 3) {
                ser.writeCompactString(buf, pos, self.topic_name);
            } else {
                ser.writeString(buf, pos, self.topic_name);
            }
            ser.writeI32(buf, pos, self.partition_index);
            if (version >= 3) ser.writeEmptyTaggedFields(buf, pos);
        }

        pub fn deserialize(_: Allocator, buf: []const u8, pos: *usize, version: i16) !RemainingPartition {
            var result = RemainingPartition{};
            result.topic_name = if (version >= 3)
                try ser.readCompactString(buf, pos)
            else
                try ser.readString(buf, pos);
            result.partition_index = ser.readI32(buf, pos);
            if (version >= 3) try ser.skipTaggedFields(buf, pos);
            return result;
        }

        pub fn calcSize(self: *const RemainingPartition, version: i16) usize {
            var size: usize = 0;
            if (version >= 3) {
                size += ser.compactStringSize(self.topic_name);
            } else {
                size += ser.stringSize(self.topic_name);
            }
            size += 4;
            if (version >= 3) size += 1;
            return size;
        }
    };

    /// The top-level error code.
    /// Versions: 0+
    error_code: i16 = 0,
    /// The partitions that the broker still leads.
    /// Versions: 0+
    remaining_partitions: []const RemainingPartition = &.{},

    pub fn serialize(self: *const ControlledShutdownResponse, buf: []u8, pos: *usize, version: i16) void {
        ser.writeI16(buf, pos, self.error_code);
        if (version >= 3) {
            ser.writeCompactArrayLen(buf, pos, self.remaining_partitions.len);
        } else {
            ser.writeArrayLen(buf, pos, self.remaining_partitions.len);
        }
        for (self.remaining_partitions) |item| {
            item.serialize(buf, pos, version);
        }
        if (version >= 3) ser.writeEmptyTaggedFields(buf, pos);
    }

    pub fn deserialize(alloc: Allocator, buf: []const u8, pos: *usize, version: i16) !ControlledShutdownResponse {
        var result = ControlledShutdownResponse{};
        result.error_code = ser.readI16(buf, pos);
        const remaining_partitions_len: usize = if (version >= 3)
            (try ser.readCompactArrayLen(buf, pos)) orelse 0
        else
            (try ser.readArrayLen(buf, pos)) orelse 0;
        if (remaining_partitions_len > 0) {
            const remaining_partitions_items = try alloc.alloc(RemainingPartition, remaining_partitions_len);
            for (remaining_partitions_items) |*item| {
                item.* = try RemainingPartition.deserialize(alloc, buf, pos, version);
            }
            result.remaining_partitions = remaining_partitions_items;
        }
        if (version >= 3) try ser.skipTaggedFields(buf, pos);
        return result;
    }

    pub fn calcSize(self: *const ControlledShutdownResponse, version: i16) usize {
        var size: usize = 0;
        size += 2;
        if (version >= 3) {
            size += ser.unsignedVarintSize(self.remaining_partitions.len + 1);
        } else {
            size += 4;
        }
        for (self.remaining_partitions) |item| {
            size += item.calcSize(version);
        }
        if (version >= 3) size += 1;
        return size;
    }
};
