// Auto-generated from StopReplicaResponse.json
// API Key: 5, Type: response
// Valid Versions: 0-4, Flexible Versions: 2+
//
// DO NOT EDIT - generated by codegen_v2.py

const std = @import("std");
const ser = @import("../serialization.zig");
const Allocator = std.mem.Allocator;

pub const StopReplicaResponse = struct {
    pub const StopReplicaPartitionError = struct {
        /// The topic name.
        /// Versions: 0+
        topic_name: ?[]const u8 = null,
        /// The partition index.
        /// Versions: 0+
        partition_index: i32 = 0,
        /// The partition error code, or 0 if there was no partition error.
        /// Versions: 0+
        error_code: i16 = 0,

        pub fn serialize(self: *const StopReplicaPartitionError, buf: []u8, pos: *usize, version: i16) void {
            if (version >= 2) {
                ser.writeCompactString(buf, pos, self.topic_name);
            } else {
                ser.writeString(buf, pos, self.topic_name);
            }
            ser.writeI32(buf, pos, self.partition_index);
            ser.writeI16(buf, pos, self.error_code);
            if (version >= 2) ser.writeEmptyTaggedFields(buf, pos);
        }

        pub fn deserialize(_: Allocator, buf: []const u8, pos: *usize, version: i16) !StopReplicaPartitionError {
            var result = StopReplicaPartitionError{};
            result.topic_name = if (version >= 2)
                try ser.readCompactString(buf, pos)
            else
                try ser.readString(buf, pos);
            result.partition_index = ser.readI32(buf, pos);
            result.error_code = ser.readI16(buf, pos);
            if (version >= 2) try ser.skipTaggedFields(buf, pos);
            return result;
        }

        pub fn calcSize(self: *const StopReplicaPartitionError, version: i16) usize {
            var size: usize = 0;
            if (version >= 2) {
                size += ser.compactStringSize(self.topic_name);
            } else {
                size += ser.stringSize(self.topic_name);
            }
            size += 4;
            size += 2;
            if (version >= 2) size += 1;
            return size;
        }
    };

    /// The top-level error code, or 0 if there was no top-level error.
    /// Versions: 0+
    error_code: i16 = 0,
    /// The responses for each partition.
    /// Versions: 0+
    partition_errors: []const StopReplicaPartitionError = &.{},

    pub fn serialize(self: *const StopReplicaResponse, buf: []u8, pos: *usize, version: i16) void {
        ser.writeI16(buf, pos, self.error_code);
        if (version >= 2) {
            ser.writeCompactArrayLen(buf, pos, self.partition_errors.len);
        } else {
            ser.writeArrayLen(buf, pos, self.partition_errors.len);
        }
        for (self.partition_errors) |item| {
            item.serialize(buf, pos, version);
        }
        if (version >= 2) ser.writeEmptyTaggedFields(buf, pos);
    }

    pub fn deserialize(alloc: Allocator, buf: []const u8, pos: *usize, version: i16) !StopReplicaResponse {
        var result = StopReplicaResponse{};
        result.error_code = ser.readI16(buf, pos);
        const partition_errors_len: usize = if (version >= 2)
            (try ser.readCompactArrayLen(buf, pos)) orelse 0
        else
            (try ser.readArrayLen(buf, pos)) orelse 0;
        if (partition_errors_len > 0) {
            const partition_errors_items = try alloc.alloc(StopReplicaPartitionError, partition_errors_len);
            for (partition_errors_items) |*item| {
                item.* = try StopReplicaPartitionError.deserialize(alloc, buf, pos, version);
            }
            result.partition_errors = partition_errors_items;
        }
        if (version >= 2) try ser.skipTaggedFields(buf, pos);
        return result;
    }

    pub fn calcSize(self: *const StopReplicaResponse, version: i16) usize {
        var size: usize = 0;
        size += 2;
        if (version >= 2) {
            size += ser.unsignedVarintSize(self.partition_errors.len + 1);
        } else {
            size += 4;
        }
        for (self.partition_errors) |item| {
            size += item.calcSize(version);
        }
        if (version >= 2) size += 1;
        return size;
    }
};
