// Auto-generated from ConsumerProtocolAssignment.json
// API Key: None, Type: data
// Valid Versions: 0-3, Flexible Versions: none
//
// DO NOT EDIT - generated by codegen_v2.py

const std = @import("std");
const ser = @import("../serialization.zig");
const Allocator = std.mem.Allocator;

pub const ConsumerProtocolAssignment = struct {
    pub const TopicPartition = struct {
        /// 
        /// Versions: 0+
        topic: ?[]const u8 = null,
        /// 
        /// Versions: 0+
        partitions: []const i32 = &.{},

        pub fn serialize(self: *const TopicPartition, buf: []u8, pos: *usize, _: i16) void {
            ser.writeString(buf, pos, self.topic);
            ser.writeArrayLen(buf, pos, self.partitions.len);
            for (self.partitions) |item| {
                ser.writeI32(buf, pos, item);
            }
        }

        pub fn deserialize(_: Allocator, buf: []const u8, pos: *usize, _: i16) !TopicPartition {
            var result = TopicPartition{};
            result.topic = try ser.readString(buf, pos);
            const partitions_len: usize = (try ser.readArrayLen(buf, pos)) orelse 0;
            if (partitions_len > 0) {
                pos.* += partitions_len * 4;
            }
            return result;
        }

        pub fn calcSize(self: *const TopicPartition, _: i16) usize {
            var size: usize = 0;
            size += ser.stringSize(self.topic);
            size += 4;
            size += self.partitions.len * 4;
            return size;
        }
    };

    /// 
    /// Versions: 0+
    assigned_partitions: []const TopicPartition = &.{},
    /// 
    /// Versions: 0+
    user_data: ?[]const u8 = null,

    pub fn serialize(self: *const ConsumerProtocolAssignment, buf: []u8, pos: *usize, version: i16) void {
        ser.writeArrayLen(buf, pos, self.assigned_partitions.len);
        for (self.assigned_partitions) |item| {
            item.serialize(buf, pos, version);
        }
        ser.writeBytesBuf(buf, pos, self.user_data);
    }

    pub fn deserialize(alloc: Allocator, buf: []const u8, pos: *usize, version: i16) !ConsumerProtocolAssignment {
        var result = ConsumerProtocolAssignment{};
        const assigned_partitions_len: usize = (try ser.readArrayLen(buf, pos)) orelse 0;
        if (assigned_partitions_len > 0) {
            const assigned_partitions_items = try alloc.alloc(TopicPartition, assigned_partitions_len);
            for (assigned_partitions_items) |*item| {
                item.* = try TopicPartition.deserialize(alloc, buf, pos, version);
            }
            result.assigned_partitions = assigned_partitions_items;
        }
        result.user_data = try ser.readBytes(buf, pos);
        return result;
    }

    pub fn calcSize(self: *const ConsumerProtocolAssignment, version: i16) usize {
        var size: usize = 0;
        size += 4;
        for (self.assigned_partitions) |item| {
            size += item.calcSize(version);
        }
        size += ser.bytesSize(self.user_data);
        return size;
    }
};
