// Auto-generated from AlterPartitionReassignmentsRequest.json
// API Key: 45, Type: request
// Valid Versions: 0, Flexible Versions: 0+
//
// DO NOT EDIT - generated by codegen_v2.py

const std = @import("std");
const ser = @import("../serialization.zig");
const Allocator = std.mem.Allocator;

pub const AlterPartitionReassignmentsRequest = struct {
    pub const ReassignableTopic = struct {
        pub const ReassignablePartition = struct {
            /// The partition index.
            /// Versions: 0+
            partition_index: i32 = 0,
            /// The replicas to place the partitions on, or null to cancel a pending reassignment for this partition.
            /// Versions: 0+
            replicas: []const i32 = &.{},

            pub fn serialize(self: *const ReassignablePartition, buf: []u8, pos: *usize, _: i16) void {
                ser.writeI32(buf, pos, self.partition_index);
                ser.writeCompactArrayLen(buf, pos, self.replicas.len);
                for (self.replicas) |item| {
                    ser.writeI32(buf, pos, item);
                }
                ser.writeEmptyTaggedFields(buf, pos);
            }

            pub fn deserialize(_: Allocator, buf: []const u8, pos: *usize, _: i16) !ReassignablePartition {
                var result = ReassignablePartition{};
                result.partition_index = ser.readI32(buf, pos);
                const replicas_len: usize = (try ser.readCompactArrayLen(buf, pos)) orelse 0;
                if (replicas_len > 0) {
                    pos.* += replicas_len * 4;
                }
                try ser.skipTaggedFields(buf, pos);
                return result;
            }

            pub fn calcSize(self: *const ReassignablePartition, _: i16) usize {
                var size: usize = 0;
                size += 4;
                size += ser.unsignedVarintSize(self.replicas.len + 1);
                size += self.replicas.len * 4;
                size += 1;
                return size;
            }
        };

        /// The topic name.
        /// Versions: 0+
        name: ?[]const u8 = null,
        /// The partitions to reassign.
        /// Versions: 0+
        partitions: []const ReassignablePartition = &.{},

        pub fn serialize(self: *const ReassignableTopic, buf: []u8, pos: *usize, version: i16) void {
            ser.writeCompactString(buf, pos, self.name);
            ser.writeCompactArrayLen(buf, pos, self.partitions.len);
            for (self.partitions) |item| {
                item.serialize(buf, pos, version);
            }
            ser.writeEmptyTaggedFields(buf, pos);
        }

        pub fn deserialize(alloc: Allocator, buf: []const u8, pos: *usize, version: i16) !ReassignableTopic {
            var result = ReassignableTopic{};
            result.name = try ser.readCompactString(buf, pos);
            const partitions_len: usize = (try ser.readCompactArrayLen(buf, pos)) orelse 0;
            if (partitions_len > 0) {
                const partitions_items = try alloc.alloc(ReassignablePartition, partitions_len);
                for (partitions_items) |*item| {
                    item.* = try ReassignablePartition.deserialize(alloc, buf, pos, version);
                }
                result.partitions = partitions_items;
            }
            try ser.skipTaggedFields(buf, pos);
            return result;
        }

        pub fn calcSize(self: *const ReassignableTopic, version: i16) usize {
            var size: usize = 0;
            size += ser.compactStringSize(self.name);
            size += ser.unsignedVarintSize(self.partitions.len + 1);
            for (self.partitions) |item| {
                size += item.calcSize(version);
            }
            size += 1;
            return size;
        }
    };

    /// The time in ms to wait for the request to complete.
    /// Versions: 0+
    timeout_ms: i32 = 60000,
    /// The topics to reassign.
    /// Versions: 0+
    topics: []const ReassignableTopic = &.{},

    pub fn serialize(self: *const AlterPartitionReassignmentsRequest, buf: []u8, pos: *usize, version: i16) void {
        ser.writeI32(buf, pos, self.timeout_ms);
        ser.writeCompactArrayLen(buf, pos, self.topics.len);
        for (self.topics) |item| {
            item.serialize(buf, pos, version);
        }
        ser.writeEmptyTaggedFields(buf, pos);
    }

    pub fn deserialize(alloc: Allocator, buf: []const u8, pos: *usize, version: i16) !AlterPartitionReassignmentsRequest {
        var result = AlterPartitionReassignmentsRequest{};
        result.timeout_ms = ser.readI32(buf, pos);
        const topics_len: usize = (try ser.readCompactArrayLen(buf, pos)) orelse 0;
        if (topics_len > 0) {
            const topics_items = try alloc.alloc(ReassignableTopic, topics_len);
            for (topics_items) |*item| {
                item.* = try ReassignableTopic.deserialize(alloc, buf, pos, version);
            }
            result.topics = topics_items;
        }
        try ser.skipTaggedFields(buf, pos);
        return result;
    }

    pub fn calcSize(self: *const AlterPartitionReassignmentsRequest, version: i16) usize {
        var size: usize = 0;
        size += 4;
        size += ser.unsignedVarintSize(self.topics.len + 1);
        for (self.topics) |item| {
            size += item.calcSize(version);
        }
        size += 1;
        return size;
    }
};
