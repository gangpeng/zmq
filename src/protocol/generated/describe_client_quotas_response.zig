// Auto-generated from DescribeClientQuotasResponse.json
// API Key: 48, Type: response
// Valid Versions: 0-1, Flexible Versions: 1+
//
// DO NOT EDIT - generated by codegen_v2.py

const std = @import("std");
const ser = @import("../serialization.zig");
const Allocator = std.mem.Allocator;

pub const DescribeClientQuotasResponse = struct {
    pub const EntryData = struct {
        pub const EntityData = struct {
            /// The entity type.
            /// Versions: 0+
            entity_type: ?[]const u8 = null,
            /// The entity name, or null if the default.
            /// Versions: 0+
            entity_name: ?[]const u8 = null,

            pub fn serialize(self: *const EntityData, buf: []u8, pos: *usize, version: i16) void {
                if (version >= 1) {
                    ser.writeCompactString(buf, pos, self.entity_type);
                } else {
                    ser.writeString(buf, pos, self.entity_type);
                }
                if (version >= 1) {
                    ser.writeCompactString(buf, pos, self.entity_name);
                } else {
                    ser.writeString(buf, pos, self.entity_name);
                }
                if (version >= 1) ser.writeEmptyTaggedFields(buf, pos);
            }

            pub fn deserialize(_: Allocator, buf: []const u8, pos: *usize, version: i16) !EntityData {
                var result = EntityData{};
                result.entity_type = if (version >= 1)
                    try ser.readCompactString(buf, pos)
                else
                    try ser.readString(buf, pos);
                result.entity_name = if (version >= 1)
                    try ser.readCompactString(buf, pos)
                else
                    try ser.readString(buf, pos);
                if (version >= 1) try ser.skipTaggedFields(buf, pos);
                return result;
            }

            pub fn calcSize(self: *const EntityData, version: i16) usize {
                var size: usize = 0;
                if (version >= 1) {
                    size += ser.compactStringSize(self.entity_type);
                } else {
                    size += ser.stringSize(self.entity_type);
                }
                if (version >= 1) {
                    size += ser.compactStringSize(self.entity_name);
                } else {
                    size += ser.stringSize(self.entity_name);
                }
                if (version >= 1) size += 1;
                return size;
            }
        };

        pub const ValueData = struct {
            /// The quota configuration key.
            /// Versions: 0+
            key: ?[]const u8 = null,
            /// The quota configuration value.
            /// Versions: 0+
            value: f64 = 0,

            pub fn serialize(self: *const ValueData, buf: []u8, pos: *usize, version: i16) void {
                if (version >= 1) {
                    ser.writeCompactString(buf, pos, self.key);
                } else {
                    ser.writeString(buf, pos, self.key);
                }
                ser.writeF64(buf, pos, self.value);
                if (version >= 1) ser.writeEmptyTaggedFields(buf, pos);
            }

            pub fn deserialize(_: Allocator, buf: []const u8, pos: *usize, version: i16) !ValueData {
                var result = ValueData{};
                result.key = if (version >= 1)
                    try ser.readCompactString(buf, pos)
                else
                    try ser.readString(buf, pos);
                result.value = ser.readF64(buf, pos);
                if (version >= 1) try ser.skipTaggedFields(buf, pos);
                return result;
            }

            pub fn calcSize(self: *const ValueData, version: i16) usize {
                var size: usize = 0;
                if (version >= 1) {
                    size += ser.compactStringSize(self.key);
                } else {
                    size += ser.stringSize(self.key);
                }
                size += 8;
                if (version >= 1) size += 1;
                return size;
            }
        };

        /// The quota entity description.
        /// Versions: 0+
        entity: []const EntityData = &.{},
        /// The quota values for the entity.
        /// Versions: 0+
        values: []const ValueData = &.{},

        pub fn serialize(self: *const EntryData, buf: []u8, pos: *usize, version: i16) void {
            if (version >= 1) {
                ser.writeCompactArrayLen(buf, pos, self.entity.len);
            } else {
                ser.writeArrayLen(buf, pos, self.entity.len);
            }
            for (self.entity) |item| {
                item.serialize(buf, pos, version);
            }
            if (version >= 1) {
                ser.writeCompactArrayLen(buf, pos, self.values.len);
            } else {
                ser.writeArrayLen(buf, pos, self.values.len);
            }
            for (self.values) |item| {
                item.serialize(buf, pos, version);
            }
            if (version >= 1) ser.writeEmptyTaggedFields(buf, pos);
        }

        pub fn deserialize(alloc: Allocator, buf: []const u8, pos: *usize, version: i16) !EntryData {
            var result = EntryData{};
            errdefer {
                if (result.entity.len > 0) alloc.free(result.entity);
                if (result.values.len > 0) alloc.free(result.values);
            }
            const entity_len: usize = if (version >= 1)
                (try ser.readCompactArrayLen(buf, pos)) orelse 0
            else
                (try ser.readArrayLen(buf, pos)) orelse 0;
            if (entity_len > 0) {
                const entity_items = try alloc.alloc(EntityData, entity_len);
                errdefer alloc.free(entity_items);
                for (entity_items) |*item| {
                    item.* = try EntityData.deserialize(alloc, buf, pos, version);
                }
                result.entity = entity_items;
            }
            const values_len: usize = if (version >= 1)
                (try ser.readCompactArrayLen(buf, pos)) orelse 0
            else
                (try ser.readArrayLen(buf, pos)) orelse 0;
            if (values_len > 0) {
                const values_items = try alloc.alloc(ValueData, values_len);
                errdefer alloc.free(values_items);
                for (values_items) |*item| {
                    item.* = try ValueData.deserialize(alloc, buf, pos, version);
                }
                result.values = values_items;
            }
            if (version >= 1) try ser.skipTaggedFields(buf, pos);
            return result;
        }

        pub fn calcSize(self: *const EntryData, version: i16) usize {
            var size: usize = 0;
            if (version >= 1) {
                size += ser.unsignedVarintSize(self.entity.len + 1);
            } else {
                size += 4;
            }
            for (self.entity) |item| {
                size += item.calcSize(version);
            }
            if (version >= 1) {
                size += ser.unsignedVarintSize(self.values.len + 1);
            } else {
                size += 4;
            }
            for (self.values) |item| {
                size += item.calcSize(version);
            }
            if (version >= 1) size += 1;
            return size;
        }
    };

    /// The duration in milliseconds for which the request was throttled due to a quota violation, or zero if the request did not violate any quota.
    /// Versions: 0+
    throttle_time_ms: i32 = 0,
    /// The error code, or `0` if the quota description succeeded.
    /// Versions: 0+
    error_code: i16 = 0,
    /// The error message, or `null` if the quota description succeeded.
    /// Versions: 0+
    error_message: ?[]const u8 = null,
    /// A result entry.
    /// Versions: 0+
    entries: ?[]const EntryData = null,

    pub fn serialize(self: *const DescribeClientQuotasResponse, buf: []u8, pos: *usize, version: i16) void {
        ser.writeI32(buf, pos, self.throttle_time_ms);
        ser.writeI16(buf, pos, self.error_code);
        if (version >= 1) {
            ser.writeCompactString(buf, pos, self.error_message);
        } else {
            ser.writeString(buf, pos, self.error_message);
        }
        if (self.entries) |entries| {
            if (version >= 1) {
                ser.writeCompactArrayLen(buf, pos, entries.len);
            } else {
                ser.writeArrayLen(buf, pos, entries.len);
            }
            for (entries) |item| {
                item.serialize(buf, pos, version);
            }
        } else {
            if (version >= 1) {
                ser.writeCompactArrayLen(buf, pos, null);
            } else {
                ser.writeArrayLen(buf, pos, null);
            }
        }
        if (version >= 1) ser.writeEmptyTaggedFields(buf, pos);
    }

    pub fn deserialize(alloc: Allocator, buf: []const u8, pos: *usize, version: i16) !DescribeClientQuotasResponse {
        var result = DescribeClientQuotasResponse{};
        errdefer if (result.entries) |entries| {
            for (entries) |entry| {
                if (entry.entity.len > 0) alloc.free(entry.entity);
                if (entry.values.len > 0) alloc.free(entry.values);
            }
            if (entries.len > 0) alloc.free(entries);
        };

        result.throttle_time_ms = ser.readI32(buf, pos);
        result.error_code = ser.readI16(buf, pos);
        result.error_message = if (version >= 1)
            try ser.readCompactString(buf, pos)
        else
            try ser.readString(buf, pos);
        const entries_len: ?usize = if (version >= 1)
            try ser.readCompactArrayLen(buf, pos)
        else
            try ser.readArrayLen(buf, pos);
        if (entries_len) |len| {
            if (len == 0) {
                result.entries = &.{};
            } else {
                const entries_items = try alloc.alloc(EntryData, len);
                var entries_init: usize = 0;
                errdefer {
                    for (entries_items[0..entries_init]) |entry| {
                        if (entry.entity.len > 0) alloc.free(entry.entity);
                        if (entry.values.len > 0) alloc.free(entry.values);
                    }
                    alloc.free(entries_items);
                }
                for (entries_items) |*item| {
                    item.* = try EntryData.deserialize(alloc, buf, pos, version);
                    entries_init += 1;
                }
                result.entries = entries_items;
            }
        } else {
            result.entries = null;
        }
        if (version >= 1) try ser.skipTaggedFields(buf, pos);
        return result;
    }

    pub fn calcSize(self: *const DescribeClientQuotasResponse, version: i16) usize {
        var size: usize = 0;
        size += 4;
        size += 2;
        if (version >= 1) {
            size += ser.compactStringSize(self.error_message);
        } else {
            size += ser.stringSize(self.error_message);
        }
        if (self.entries) |entries| {
            if (version >= 1) {
                size += ser.unsignedVarintSize(entries.len + 1);
            } else {
                size += 4;
            }
            for (entries) |item| {
                size += item.calcSize(version);
            }
        } else {
            size += if (version >= 1) 1 else 4;
        }
        if (version >= 1) size += 1;
        return size;
    }
};
