// Auto-generated from DescribeQuorumResponse.json
// API Key: 55, Type: response
// Valid Versions: 0-2, Flexible Versions: 0+
//
// DO NOT EDIT - generated by codegen_v2.py

const std = @import("std");
const ser = @import("../serialization.zig");
const Allocator = std.mem.Allocator;

pub const DescribeQuorumResponse = struct {
    pub const TopicData = struct {
        pub const PartitionData = struct {
            /// The partition index.
            /// Versions: 0+
            partition_index: i32 = 0,
            /// 
            /// Versions: 0+
            error_code: i16 = 0,
            /// The error message, or null if there was no error.
            /// Versions: 2+
            error_message: ?[]const u8 = null,
            /// The ID of the current leader or -1 if the leader is unknown.
            /// Versions: 0+
            leader_id: i32 = 0,
            /// The latest known leader epoch
            /// Versions: 0+
            leader_epoch: i32 = 0,
            /// 
            /// Versions: 0+
            high_watermark: i64 = 0,
            /// 
            /// Versions: 0+
            current_voters: []const ReplicaState = &.{},
            /// 
            /// Versions: 0+
            observers: []const ReplicaState = &.{},

            pub fn serialize(self: *const PartitionData, buf: []u8, pos: *usize, version: i16) void {
                ser.writeI32(buf, pos, self.partition_index);
                ser.writeI16(buf, pos, self.error_code);
                if (version >= 2) {
                    ser.writeCompactString(buf, pos, self.error_message);
                }
                ser.writeI32(buf, pos, self.leader_id);
                ser.writeI32(buf, pos, self.leader_epoch);
                ser.writeI64(buf, pos, self.high_watermark);
                ser.writeCompactArrayLen(buf, pos, self.current_voters.len);
                for (self.current_voters) |item| {
                    item.serialize(buf, pos, version);
                }
                ser.writeCompactArrayLen(buf, pos, self.observers.len);
                for (self.observers) |item| {
                    item.serialize(buf, pos, version);
                }
                ser.writeEmptyTaggedFields(buf, pos);
            }

            pub fn deserialize(_: Allocator, buf: []const u8, pos: *usize, version: i16) !PartitionData {
                var result = PartitionData{};
                result.partition_index = ser.readI32(buf, pos);
                result.error_code = ser.readI16(buf, pos);
                if (version >= 2) {
                    result.error_message = try ser.readCompactString(buf, pos);
                }
                result.leader_id = ser.readI32(buf, pos);
                result.leader_epoch = ser.readI32(buf, pos);
                result.high_watermark = ser.readI64(buf, pos);
                const current_voters_len: usize = (try ser.readCompactArrayLen(buf, pos)) orelse 0;
                _ = current_voters_len;
                const observers_len: usize = (try ser.readCompactArrayLen(buf, pos)) orelse 0;
                _ = observers_len;
                try ser.skipTaggedFields(buf, pos);
                return result;
            }

            pub fn calcSize(self: *const PartitionData, version: i16) usize {
                var size: usize = 0;
                size += 4;
                size += 2;
                if (version >= 2) {
                    size += ser.compactStringSize(self.error_message);
                }
                size += 4;
                size += 4;
                size += 8;
                size += ser.unsignedVarintSize(self.current_voters.len + 1);
                size += ser.unsignedVarintSize(self.observers.len + 1);
                size += 1;
                return size;
            }
        };

        /// The topic name.
        /// Versions: 0+
        topic_name: ?[]const u8 = null,
        /// 
        /// Versions: 0+
        partitions: []const PartitionData = &.{},

        pub fn serialize(self: *const TopicData, buf: []u8, pos: *usize, version: i16) void {
            ser.writeCompactString(buf, pos, self.topic_name);
            ser.writeCompactArrayLen(buf, pos, self.partitions.len);
            for (self.partitions) |item| {
                item.serialize(buf, pos, version);
            }
            ser.writeEmptyTaggedFields(buf, pos);
        }

        pub fn deserialize(alloc: Allocator, buf: []const u8, pos: *usize, version: i16) !TopicData {
            var result = TopicData{};
            result.topic_name = try ser.readCompactString(buf, pos);
            const partitions_len: usize = (try ser.readCompactArrayLen(buf, pos)) orelse 0;
            if (partitions_len > 0) {
                const partitions_items = try alloc.alloc(PartitionData, partitions_len);
                for (partitions_items) |*item| {
                    item.* = try PartitionData.deserialize(alloc, buf, pos, version);
                }
                result.partitions = partitions_items;
            }
            try ser.skipTaggedFields(buf, pos);
            return result;
        }

        pub fn calcSize(self: *const TopicData, version: i16) usize {
            var size: usize = 0;
            size += ser.compactStringSize(self.topic_name);
            size += ser.unsignedVarintSize(self.partitions.len + 1);
            for (self.partitions) |item| {
                size += item.calcSize(version);
            }
            size += 1;
            return size;
        }
    };

    pub const Node = struct {
        pub const Listener = struct {
            /// The name of the endpoint
            /// Versions: 2+
            name: ?[]const u8 = null,
            /// The hostname
            /// Versions: 2+
            host: ?[]const u8 = null,
            /// The port
            /// Versions: 2+
            port: u16 = 0,

            pub fn serialize(self: *const Listener, buf: []u8, pos: *usize, version: i16) void {
                if (version >= 2) {
                    ser.writeCompactString(buf, pos, self.name);
                }
                if (version >= 2) {
                    ser.writeCompactString(buf, pos, self.host);
                }
                if (version >= 2) {
                    ser.writeU16(buf, pos, self.port);
                }
                ser.writeEmptyTaggedFields(buf, pos);
            }

            pub fn deserialize(_: Allocator, buf: []const u8, pos: *usize, version: i16) !Listener {
                var result = Listener{};
                if (version >= 2) {
                    result.name = try ser.readCompactString(buf, pos);
                }
                if (version >= 2) {
                    result.host = try ser.readCompactString(buf, pos);
                }
                if (version >= 2) {
                    result.port = ser.readU16(buf, pos);
                }
                try ser.skipTaggedFields(buf, pos);
                return result;
            }

            pub fn calcSize(self: *const Listener, version: i16) usize {
                var size: usize = 0;
                if (version >= 2) {
                    size += ser.compactStringSize(self.name);
                }
                if (version >= 2) {
                    size += ser.compactStringSize(self.host);
                }
                if (version >= 2) {
                    size += 2;
                }
                size += 1;
                return size;
            }
        };

        /// The ID of the associated node
        /// Versions: 2+
        node_id: i32 = 0,
        /// The listeners of this controller
        /// Versions: 2+
        listeners: []const Listener = &.{},

        pub fn serialize(self: *const Node, buf: []u8, pos: *usize, version: i16) void {
            if (version >= 2) {
                ser.writeI32(buf, pos, self.node_id);
            }
            if (version >= 2) {
                ser.writeCompactArrayLen(buf, pos, self.listeners.len);
                for (self.listeners) |item| {
                    item.serialize(buf, pos, version);
                }
            }
            ser.writeEmptyTaggedFields(buf, pos);
        }

        pub fn deserialize(alloc: Allocator, buf: []const u8, pos: *usize, version: i16) !Node {
            var result = Node{};
            if (version >= 2) {
                result.node_id = ser.readI32(buf, pos);
            }
            if (version >= 2) {
                const listeners_len: usize = (try ser.readCompactArrayLen(buf, pos)) orelse 0;
                if (listeners_len > 0) {
                    const listeners_items = try alloc.alloc(Listener, listeners_len);
                    for (listeners_items) |*item| {
                        item.* = try Listener.deserialize(alloc, buf, pos, version);
                    }
                    result.listeners = listeners_items;
                }
            }
            try ser.skipTaggedFields(buf, pos);
            return result;
        }

        pub fn calcSize(self: *const Node, version: i16) usize {
            var size: usize = 0;
            if (version >= 2) {
                size += 4;
            }
            if (version >= 2) {
                size += ser.unsignedVarintSize(self.listeners.len + 1);
                for (self.listeners) |item| {
                    size += item.calcSize(version);
                }
            }
            size += 1;
            return size;
        }
    };

    /// The top level error code.
    /// Versions: 0+
    error_code: i16 = 0,
    /// The error message, or null if there was no error.
    /// Versions: 2+
    error_message: ?[]const u8 = null,
    /// 
    /// Versions: 0+
    topics: []const TopicData = &.{},
    /// 
    /// Versions: 2+
    nodes: []const Node = &.{},

    pub fn serialize(self: *const DescribeQuorumResponse, buf: []u8, pos: *usize, version: i16) void {
        ser.writeI16(buf, pos, self.error_code);
        if (version >= 2) {
            ser.writeCompactString(buf, pos, self.error_message);
        }
        ser.writeCompactArrayLen(buf, pos, self.topics.len);
        for (self.topics) |item| {
            item.serialize(buf, pos, version);
        }
        if (version >= 2) {
            ser.writeCompactArrayLen(buf, pos, self.nodes.len);
            for (self.nodes) |item| {
                item.serialize(buf, pos, version);
            }
        }
        ser.writeEmptyTaggedFields(buf, pos);
    }

    pub fn deserialize(alloc: Allocator, buf: []const u8, pos: *usize, version: i16) !DescribeQuorumResponse {
        var result = DescribeQuorumResponse{};
        result.error_code = ser.readI16(buf, pos);
        if (version >= 2) {
            result.error_message = try ser.readCompactString(buf, pos);
        }
        const topics_len: usize = (try ser.readCompactArrayLen(buf, pos)) orelse 0;
        if (topics_len > 0) {
            const topics_items = try alloc.alloc(TopicData, topics_len);
            for (topics_items) |*item| {
                item.* = try TopicData.deserialize(alloc, buf, pos, version);
            }
            result.topics = topics_items;
        }
        if (version >= 2) {
            const nodes_len: usize = (try ser.readCompactArrayLen(buf, pos)) orelse 0;
            if (nodes_len > 0) {
                const nodes_items = try alloc.alloc(Node, nodes_len);
                for (nodes_items) |*item| {
                    item.* = try Node.deserialize(alloc, buf, pos, version);
                }
                result.nodes = nodes_items;
            }
        }
        try ser.skipTaggedFields(buf, pos);
        return result;
    }

    pub fn calcSize(self: *const DescribeQuorumResponse, version: i16) usize {
        var size: usize = 0;
        size += 2;
        if (version >= 2) {
            size += ser.compactStringSize(self.error_message);
        }
        size += ser.unsignedVarintSize(self.topics.len + 1);
        for (self.topics) |item| {
            size += item.calcSize(version);
        }
        if (version >= 2) {
            size += ser.unsignedVarintSize(self.nodes.len + 1);
            for (self.nodes) |item| {
                size += item.calcSize(version);
            }
        }
        size += 1;
        return size;
    }
};
