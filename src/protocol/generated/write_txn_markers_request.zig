// Auto-generated from WriteTxnMarkersRequest.json
// API Key: 27, Type: request
// Valid Versions: 0-1, Flexible Versions: 1+
//
// DO NOT EDIT - generated by codegen_v2.py

const std = @import("std");
const ser = @import("../serialization.zig");
const Allocator = std.mem.Allocator;

pub const WriteTxnMarkersRequest = struct {
    pub const WritableTxnMarker = struct {
        pub const WritableTxnMarkerTopic = struct {
            /// The topic name.
            /// Versions: 0+
            name: ?[]const u8 = null,
            /// The indexes of the partitions to write transaction markers for.
            /// Versions: 0+
            partition_indexes: []const i32 = &.{},

            pub fn serialize(self: *const WritableTxnMarkerTopic, buf: []u8, pos: *usize, version: i16) void {
                if (version >= 1) {
                    ser.writeCompactString(buf, pos, self.name);
                } else {
                    ser.writeString(buf, pos, self.name);
                }
                if (version >= 1) {
                    ser.writeCompactArrayLen(buf, pos, self.partition_indexes.len);
                } else {
                    ser.writeArrayLen(buf, pos, self.partition_indexes.len);
                }
                for (self.partition_indexes) |item| {
                    ser.writeI32(buf, pos, item);
                }
                if (version >= 1) ser.writeEmptyTaggedFields(buf, pos);
            }

            pub fn deserialize(alloc: Allocator, buf: []const u8, pos: *usize, version: i16) !WritableTxnMarkerTopic {
                var result = WritableTxnMarkerTopic{};
                result.name = if (version >= 1)
                    try ser.readCompactString(buf, pos)
                else
                    try ser.readString(buf, pos);
                const partition_indexes_len: usize = if (version >= 1)
                    (try ser.readCompactArrayLen(buf, pos)) orelse 0
                else
                    (try ser.readArrayLen(buf, pos)) orelse 0;
                if (partition_indexes_len > 0) {
                    const partition_indexes_items = try alloc.alloc(i32, partition_indexes_len);
                    for (partition_indexes_items) |*item| {
                        item.* = ser.readI32(buf, pos);
                    }
                    result.partition_indexes = partition_indexes_items;
                }
                if (version >= 1) try ser.skipTaggedFields(buf, pos);
                return result;
            }

            pub fn calcSize(self: *const WritableTxnMarkerTopic, version: i16) usize {
                var size: usize = 0;
                if (version >= 1) {
                    size += ser.compactStringSize(self.name);
                } else {
                    size += ser.stringSize(self.name);
                }
                if (version >= 1) {
                    size += ser.unsignedVarintSize(self.partition_indexes.len + 1);
                } else {
                    size += 4;
                }
                size += self.partition_indexes.len * 4;
                if (version >= 1) size += 1;
                return size;
            }
        };

        /// The current producer ID.
        /// Versions: 0+
        producer_id: i64 = 0,
        /// The current epoch associated with the producer ID.
        /// Versions: 0+
        producer_epoch: i16 = 0,
        /// The result of the transaction to write to the partitions (false = ABORT, true = COMMIT).
        /// Versions: 0+
        transaction_result: bool = false,
        /// Each topic that we want to write transaction marker(s) for.
        /// Versions: 0+
        topics: []const WritableTxnMarkerTopic = &.{},
        /// Epoch associated with the transaction state partition hosted by this transaction coordinator
        /// Versions: 0+
        coordinator_epoch: i32 = 0,

        pub fn serialize(self: *const WritableTxnMarker, buf: []u8, pos: *usize, version: i16) void {
            ser.writeI64(buf, pos, self.producer_id);
            ser.writeI16(buf, pos, self.producer_epoch);
            ser.writeBool(buf, pos, self.transaction_result);
            if (version >= 1) {
                ser.writeCompactArrayLen(buf, pos, self.topics.len);
            } else {
                ser.writeArrayLen(buf, pos, self.topics.len);
            }
            for (self.topics) |item| {
                item.serialize(buf, pos, version);
            }
            ser.writeI32(buf, pos, self.coordinator_epoch);
            if (version >= 1) ser.writeEmptyTaggedFields(buf, pos);
        }

        pub fn deserialize(alloc: Allocator, buf: []const u8, pos: *usize, version: i16) !WritableTxnMarker {
            var result = WritableTxnMarker{};
            result.producer_id = ser.readI64(buf, pos);
            result.producer_epoch = ser.readI16(buf, pos);
            result.transaction_result = try ser.readBool(buf, pos);
            const topics_len: usize = if (version >= 1)
                (try ser.readCompactArrayLen(buf, pos)) orelse 0
            else
                (try ser.readArrayLen(buf, pos)) orelse 0;
            if (topics_len > 0) {
                const topics_items = try alloc.alloc(WritableTxnMarkerTopic, topics_len);
                for (topics_items) |*item| {
                    item.* = try WritableTxnMarkerTopic.deserialize(alloc, buf, pos, version);
                }
                result.topics = topics_items;
            }
            result.coordinator_epoch = ser.readI32(buf, pos);
            if (version >= 1) try ser.skipTaggedFields(buf, pos);
            return result;
        }

        pub fn calcSize(self: *const WritableTxnMarker, version: i16) usize {
            var size: usize = 0;
            size += 8;
            size += 2;
            size += 1;
            if (version >= 1) {
                size += ser.unsignedVarintSize(self.topics.len + 1);
            } else {
                size += 4;
            }
            for (self.topics) |item| {
                size += item.calcSize(version);
            }
            size += 4;
            if (version >= 1) size += 1;
            return size;
        }
    };

    /// The transaction markers to be written.
    /// Versions: 0+
    markers: []const WritableTxnMarker = &.{},

    pub fn serialize(self: *const WriteTxnMarkersRequest, buf: []u8, pos: *usize, version: i16) void {
        if (version >= 1) {
            ser.writeCompactArrayLen(buf, pos, self.markers.len);
        } else {
            ser.writeArrayLen(buf, pos, self.markers.len);
        }
        for (self.markers) |item| {
            item.serialize(buf, pos, version);
        }
        if (version >= 1) ser.writeEmptyTaggedFields(buf, pos);
    }

    pub fn deserialize(alloc: Allocator, buf: []const u8, pos: *usize, version: i16) !WriteTxnMarkersRequest {
        var result = WriteTxnMarkersRequest{};
        const markers_len: usize = if (version >= 1)
            (try ser.readCompactArrayLen(buf, pos)) orelse 0
        else
            (try ser.readArrayLen(buf, pos)) orelse 0;
        if (markers_len > 0) {
            const markers_items = try alloc.alloc(WritableTxnMarker, markers_len);
            for (markers_items) |*item| {
                item.* = try WritableTxnMarker.deserialize(alloc, buf, pos, version);
            }
            result.markers = markers_items;
        }
        if (version >= 1) try ser.skipTaggedFields(buf, pos);
        return result;
    }

    pub fn calcSize(self: *const WriteTxnMarkersRequest, version: i16) usize {
        var size: usize = 0;
        if (version >= 1) {
            size += ser.unsignedVarintSize(self.markers.len + 1);
        } else {
            size += 4;
        }
        for (self.markers) |item| {
            size += item.calcSize(version);
        }
        if (version >= 1) size += 1;
        return size;
    }
};
