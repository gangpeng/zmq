// Auto-generated from AlterClientQuotasRequest.json
// API Key: 49, Type: request
// Valid Versions: 0-1, Flexible Versions: 1+
//
// DO NOT EDIT - generated by codegen_v2.py

const std = @import("std");
const ser = @import("../serialization.zig");
const Allocator = std.mem.Allocator;

pub const AlterClientQuotasRequest = struct {
    pub const EntryData = struct {
        pub const EntityData = struct {
            /// The entity type.
            /// Versions: 0+
            entity_type: ?[]const u8 = null,
            /// The name of the entity, or null if the default.
            /// Versions: 0+
            entity_name: ?[]const u8 = null,

            pub fn serialize(self: *const EntityData, buf: []u8, pos: *usize, version: i16) void {
                if (version >= 1) {
                    ser.writeCompactString(buf, pos, self.entity_type);
                } else {
                    ser.writeString(buf, pos, self.entity_type);
                }
                if (version >= 1) {
                    ser.writeCompactString(buf, pos, self.entity_name);
                } else {
                    ser.writeString(buf, pos, self.entity_name);
                }
                if (version >= 1) ser.writeEmptyTaggedFields(buf, pos);
            }

            pub fn deserialize(_: Allocator, buf: []const u8, pos: *usize, version: i16) !EntityData {
                var result = EntityData{};
                result.entity_type = if (version >= 1)
                    try ser.readCompactString(buf, pos)
                else
                    try ser.readString(buf, pos);
                result.entity_name = if (version >= 1)
                    try ser.readCompactString(buf, pos)
                else
                    try ser.readString(buf, pos);
                if (version >= 1) try ser.skipTaggedFields(buf, pos);
                return result;
            }

            pub fn calcSize(self: *const EntityData, version: i16) usize {
                var size: usize = 0;
                if (version >= 1) {
                    size += ser.compactStringSize(self.entity_type);
                } else {
                    size += ser.stringSize(self.entity_type);
                }
                if (version >= 1) {
                    size += ser.compactStringSize(self.entity_name);
                } else {
                    size += ser.stringSize(self.entity_name);
                }
                if (version >= 1) size += 1;
                return size;
            }
        };

        pub const OpData = struct {
            /// The quota configuration key.
            /// Versions: 0+
            key: ?[]const u8 = null,
            /// The value to set, otherwise ignored if the value is to be removed.
            /// Versions: 0+
            value: f64 = 0,
            /// Whether the quota configuration value should be removed, otherwise set.
            /// Versions: 0+
            remove: bool = false,

            pub fn serialize(self: *const OpData, buf: []u8, pos: *usize, version: i16) void {
                if (version >= 1) {
                    ser.writeCompactString(buf, pos, self.key);
                } else {
                    ser.writeString(buf, pos, self.key);
                }
                ser.writeF64(buf, pos, self.value);
                ser.writeBool(buf, pos, self.remove);
                if (version >= 1) ser.writeEmptyTaggedFields(buf, pos);
            }

            pub fn deserialize(_: Allocator, buf: []const u8, pos: *usize, version: i16) !OpData {
                var result = OpData{};
                result.key = if (version >= 1)
                    try ser.readCompactString(buf, pos)
                else
                    try ser.readString(buf, pos);
                result.value = ser.readF64(buf, pos);
                result.remove = try ser.readBool(buf, pos);
                if (version >= 1) try ser.skipTaggedFields(buf, pos);
                return result;
            }

            pub fn calcSize(self: *const OpData, version: i16) usize {
                var size: usize = 0;
                if (version >= 1) {
                    size += ser.compactStringSize(self.key);
                } else {
                    size += ser.stringSize(self.key);
                }
                size += 8;
                size += 1;
                if (version >= 1) size += 1;
                return size;
            }
        };

        /// The quota entity to alter.
        /// Versions: 0+
        entity: []const EntityData = &.{},
        /// An individual quota configuration entry to alter.
        /// Versions: 0+
        ops: []const OpData = &.{},

        pub fn serialize(self: *const EntryData, buf: []u8, pos: *usize, version: i16) void {
            if (version >= 1) {
                ser.writeCompactArrayLen(buf, pos, self.entity.len);
            } else {
                ser.writeArrayLen(buf, pos, self.entity.len);
            }
            for (self.entity) |item| {
                item.serialize(buf, pos, version);
            }
            if (version >= 1) {
                ser.writeCompactArrayLen(buf, pos, self.ops.len);
            } else {
                ser.writeArrayLen(buf, pos, self.ops.len);
            }
            for (self.ops) |item| {
                item.serialize(buf, pos, version);
            }
            if (version >= 1) ser.writeEmptyTaggedFields(buf, pos);
        }

        pub fn deserialize(alloc: Allocator, buf: []const u8, pos: *usize, version: i16) !EntryData {
            var result = EntryData{};
            const entity_len: usize = if (version >= 1)
                (try ser.readCompactArrayLen(buf, pos)) orelse 0
            else
                (try ser.readArrayLen(buf, pos)) orelse 0;
            if (entity_len > 0) {
                const entity_items = try alloc.alloc(EntityData, entity_len);
                for (entity_items) |*item| {
                    item.* = try EntityData.deserialize(alloc, buf, pos, version);
                }
                result.entity = entity_items;
            }
            const ops_len: usize = if (version >= 1)
                (try ser.readCompactArrayLen(buf, pos)) orelse 0
            else
                (try ser.readArrayLen(buf, pos)) orelse 0;
            if (ops_len > 0) {
                const ops_items = try alloc.alloc(OpData, ops_len);
                for (ops_items) |*item| {
                    item.* = try OpData.deserialize(alloc, buf, pos, version);
                }
                result.ops = ops_items;
            }
            if (version >= 1) try ser.skipTaggedFields(buf, pos);
            return result;
        }

        pub fn calcSize(self: *const EntryData, version: i16) usize {
            var size: usize = 0;
            if (version >= 1) {
                size += ser.unsignedVarintSize(self.entity.len + 1);
            } else {
                size += 4;
            }
            for (self.entity) |item| {
                size += item.calcSize(version);
            }
            if (version >= 1) {
                size += ser.unsignedVarintSize(self.ops.len + 1);
            } else {
                size += 4;
            }
            for (self.ops) |item| {
                size += item.calcSize(version);
            }
            if (version >= 1) size += 1;
            return size;
        }
    };

    /// The quota configuration entries to alter.
    /// Versions: 0+
    entries: []const EntryData = &.{},
    /// Whether the alteration should be validated, but not performed.
    /// Versions: 0+
    validate_only: bool = false,

    pub fn serialize(self: *const AlterClientQuotasRequest, buf: []u8, pos: *usize, version: i16) void {
        if (version >= 1) {
            ser.writeCompactArrayLen(buf, pos, self.entries.len);
        } else {
            ser.writeArrayLen(buf, pos, self.entries.len);
        }
        for (self.entries) |item| {
            item.serialize(buf, pos, version);
        }
        ser.writeBool(buf, pos, self.validate_only);
        if (version >= 1) ser.writeEmptyTaggedFields(buf, pos);
    }

    pub fn deserialize(alloc: Allocator, buf: []const u8, pos: *usize, version: i16) !AlterClientQuotasRequest {
        var result = AlterClientQuotasRequest{};
        const entries_len: usize = if (version >= 1)
            (try ser.readCompactArrayLen(buf, pos)) orelse 0
        else
            (try ser.readArrayLen(buf, pos)) orelse 0;
        if (entries_len > 0) {
            const entries_items = try alloc.alloc(EntryData, entries_len);
            for (entries_items) |*item| {
                item.* = try EntryData.deserialize(alloc, buf, pos, version);
            }
            result.entries = entries_items;
        }
        result.validate_only = try ser.readBool(buf, pos);
        if (version >= 1) try ser.skipTaggedFields(buf, pos);
        return result;
    }

    pub fn calcSize(self: *const AlterClientQuotasRequest, version: i16) usize {
        var size: usize = 0;
        if (version >= 1) {
            size += ser.unsignedVarintSize(self.entries.len + 1);
        } else {
            size += 4;
        }
        for (self.entries) |item| {
            size += item.calcSize(version);
        }
        size += 1;
        if (version >= 1) size += 1;
        return size;
    }
};
