// Auto-generated from JoinGroupRequest.json
// API Key: 11, Type: request
// Valid Versions: 0-9, Flexible Versions: 6+
//
// DO NOT EDIT - generated by codegen_v2.py

const std = @import("std");
const ser = @import("../serialization.zig");
const Allocator = std.mem.Allocator;

pub const JoinGroupRequest = struct {
    pub const JoinGroupRequestProtocol = struct {
        /// The protocol name.
        /// Versions: 0+
        name: ?[]const u8 = null,
        /// The protocol metadata.
        /// Versions: 0+
        metadata: ?[]const u8 = null,

        pub fn serialize(self: *const JoinGroupRequestProtocol, buf: []u8, pos: *usize, version: i16) void {
            if (version >= 6) {
                ser.writeCompactString(buf, pos, self.name);
            } else {
                ser.writeString(buf, pos, self.name);
            }
            if (version >= 6) {
                ser.writeCompactBytes(buf, pos, self.metadata);
            } else {
                ser.writeBytesBuf(buf, pos, self.metadata);
            }
            if (version >= 6) ser.writeEmptyTaggedFields(buf, pos);
        }

        pub fn deserialize(_: Allocator, buf: []const u8, pos: *usize, version: i16) !JoinGroupRequestProtocol {
            var result = JoinGroupRequestProtocol{};
            result.name = if (version >= 6)
                try ser.readCompactString(buf, pos)
            else
                try ser.readString(buf, pos);
            result.metadata = if (version >= 6)
                try ser.readCompactBytes(buf, pos)
            else
                try ser.readBytes(buf, pos);
            if (version >= 6) try ser.skipTaggedFields(buf, pos);
            return result;
        }

        pub fn calcSize(self: *const JoinGroupRequestProtocol, version: i16) usize {
            var size: usize = 0;
            if (version >= 6) {
                size += ser.compactStringSize(self.name);
            } else {
                size += ser.stringSize(self.name);
            }
            if (version >= 6) {
                size += ser.compactBytesSize(self.metadata);
            } else {
                size += ser.bytesSize(self.metadata);
            }
            if (version >= 6) size += 1;
            return size;
        }
    };

    /// The group identifier.
    /// Versions: 0+
    group_id: ?[]const u8 = null,
    /// The coordinator considers the consumer dead if it receives no heartbeat after this timeout in milliseconds.
    /// Versions: 0+
    session_timeout_ms: i32 = 0,
    /// The maximum time in milliseconds that the coordinator will wait for each member to rejoin when rebalancing the group.
    /// Versions: 1+
    rebalance_timeout_ms: i32 = -1,
    /// The member id assigned by the group coordinator.
    /// Versions: 0+
    member_id: ?[]const u8 = null,
    /// The unique identifier of the consumer instance provided by end user.
    /// Versions: 5+
    group_instance_id: ?[]const u8 = null,
    /// The unique name the for class of protocols implemented by the group we want to join.
    /// Versions: 0+
    protocol_type: ?[]const u8 = null,
    /// The list of protocols that the member supports.
    /// Versions: 0+
    protocols: []const JoinGroupRequestProtocol = &.{},
    /// The reason why the member (re-)joins the group.
    /// Versions: 8+
    reason: ?[]const u8 = null,

    pub fn serialize(self: *const JoinGroupRequest, buf: []u8, pos: *usize, version: i16) void {
        if (version >= 6) {
            ser.writeCompactString(buf, pos, self.group_id);
        } else {
            ser.writeString(buf, pos, self.group_id);
        }
        ser.writeI32(buf, pos, self.session_timeout_ms);
        if (version >= 1) {
            ser.writeI32(buf, pos, self.rebalance_timeout_ms);
        }
        if (version >= 6) {
            ser.writeCompactString(buf, pos, self.member_id);
        } else {
            ser.writeString(buf, pos, self.member_id);
        }
        if (version >= 5) {
            if (version >= 6) {
                ser.writeCompactString(buf, pos, self.group_instance_id);
            } else {
                ser.writeString(buf, pos, self.group_instance_id);
            }
        }
        if (version >= 6) {
            ser.writeCompactString(buf, pos, self.protocol_type);
        } else {
            ser.writeString(buf, pos, self.protocol_type);
        }
        if (version >= 6) {
            ser.writeCompactArrayLen(buf, pos, self.protocols.len);
        } else {
            ser.writeArrayLen(buf, pos, self.protocols.len);
        }
        for (self.protocols) |item| {
            item.serialize(buf, pos, version);
        }
        if (version >= 8) {
            if (version >= 6) {
                ser.writeCompactString(buf, pos, self.reason);
            } else {
                ser.writeString(buf, pos, self.reason);
            }
        }
        if (version >= 6) ser.writeEmptyTaggedFields(buf, pos);
    }

    pub fn deserialize(alloc: Allocator, buf: []const u8, pos: *usize, version: i16) !JoinGroupRequest {
        var result = JoinGroupRequest{};
        result.group_id = if (version >= 6)
            try ser.readCompactString(buf, pos)
        else
            try ser.readString(buf, pos);
        result.session_timeout_ms = ser.readI32(buf, pos);
        if (version >= 1) {
            result.rebalance_timeout_ms = ser.readI32(buf, pos);
        }
        result.member_id = if (version >= 6)
            try ser.readCompactString(buf, pos)
        else
            try ser.readString(buf, pos);
        if (version >= 5) {
            result.group_instance_id = if (version >= 6)
                try ser.readCompactString(buf, pos)
            else
                try ser.readString(buf, pos);
        }
        result.protocol_type = if (version >= 6)
            try ser.readCompactString(buf, pos)
        else
            try ser.readString(buf, pos);
        const protocols_len: usize = if (version >= 6)
            (try ser.readCompactArrayLen(buf, pos)) orelse 0
        else
            (try ser.readArrayLen(buf, pos)) orelse 0;
        if (protocols_len > 0) {
            const protocols_items = try alloc.alloc(JoinGroupRequestProtocol, protocols_len);
            for (protocols_items) |*item| {
                item.* = try JoinGroupRequestProtocol.deserialize(alloc, buf, pos, version);
            }
            result.protocols = protocols_items;
        }
        if (version >= 8) {
            result.reason = if (version >= 6)
                try ser.readCompactString(buf, pos)
            else
                try ser.readString(buf, pos);
        }
        if (version >= 6) try ser.skipTaggedFields(buf, pos);
        return result;
    }

    pub fn calcSize(self: *const JoinGroupRequest, version: i16) usize {
        var size: usize = 0;
        if (version >= 6) {
            size += ser.compactStringSize(self.group_id);
        } else {
            size += ser.stringSize(self.group_id);
        }
        size += 4;
        if (version >= 1) {
            size += 4;
        }
        if (version >= 6) {
            size += ser.compactStringSize(self.member_id);
        } else {
            size += ser.stringSize(self.member_id);
        }
        if (version >= 5) {
            if (version >= 6) {
                size += ser.compactStringSize(self.group_instance_id);
            } else {
                size += ser.stringSize(self.group_instance_id);
            }
        }
        if (version >= 6) {
            size += ser.compactStringSize(self.protocol_type);
        } else {
            size += ser.stringSize(self.protocol_type);
        }
        if (version >= 6) {
            size += ser.unsignedVarintSize(self.protocols.len + 1);
        } else {
            size += 4;
        }
        for (self.protocols) |item| {
            size += item.calcSize(version);
        }
        if (version >= 8) {
            if (version >= 6) {
                size += ser.compactStringSize(self.reason);
            } else {
                size += ser.stringSize(self.reason);
            }
        }
        if (version >= 6) size += 1;
        return size;
    }
};
