// Auto-generated from DescribeStreamsRequest.json
// API Key: 601, Type: request
// Valid Versions: 0, Flexible Versions: 0+
//
// DO NOT EDIT - generated by codegen_v2.py

const std = @import("std");
const ser = @import("../serialization.zig");
const Allocator = std.mem.Allocator;

pub const DescribeStreamsRequest = struct {
    pub const TopicPartitionData = struct {
        pub const PartitionData = struct {
            /// The partition index.
            /// Versions: 0+
            partition_index: i32 = 0,

            pub fn serialize(self: *const PartitionData, buf: []u8, pos: *usize, _: i16) void {
                ser.writeI32(buf, pos, self.partition_index);
                ser.writeEmptyTaggedFields(buf, pos);
            }

            pub fn deserialize(_: Allocator, buf: []const u8, pos: *usize, _: i16) !PartitionData {
                var result = PartitionData{};
                result.partition_index = ser.readI32(buf, pos);
                try ser.skipTaggedFields(buf, pos);
                return result;
            }

            pub fn calcSize(_: *const PartitionData, _: i16) usize {
                var size: usize = 0;
                size += 4;
                size += 1;
                return size;
            }
        };

        /// The topic name.
        /// Versions: 0+
        topic_name: ?[]const u8 = null,
        /// 
        /// Versions: 0+
        partitions: []const PartitionData = &.{},

        pub fn serialize(self: *const TopicPartitionData, buf: []u8, pos: *usize, version: i16) void {
            ser.writeCompactString(buf, pos, self.topic_name);
            ser.writeCompactArrayLen(buf, pos, self.partitions.len);
            for (self.partitions) |item| {
                item.serialize(buf, pos, version);
            }
            ser.writeEmptyTaggedFields(buf, pos);
        }

        pub fn deserialize(alloc: Allocator, buf: []const u8, pos: *usize, version: i16) !TopicPartitionData {
            var result = TopicPartitionData{};
            result.topic_name = try ser.readCompactString(buf, pos);
            const partitions_len: usize = (try ser.readCompactArrayLen(buf, pos)) orelse 0;
            if (partitions_len > 0) {
                const partitions_items = try alloc.alloc(PartitionData, partitions_len);
                for (partitions_items) |*item| {
                    item.* = try PartitionData.deserialize(alloc, buf, pos, version);
                }
                result.partitions = partitions_items;
            }
            try ser.skipTaggedFields(buf, pos);
            return result;
        }

        pub fn calcSize(self: *const TopicPartitionData, version: i16) usize {
            var size: usize = 0;
            size += ser.compactStringSize(self.topic_name);
            size += ser.unsignedVarintSize(self.partitions.len + 1);
            for (self.partitions) |item| {
                size += item.calcSize(version);
            }
            size += 1;
            return size;
        }
    };

    /// 
    /// Versions: 0+
    topic_partitions: []const TopicPartitionData = &.{},
    /// The node id.
    /// Versions: 0+
    node_id: i32 = -1,
    /// The stream id.
    /// Versions: 0+
    stream_id: i64 = -1,

    pub fn serialize(self: *const DescribeStreamsRequest, buf: []u8, pos: *usize, version: i16) void {
        ser.writeCompactArrayLen(buf, pos, self.topic_partitions.len);
        for (self.topic_partitions) |item| {
            item.serialize(buf, pos, version);
        }
        ser.writeI32(buf, pos, self.node_id);
        ser.writeI64(buf, pos, self.stream_id);
        ser.writeEmptyTaggedFields(buf, pos);
    }

    pub fn deserialize(alloc: Allocator, buf: []const u8, pos: *usize, version: i16) !DescribeStreamsRequest {
        var result = DescribeStreamsRequest{};
        const topic_partitions_len: usize = (try ser.readCompactArrayLen(buf, pos)) orelse 0;
        if (topic_partitions_len > 0) {
            const topic_partitions_items = try alloc.alloc(TopicPartitionData, topic_partitions_len);
            for (topic_partitions_items) |*item| {
                item.* = try TopicPartitionData.deserialize(alloc, buf, pos, version);
            }
            result.topic_partitions = topic_partitions_items;
        }
        result.node_id = ser.readI32(buf, pos);
        result.stream_id = ser.readI64(buf, pos);
        try ser.skipTaggedFields(buf, pos);
        return result;
    }

    pub fn calcSize(self: *const DescribeStreamsRequest, version: i16) usize {
        var size: usize = 0;
        size += ser.unsignedVarintSize(self.topic_partitions.len + 1);
        for (self.topic_partitions) |item| {
            size += item.calcSize(version);
        }
        size += 4;
        size += 8;
        size += 1;
        return size;
    }
};
