// Auto-generated from DeleteAclsResponse.json
// API Key: 31, Type: response
// Valid Versions: 0-3, Flexible Versions: 2+
//
// DO NOT EDIT - generated by codegen_v2.py

const std = @import("std");
const ser = @import("../serialization.zig");
const Allocator = std.mem.Allocator;

pub const DeleteAclsResponse = struct {
    pub const DeleteAclsFilterResult = struct {
        pub const DeleteAclsMatchingAcl = struct {
            /// The deletion error code, or 0 if the deletion succeeded.
            /// Versions: 0+
            error_code: i16 = 0,
            /// The deletion error message, or null if the deletion succeeded.
            /// Versions: 0+
            error_message: ?[]const u8 = null,
            /// The ACL resource type.
            /// Versions: 0+
            resource_type: i8 = 0,
            /// The ACL resource name.
            /// Versions: 0+
            resource_name: ?[]const u8 = null,
            /// The ACL resource pattern type.
            /// Versions: 1+
            pattern_type: i8 = 3,
            /// The ACL principal.
            /// Versions: 0+
            principal: ?[]const u8 = null,
            /// The ACL host.
            /// Versions: 0+
            host: ?[]const u8 = null,
            /// The ACL operation.
            /// Versions: 0+
            operation: i8 = 0,
            /// The ACL permission type.
            /// Versions: 0+
            permission_type: i8 = 0,

            pub fn serialize(self: *const DeleteAclsMatchingAcl, buf: []u8, pos: *usize, version: i16) void {
                ser.writeI16(buf, pos, self.error_code);
                if (version >= 2) {
                    ser.writeCompactString(buf, pos, self.error_message);
                } else {
                    ser.writeString(buf, pos, self.error_message);
                }
                ser.writeI8(buf, pos, self.resource_type);
                if (version >= 2) {
                    ser.writeCompactString(buf, pos, self.resource_name);
                } else {
                    ser.writeString(buf, pos, self.resource_name);
                }
                if (version >= 1) {
                    ser.writeI8(buf, pos, self.pattern_type);
                }
                if (version >= 2) {
                    ser.writeCompactString(buf, pos, self.principal);
                } else {
                    ser.writeString(buf, pos, self.principal);
                }
                if (version >= 2) {
                    ser.writeCompactString(buf, pos, self.host);
                } else {
                    ser.writeString(buf, pos, self.host);
                }
                ser.writeI8(buf, pos, self.operation);
                ser.writeI8(buf, pos, self.permission_type);
                if (version >= 2) ser.writeEmptyTaggedFields(buf, pos);
            }

            pub fn deserialize(_: Allocator, buf: []const u8, pos: *usize, version: i16) !DeleteAclsMatchingAcl {
                var result = DeleteAclsMatchingAcl{};
                result.error_code = ser.readI16(buf, pos);
                result.error_message = if (version >= 2)
                    try ser.readCompactString(buf, pos)
                else
                    try ser.readString(buf, pos);
                result.resource_type = ser.readI8(buf, pos);
                result.resource_name = if (version >= 2)
                    try ser.readCompactString(buf, pos)
                else
                    try ser.readString(buf, pos);
                if (version >= 1) {
                    result.pattern_type = ser.readI8(buf, pos);
                }
                result.principal = if (version >= 2)
                    try ser.readCompactString(buf, pos)
                else
                    try ser.readString(buf, pos);
                result.host = if (version >= 2)
                    try ser.readCompactString(buf, pos)
                else
                    try ser.readString(buf, pos);
                result.operation = ser.readI8(buf, pos);
                result.permission_type = ser.readI8(buf, pos);
                if (version >= 2) try ser.skipTaggedFields(buf, pos);
                return result;
            }

            pub fn calcSize(self: *const DeleteAclsMatchingAcl, version: i16) usize {
                var size: usize = 0;
                size += 2;
                if (version >= 2) {
                    size += ser.compactStringSize(self.error_message);
                } else {
                    size += ser.stringSize(self.error_message);
                }
                size += 1;
                if (version >= 2) {
                    size += ser.compactStringSize(self.resource_name);
                } else {
                    size += ser.stringSize(self.resource_name);
                }
                if (version >= 1) {
                    size += 1;
                }
                if (version >= 2) {
                    size += ser.compactStringSize(self.principal);
                } else {
                    size += ser.stringSize(self.principal);
                }
                if (version >= 2) {
                    size += ser.compactStringSize(self.host);
                } else {
                    size += ser.stringSize(self.host);
                }
                size += 1;
                size += 1;
                if (version >= 2) size += 1;
                return size;
            }
        };

        /// The error code, or 0 if the filter succeeded.
        /// Versions: 0+
        error_code: i16 = 0,
        /// The error message, or null if the filter succeeded.
        /// Versions: 0+
        error_message: ?[]const u8 = null,
        /// The ACLs which matched this filter.
        /// Versions: 0+
        matching_acls: []const DeleteAclsMatchingAcl = &.{},

        pub fn serialize(self: *const DeleteAclsFilterResult, buf: []u8, pos: *usize, version: i16) void {
            ser.writeI16(buf, pos, self.error_code);
            if (version >= 2) {
                ser.writeCompactString(buf, pos, self.error_message);
            } else {
                ser.writeString(buf, pos, self.error_message);
            }
            if (version >= 2) {
                ser.writeCompactArrayLen(buf, pos, self.matching_acls.len);
            } else {
                ser.writeArrayLen(buf, pos, self.matching_acls.len);
            }
            for (self.matching_acls) |item| {
                item.serialize(buf, pos, version);
            }
            if (version >= 2) ser.writeEmptyTaggedFields(buf, pos);
        }

        pub fn deserialize(alloc: Allocator, buf: []const u8, pos: *usize, version: i16) !DeleteAclsFilterResult {
            var result = DeleteAclsFilterResult{};
            result.error_code = ser.readI16(buf, pos);
            result.error_message = if (version >= 2)
                try ser.readCompactString(buf, pos)
            else
                try ser.readString(buf, pos);
            const matching_acls_len: usize = if (version >= 2)
                (try ser.readCompactArrayLen(buf, pos)) orelse 0
            else
                (try ser.readArrayLen(buf, pos)) orelse 0;
            if (matching_acls_len > 0) {
                const matching_acls_items = try alloc.alloc(DeleteAclsMatchingAcl, matching_acls_len);
                for (matching_acls_items) |*item| {
                    item.* = try DeleteAclsMatchingAcl.deserialize(alloc, buf, pos, version);
                }
                result.matching_acls = matching_acls_items;
            }
            if (version >= 2) try ser.skipTaggedFields(buf, pos);
            return result;
        }

        pub fn calcSize(self: *const DeleteAclsFilterResult, version: i16) usize {
            var size: usize = 0;
            size += 2;
            if (version >= 2) {
                size += ser.compactStringSize(self.error_message);
            } else {
                size += ser.stringSize(self.error_message);
            }
            if (version >= 2) {
                size += ser.unsignedVarintSize(self.matching_acls.len + 1);
            } else {
                size += 4;
            }
            for (self.matching_acls) |item| {
                size += item.calcSize(version);
            }
            if (version >= 2) size += 1;
            return size;
        }
    };

    /// The duration in milliseconds for which the request was throttled due to a quota violation, or zero if the request did not violate any quota.
    /// Versions: 0+
    throttle_time_ms: i32 = 0,
    /// The results for each filter.
    /// Versions: 0+
    filter_results: []const DeleteAclsFilterResult = &.{},

    pub fn serialize(self: *const DeleteAclsResponse, buf: []u8, pos: *usize, version: i16) void {
        ser.writeI32(buf, pos, self.throttle_time_ms);
        if (version >= 2) {
            ser.writeCompactArrayLen(buf, pos, self.filter_results.len);
        } else {
            ser.writeArrayLen(buf, pos, self.filter_results.len);
        }
        for (self.filter_results) |item| {
            item.serialize(buf, pos, version);
        }
        if (version >= 2) ser.writeEmptyTaggedFields(buf, pos);
    }

    pub fn deserialize(alloc: Allocator, buf: []const u8, pos: *usize, version: i16) !DeleteAclsResponse {
        var result = DeleteAclsResponse{};
        result.throttle_time_ms = ser.readI32(buf, pos);
        const filter_results_len: usize = if (version >= 2)
            (try ser.readCompactArrayLen(buf, pos)) orelse 0
        else
            (try ser.readArrayLen(buf, pos)) orelse 0;
        if (filter_results_len > 0) {
            const filter_results_items = try alloc.alloc(DeleteAclsFilterResult, filter_results_len);
            for (filter_results_items) |*item| {
                item.* = try DeleteAclsFilterResult.deserialize(alloc, buf, pos, version);
            }
            result.filter_results = filter_results_items;
        }
        if (version >= 2) try ser.skipTaggedFields(buf, pos);
        return result;
    }

    pub fn calcSize(self: *const DeleteAclsResponse, version: i16) usize {
        var size: usize = 0;
        size += 4;
        if (version >= 2) {
            size += ser.unsignedVarintSize(self.filter_results.len + 1);
        } else {
            size += 4;
        }
        for (self.filter_results) |item| {
            size += item.calcSize(version);
        }
        if (version >= 2) size += 1;
        return size;
    }
};
