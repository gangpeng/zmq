// Auto-generated from OffsetCommitResponse.json
// API Key: 8, Type: response
// Valid Versions: 0-9, Flexible Versions: 8+
//
// DO NOT EDIT - generated by codegen_v2.py

const std = @import("std");
const ser = @import("../serialization.zig");
const Allocator = std.mem.Allocator;

pub const OffsetCommitResponse = struct {
    pub const OffsetCommitResponseTopic = struct {
        pub const OffsetCommitResponsePartition = struct {
            /// The partition index.
            /// Versions: 0+
            partition_index: i32 = 0,
            /// The error code, or 0 if there was no error.
            /// Versions: 0+
            error_code: i16 = 0,

            pub fn serialize(self: *const OffsetCommitResponsePartition, buf: []u8, pos: *usize, version: i16) void {
                ser.writeI32(buf, pos, self.partition_index);
                ser.writeI16(buf, pos, self.error_code);
                if (version >= 8) ser.writeEmptyTaggedFields(buf, pos);
            }

            pub fn deserialize(_: Allocator, buf: []const u8, pos: *usize, version: i16) !OffsetCommitResponsePartition {
                var result = OffsetCommitResponsePartition{};
                result.partition_index = ser.readI32(buf, pos);
                result.error_code = ser.readI16(buf, pos);
                if (version >= 8) try ser.skipTaggedFields(buf, pos);
                return result;
            }

            pub fn calcSize(_: *const OffsetCommitResponsePartition, version: i16) usize {
                var size: usize = 0;
                size += 4;
                size += 2;
                if (version >= 8) size += 1;
                return size;
            }
        };

        /// The topic name.
        /// Versions: 0+
        name: ?[]const u8 = null,
        /// The responses for each partition in the topic.
        /// Versions: 0+
        partitions: []const OffsetCommitResponsePartition = &.{},

        pub fn serialize(self: *const OffsetCommitResponseTopic, buf: []u8, pos: *usize, version: i16) void {
            if (version >= 8) {
                ser.writeCompactString(buf, pos, self.name);
            } else {
                ser.writeString(buf, pos, self.name);
            }
            if (version >= 8) {
                ser.writeCompactArrayLen(buf, pos, self.partitions.len);
            } else {
                ser.writeArrayLen(buf, pos, self.partitions.len);
            }
            for (self.partitions) |item| {
                item.serialize(buf, pos, version);
            }
            if (version >= 8) ser.writeEmptyTaggedFields(buf, pos);
        }

        pub fn deserialize(alloc: Allocator, buf: []const u8, pos: *usize, version: i16) !OffsetCommitResponseTopic {
            var result = OffsetCommitResponseTopic{};
            result.name = if (version >= 8)
                try ser.readCompactString(buf, pos)
            else
                try ser.readString(buf, pos);
            const partitions_len: usize = if (version >= 8)
                (try ser.readCompactArrayLen(buf, pos)) orelse 0
            else
                (try ser.readArrayLen(buf, pos)) orelse 0;
            if (partitions_len > 0) {
                const partitions_items = try alloc.alloc(OffsetCommitResponsePartition, partitions_len);
                for (partitions_items) |*item| {
                    item.* = try OffsetCommitResponsePartition.deserialize(alloc, buf, pos, version);
                }
                result.partitions = partitions_items;
            }
            if (version >= 8) try ser.skipTaggedFields(buf, pos);
            return result;
        }

        pub fn calcSize(self: *const OffsetCommitResponseTopic, version: i16) usize {
            var size: usize = 0;
            if (version >= 8) {
                size += ser.compactStringSize(self.name);
            } else {
                size += ser.stringSize(self.name);
            }
            if (version >= 8) {
                size += ser.unsignedVarintSize(self.partitions.len + 1);
            } else {
                size += 4;
            }
            for (self.partitions) |item| {
                size += item.calcSize(version);
            }
            if (version >= 8) size += 1;
            return size;
        }
    };

    /// The duration in milliseconds for which the request was throttled due to a quota violation, or zero if the request did not violate any quota.
    /// Versions: 3+
    throttle_time_ms: i32 = 0,
    /// The responses for each topic.
    /// Versions: 0+
    topics: []const OffsetCommitResponseTopic = &.{},

    pub fn serialize(self: *const OffsetCommitResponse, buf: []u8, pos: *usize, version: i16) void {
        if (version >= 3) {
            ser.writeI32(buf, pos, self.throttle_time_ms);
        }
        if (version >= 8) {
            ser.writeCompactArrayLen(buf, pos, self.topics.len);
        } else {
            ser.writeArrayLen(buf, pos, self.topics.len);
        }
        for (self.topics) |item| {
            item.serialize(buf, pos, version);
        }
        if (version >= 8) ser.writeEmptyTaggedFields(buf, pos);
    }

    pub fn deserialize(alloc: Allocator, buf: []const u8, pos: *usize, version: i16) !OffsetCommitResponse {
        var result = OffsetCommitResponse{};
        if (version >= 3) {
            result.throttle_time_ms = ser.readI32(buf, pos);
        }
        const topics_len: usize = if (version >= 8)
            (try ser.readCompactArrayLen(buf, pos)) orelse 0
        else
            (try ser.readArrayLen(buf, pos)) orelse 0;
        if (topics_len > 0) {
            const topics_items = try alloc.alloc(OffsetCommitResponseTopic, topics_len);
            for (topics_items) |*item| {
                item.* = try OffsetCommitResponseTopic.deserialize(alloc, buf, pos, version);
            }
            result.topics = topics_items;
        }
        if (version >= 8) try ser.skipTaggedFields(buf, pos);
        return result;
    }

    pub fn calcSize(self: *const OffsetCommitResponse, version: i16) usize {
        var size: usize = 0;
        if (version >= 3) {
            size += 4;
        }
        if (version >= 8) {
            size += ser.unsignedVarintSize(self.topics.len + 1);
        } else {
            size += 4;
        }
        for (self.topics) |item| {
            size += item.calcSize(version);
        }
        if (version >= 8) size += 1;
        return size;
    }
};
