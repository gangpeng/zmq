// Auto-generated from AddPartitionsToTxnRequest.json
// API Key: 24, Type: request
// Valid Versions: 0-5, Flexible Versions: 3+
//
// DO NOT EDIT - generated by codegen_v2.py

const std = @import("std");
const ser = @import("../serialization.zig");
const Allocator = std.mem.Allocator;

pub const AddPartitionsToTxnTopic = struct {
    /// The name of the topic.
    /// Versions: 0+
    name: ?[]const u8 = null,
    /// The partition indexes to add to the transaction
    /// Versions: 0+
    partitions: []const i32 = &.{},

    pub fn serialize(self: *const AddPartitionsToTxnTopic, buf: []u8, pos: *usize, version: i16) void {
        if (version >= 3) {
            ser.writeCompactString(buf, pos, self.name);
        } else {
            ser.writeString(buf, pos, self.name);
        }
        if (version >= 3) {
            ser.writeCompactArrayLen(buf, pos, self.partitions.len);
        } else {
            ser.writeArrayLen(buf, pos, self.partitions.len);
        }
        for (self.partitions) |item| {
            ser.writeI32(buf, pos, item);
        }
        if (version >= 3) ser.writeEmptyTaggedFields(buf, pos);
    }

    pub fn deserialize(alloc: Allocator, buf: []const u8, pos: *usize, version: i16) !AddPartitionsToTxnTopic {
        var result = AddPartitionsToTxnTopic{};
        result.name = if (version >= 3)
            try ser.readCompactString(buf, pos)
        else
            try ser.readString(buf, pos);
        const partitions_len: usize = if (version >= 3)
            (try ser.readCompactArrayLen(buf, pos)) orelse 0
        else
            (try ser.readArrayLen(buf, pos)) orelse 0;
        if (partitions_len > 0) {
            const partitions_items = try alloc.alloc(i32, partitions_len);
            for (partitions_items) |*item| {
                item.* = ser.readI32(buf, pos);
            }
            result.partitions = partitions_items;
        }
        if (version >= 3) try ser.skipTaggedFields(buf, pos);
        return result;
    }

    pub fn calcSize(self: *const AddPartitionsToTxnTopic, version: i16) usize {
        var size: usize = 0;
        if (version >= 3) {
            size += ser.compactStringSize(self.name);
        } else {
            size += ser.stringSize(self.name);
        }
        if (version >= 3) {
            size += ser.unsignedVarintSize(self.partitions.len + 1);
        } else {
            size += 4;
        }
        size += self.partitions.len * 4;
        if (version >= 3) size += 1;
        return size;
    }
};

pub const AddPartitionsToTxnRequest = struct {
    pub const AddPartitionsToTxnTransaction = struct {
        /// The transactional id corresponding to the transaction.
        /// Versions: 4+
        transactional_id: ?[]const u8 = null,
        /// Current producer id in use by the transactional id.
        /// Versions: 4+
        producer_id: i64 = 0,
        /// Current epoch associated with the producer id.
        /// Versions: 4+
        producer_epoch: i16 = 0,
        /// Boolean to signify if we want to check if the partition is in the transaction rather than add it.
        /// Versions: 4+
        verify_only: bool = false,
        /// The partitions to add to the transaction.
        /// Versions: 4+
        topics: []const AddPartitionsToTxnTopic = &.{},

        pub fn serialize(self: *const AddPartitionsToTxnTransaction, buf: []u8, pos: *usize, version: i16) void {
            if (version >= 4) {
                if (version >= 3) {
                    ser.writeCompactString(buf, pos, self.transactional_id);
                } else {
                    ser.writeString(buf, pos, self.transactional_id);
                }
            }
            if (version >= 4) {
                ser.writeI64(buf, pos, self.producer_id);
            }
            if (version >= 4) {
                ser.writeI16(buf, pos, self.producer_epoch);
            }
            if (version >= 4) {
                ser.writeBool(buf, pos, self.verify_only);
            }
            if (version >= 4) {
                if (version >= 3) {
                    ser.writeCompactArrayLen(buf, pos, self.topics.len);
                } else {
                    ser.writeArrayLen(buf, pos, self.topics.len);
                }
                for (self.topics) |item| {
                    item.serialize(buf, pos, version);
                }
            }
            if (version >= 3) ser.writeEmptyTaggedFields(buf, pos);
        }

        pub fn deserialize(alloc: Allocator, buf: []const u8, pos: *usize, version: i16) !AddPartitionsToTxnTransaction {
            var result = AddPartitionsToTxnTransaction{};
            if (version >= 4) {
                result.transactional_id = if (version >= 3)
                    try ser.readCompactString(buf, pos)
                else
                    try ser.readString(buf, pos);
            }
            if (version >= 4) {
                result.producer_id = ser.readI64(buf, pos);
            }
            if (version >= 4) {
                result.producer_epoch = ser.readI16(buf, pos);
            }
            if (version >= 4) {
                result.verify_only = try ser.readBool(buf, pos);
            }
            if (version >= 4) {
                const topics_len: usize = if (version >= 3)
                    (try ser.readCompactArrayLen(buf, pos)) orelse 0
                else
                    (try ser.readArrayLen(buf, pos)) orelse 0;
                if (topics_len > 0) {
                    const topics_items = try alloc.alloc(AddPartitionsToTxnTopic, topics_len);
                    for (topics_items) |*item| {
                        item.* = try AddPartitionsToTxnTopic.deserialize(alloc, buf, pos, version);
                    }
                    result.topics = topics_items;
                }
            }
            if (version >= 3) try ser.skipTaggedFields(buf, pos);
            return result;
        }

        pub fn calcSize(self: *const AddPartitionsToTxnTransaction, version: i16) usize {
            var size: usize = 0;
            if (version >= 4) {
                if (version >= 3) {
                    size += ser.compactStringSize(self.transactional_id);
                } else {
                    size += ser.stringSize(self.transactional_id);
                }
            }
            if (version >= 4) {
                size += 8;
            }
            if (version >= 4) {
                size += 2;
            }
            if (version >= 4) {
                size += 1;
            }
            if (version >= 4) {
                if (version >= 3) {
                    size += ser.unsignedVarintSize(self.topics.len + 1);
                } else {
                    size += 4;
                }
                for (self.topics) |item| {
                    size += item.calcSize(version);
                }
            }
            if (version >= 3) size += 1;
            return size;
        }
    };

    /// List of transactions to add partitions to.
    /// Versions: 4+
    transactions: []const AddPartitionsToTxnTransaction = &.{},
    /// The transactional id corresponding to the transaction.
    /// Versions: 0-3
    v3_and_below_transactional_id: ?[]const u8 = null,
    /// Current producer id in use by the transactional id.
    /// Versions: 0-3
    v3_and_below_producer_id: i64 = 0,
    /// Current epoch associated with the producer id.
    /// Versions: 0-3
    v3_and_below_producer_epoch: i16 = 0,
    /// The partitions to add to the transaction.
    /// Versions: 0-3
    v3_and_below_topics: []const AddPartitionsToTxnTopic = &.{},

    pub fn serialize(self: *const AddPartitionsToTxnRequest, buf: []u8, pos: *usize, version: i16) void {
        if (version >= 4) {
            if (version >= 3) {
                ser.writeCompactArrayLen(buf, pos, self.transactions.len);
            } else {
                ser.writeArrayLen(buf, pos, self.transactions.len);
            }
            for (self.transactions) |item| {
                item.serialize(buf, pos, version);
            }
        }
        if (version <= 3) {
            if (version >= 3) {
                ser.writeCompactString(buf, pos, self.v3_and_below_transactional_id);
            } else {
                ser.writeString(buf, pos, self.v3_and_below_transactional_id);
            }
        }
        if (version <= 3) {
            ser.writeI64(buf, pos, self.v3_and_below_producer_id);
        }
        if (version <= 3) {
            ser.writeI16(buf, pos, self.v3_and_below_producer_epoch);
        }
        if (version <= 3) {
            if (version >= 3) {
                ser.writeCompactArrayLen(buf, pos, self.v3_and_below_topics.len);
            } else {
                ser.writeArrayLen(buf, pos, self.v3_and_below_topics.len);
            }
            for (self.v3_and_below_topics) |item| {
                item.serialize(buf, pos, version);
            }
        }
        if (version >= 3) ser.writeEmptyTaggedFields(buf, pos);
    }

    pub fn deserialize(alloc: Allocator, buf: []const u8, pos: *usize, version: i16) !AddPartitionsToTxnRequest {
        var result = AddPartitionsToTxnRequest{};
        if (version >= 4) {
            const transactions_len: usize = if (version >= 3)
                (try ser.readCompactArrayLen(buf, pos)) orelse 0
            else
                (try ser.readArrayLen(buf, pos)) orelse 0;
            if (transactions_len > 0) {
                const transactions_items = try alloc.alloc(AddPartitionsToTxnTransaction, transactions_len);
                for (transactions_items) |*item| {
                    item.* = try AddPartitionsToTxnTransaction.deserialize(alloc, buf, pos, version);
                }
                result.transactions = transactions_items;
            }
        }
        if (version <= 3) {
            result.v3_and_below_transactional_id = if (version >= 3)
                try ser.readCompactString(buf, pos)
            else
                try ser.readString(buf, pos);
        }
        if (version <= 3) {
            result.v3_and_below_producer_id = ser.readI64(buf, pos);
        }
        if (version <= 3) {
            result.v3_and_below_producer_epoch = ser.readI16(buf, pos);
        }
        if (version <= 3) {
            const v3_and_below_topics_len: usize = if (version >= 3)
                (try ser.readCompactArrayLen(buf, pos)) orelse 0
            else
                (try ser.readArrayLen(buf, pos)) orelse 0;
            if (v3_and_below_topics_len > 0) {
                const v3_and_below_topics_items = try alloc.alloc(AddPartitionsToTxnTopic, v3_and_below_topics_len);
                for (v3_and_below_topics_items) |*item| {
                    item.* = try AddPartitionsToTxnTopic.deserialize(alloc, buf, pos, version);
                }
                result.v3_and_below_topics = v3_and_below_topics_items;
            }
        }
        if (version >= 3) try ser.skipTaggedFields(buf, pos);
        return result;
    }

    pub fn calcSize(self: *const AddPartitionsToTxnRequest, version: i16) usize {
        var size: usize = 0;
        if (version >= 4) {
            if (version >= 3) {
                size += ser.unsignedVarintSize(self.transactions.len + 1);
            } else {
                size += 4;
            }
            for (self.transactions) |item| {
                size += item.calcSize(version);
            }
        }
        if (version <= 3) {
            if (version >= 3) {
                size += ser.compactStringSize(self.v3_and_below_transactional_id);
            } else {
                size += ser.stringSize(self.v3_and_below_transactional_id);
            }
        }
        if (version <= 3) {
            size += 8;
        }
        if (version <= 3) {
            size += 2;
        }
        if (version <= 3) {
            if (version >= 3) {
                size += ser.unsignedVarintSize(self.v3_and_below_topics.len + 1);
            } else {
                size += 4;
            }
            for (self.v3_and_below_topics) |item| {
                size += item.calcSize(version);
            }
        }
        if (version >= 3) size += 1;
        return size;
    }
};
