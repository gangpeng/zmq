// Auto-generated from OffsetDeleteRequest.json
// API Key: 47, Type: request
// Valid Versions: 0, Flexible Versions: none
//
// DO NOT EDIT - generated by codegen_v2.py

const std = @import("std");
const ser = @import("../serialization.zig");
const Allocator = std.mem.Allocator;

pub const OffsetDeleteRequest = struct {
    pub const OffsetDeleteRequestTopic = struct {
        pub const OffsetDeleteRequestPartition = struct {
            /// The partition index.
            /// Versions: 0+
            partition_index: i32 = 0,

            pub fn serialize(self: *const OffsetDeleteRequestPartition, buf: []u8, pos: *usize, _: i16) void {
                ser.writeI32(buf, pos, self.partition_index);
            }

            pub fn deserialize(_: Allocator, buf: []const u8, pos: *usize, _: i16) !OffsetDeleteRequestPartition {
                var result = OffsetDeleteRequestPartition{};
                result.partition_index = ser.readI32(buf, pos);
                return result;
            }

            pub fn calcSize(_: *const OffsetDeleteRequestPartition, _: i16) usize {
                var size: usize = 0;
                size += 4;
                return size;
            }
        };

        /// The topic name.
        /// Versions: 0+
        name: ?[]const u8 = null,
        /// Each partition to delete offsets for.
        /// Versions: 0+
        partitions: []const OffsetDeleteRequestPartition = &.{},

        pub fn serialize(self: *const OffsetDeleteRequestTopic, buf: []u8, pos: *usize, version: i16) void {
            ser.writeString(buf, pos, self.name);
            ser.writeArrayLen(buf, pos, self.partitions.len);
            for (self.partitions) |item| {
                item.serialize(buf, pos, version);
            }
        }

        pub fn deserialize(alloc: Allocator, buf: []const u8, pos: *usize, version: i16) !OffsetDeleteRequestTopic {
            var result = OffsetDeleteRequestTopic{};
            result.name = try ser.readString(buf, pos);
            const partitions_len: usize = (try ser.readArrayLen(buf, pos)) orelse 0;
            if (partitions_len > 0) {
                const partitions_items = try alloc.alloc(OffsetDeleteRequestPartition, partitions_len);
                for (partitions_items) |*item| {
                    item.* = try OffsetDeleteRequestPartition.deserialize(alloc, buf, pos, version);
                }
                result.partitions = partitions_items;
            }
            return result;
        }

        pub fn calcSize(self: *const OffsetDeleteRequestTopic, version: i16) usize {
            var size: usize = 0;
            size += ser.stringSize(self.name);
            size += 4;
            for (self.partitions) |item| {
                size += item.calcSize(version);
            }
            return size;
        }
    };

    /// The unique group identifier.
    /// Versions: 0+
    group_id: ?[]const u8 = null,
    /// The topics to delete offsets for
    /// Versions: 0+
    topics: []const OffsetDeleteRequestTopic = &.{},

    pub fn serialize(self: *const OffsetDeleteRequest, buf: []u8, pos: *usize, version: i16) void {
        ser.writeString(buf, pos, self.group_id);
        ser.writeArrayLen(buf, pos, self.topics.len);
        for (self.topics) |item| {
            item.serialize(buf, pos, version);
        }
    }

    pub fn deserialize(alloc: Allocator, buf: []const u8, pos: *usize, version: i16) !OffsetDeleteRequest {
        var result = OffsetDeleteRequest{};
        result.group_id = try ser.readString(buf, pos);
        const topics_len: usize = (try ser.readArrayLen(buf, pos)) orelse 0;
        if (topics_len > 0) {
            const topics_items = try alloc.alloc(OffsetDeleteRequestTopic, topics_len);
            for (topics_items) |*item| {
                item.* = try OffsetDeleteRequestTopic.deserialize(alloc, buf, pos, version);
            }
            result.topics = topics_items;
        }
        return result;
    }

    pub fn calcSize(self: *const OffsetDeleteRequest, version: i16) usize {
        var size: usize = 0;
        size += ser.stringSize(self.group_id);
        size += 4;
        for (self.topics) |item| {
            size += item.calcSize(version);
        }
        return size;
    }
};
