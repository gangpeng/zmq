// Auto-generated from AlterUserScramCredentialsRequest.json
// API Key: 51, Type: request
// Valid Versions: 0, Flexible Versions: 0+
//
// DO NOT EDIT - generated by codegen_v2.py

const std = @import("std");
const ser = @import("../serialization.zig");
const Allocator = std.mem.Allocator;

pub const AlterUserScramCredentialsRequest = struct {
    pub const ScramCredentialDeletion = struct {
        /// The user name.
        /// Versions: 0+
        name: ?[]const u8 = null,
        /// The SCRAM mechanism.
        /// Versions: 0+
        mechanism: i8 = 0,

        pub fn serialize(self: *const ScramCredentialDeletion, buf: []u8, pos: *usize, _: i16) void {
            ser.writeCompactString(buf, pos, self.name);
            ser.writeI8(buf, pos, self.mechanism);
            ser.writeEmptyTaggedFields(buf, pos);
        }

        pub fn deserialize(_: Allocator, buf: []const u8, pos: *usize, _: i16) !ScramCredentialDeletion {
            var result = ScramCredentialDeletion{};
            result.name = try ser.readCompactString(buf, pos);
            result.mechanism = ser.readI8(buf, pos);
            try ser.skipTaggedFields(buf, pos);
            return result;
        }

        pub fn calcSize(self: *const ScramCredentialDeletion, _: i16) usize {
            var size: usize = 0;
            size += ser.compactStringSize(self.name);
            size += 1;
            size += 1;
            return size;
        }
    };

    pub const ScramCredentialUpsertion = struct {
        /// The user name.
        /// Versions: 0+
        name: ?[]const u8 = null,
        /// The SCRAM mechanism.
        /// Versions: 0+
        mechanism: i8 = 0,
        /// The number of iterations.
        /// Versions: 0+
        iterations: i32 = 0,
        /// A random salt generated by the client.
        /// Versions: 0+
        salt: ?[]const u8 = null,
        /// The salted password.
        /// Versions: 0+
        salted_password: ?[]const u8 = null,

        pub fn serialize(self: *const ScramCredentialUpsertion, buf: []u8, pos: *usize, _: i16) void {
            ser.writeCompactString(buf, pos, self.name);
            ser.writeI8(buf, pos, self.mechanism);
            ser.writeI32(buf, pos, self.iterations);
            ser.writeCompactBytes(buf, pos, self.salt);
            ser.writeCompactBytes(buf, pos, self.salted_password);
            ser.writeEmptyTaggedFields(buf, pos);
        }

        pub fn deserialize(_: Allocator, buf: []const u8, pos: *usize, _: i16) !ScramCredentialUpsertion {
            var result = ScramCredentialUpsertion{};
            result.name = try ser.readCompactString(buf, pos);
            result.mechanism = ser.readI8(buf, pos);
            result.iterations = ser.readI32(buf, pos);
            result.salt = try ser.readCompactBytes(buf, pos);
            result.salted_password = try ser.readCompactBytes(buf, pos);
            try ser.skipTaggedFields(buf, pos);
            return result;
        }

        pub fn calcSize(self: *const ScramCredentialUpsertion, _: i16) usize {
            var size: usize = 0;
            size += ser.compactStringSize(self.name);
            size += 1;
            size += 4;
            size += ser.compactBytesSize(self.salt);
            size += ser.compactBytesSize(self.salted_password);
            size += 1;
            return size;
        }
    };

    /// The SCRAM credentials to remove.
    /// Versions: 0+
    deletions: []const ScramCredentialDeletion = &.{},
    /// The SCRAM credentials to update/insert.
    /// Versions: 0+
    upsertions: []const ScramCredentialUpsertion = &.{},

    pub fn serialize(self: *const AlterUserScramCredentialsRequest, buf: []u8, pos: *usize, version: i16) void {
        ser.writeCompactArrayLen(buf, pos, self.deletions.len);
        for (self.deletions) |item| {
            item.serialize(buf, pos, version);
        }
        ser.writeCompactArrayLen(buf, pos, self.upsertions.len);
        for (self.upsertions) |item| {
            item.serialize(buf, pos, version);
        }
        ser.writeEmptyTaggedFields(buf, pos);
    }

    pub fn deserialize(alloc: Allocator, buf: []const u8, pos: *usize, version: i16) !AlterUserScramCredentialsRequest {
        var result = AlterUserScramCredentialsRequest{};
        const deletions_len: usize = (try ser.readCompactArrayLen(buf, pos)) orelse 0;
        if (deletions_len > 0) {
            const deletions_items = try alloc.alloc(ScramCredentialDeletion, deletions_len);
            for (deletions_items) |*item| {
                item.* = try ScramCredentialDeletion.deserialize(alloc, buf, pos, version);
            }
            result.deletions = deletions_items;
        }
        const upsertions_len: usize = (try ser.readCompactArrayLen(buf, pos)) orelse 0;
        if (upsertions_len > 0) {
            const upsertions_items = try alloc.alloc(ScramCredentialUpsertion, upsertions_len);
            for (upsertions_items) |*item| {
                item.* = try ScramCredentialUpsertion.deserialize(alloc, buf, pos, version);
            }
            result.upsertions = upsertions_items;
        }
        try ser.skipTaggedFields(buf, pos);
        return result;
    }

    pub fn calcSize(self: *const AlterUserScramCredentialsRequest, version: i16) usize {
        var size: usize = 0;
        size += ser.unsignedVarintSize(self.deletions.len + 1);
        for (self.deletions) |item| {
            size += item.calcSize(version);
        }
        size += ser.unsignedVarintSize(self.upsertions.len + 1);
        for (self.upsertions) |item| {
            size += item.calcSize(version);
        }
        size += 1;
        return size;
    }
};
