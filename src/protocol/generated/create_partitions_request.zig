// Auto-generated from CreatePartitionsRequest.json
// API Key: 37, Type: request
// Valid Versions: 0-3, Flexible Versions: 2+
//
// DO NOT EDIT - generated by codegen_v2.py

const std = @import("std");
const ser = @import("../serialization.zig");
const Allocator = std.mem.Allocator;

pub const CreatePartitionsRequest = struct {
    pub const CreatePartitionsTopic = struct {
        pub const CreatePartitionsAssignment = struct {
            /// The assigned broker IDs.
            /// Versions: 0+
            broker_ids: []const i32 = &.{},

            pub fn serialize(self: *const CreatePartitionsAssignment, buf: []u8, pos: *usize, version: i16) void {
                if (version >= 2) {
                    ser.writeCompactArrayLen(buf, pos, self.broker_ids.len);
                } else {
                    ser.writeArrayLen(buf, pos, self.broker_ids.len);
                }
                for (self.broker_ids) |item| {
                    ser.writeI32(buf, pos, item);
                }
                if (version >= 2) ser.writeEmptyTaggedFields(buf, pos);
            }

            pub fn deserialize(_: Allocator, buf: []const u8, pos: *usize, version: i16) !CreatePartitionsAssignment {
                var result = CreatePartitionsAssignment{};
                const broker_ids_len: usize = if (version >= 2)
                    (try ser.readCompactArrayLen(buf, pos)) orelse 0
                else
                    (try ser.readArrayLen(buf, pos)) orelse 0;
                if (broker_ids_len > 0) {
                    pos.* += broker_ids_len * 4;
                }
                if (version >= 2) try ser.skipTaggedFields(buf, pos);
                return result;
            }

            pub fn calcSize(self: *const CreatePartitionsAssignment, version: i16) usize {
                var size: usize = 0;
                if (version >= 2) {
                    size += ser.unsignedVarintSize(self.broker_ids.len + 1);
                } else {
                    size += 4;
                }
                size += self.broker_ids.len * 4;
                if (version >= 2) size += 1;
                return size;
            }
        };

        /// The topic name.
        /// Versions: 0+
        name: ?[]const u8 = null,
        /// The new partition count.
        /// Versions: 0+
        count: i32 = 0,
        /// The new partition assignments.
        /// Versions: 0+
        assignments: []const CreatePartitionsAssignment = &.{},

        pub fn serialize(self: *const CreatePartitionsTopic, buf: []u8, pos: *usize, version: i16) void {
            if (version >= 2) {
                ser.writeCompactString(buf, pos, self.name);
            } else {
                ser.writeString(buf, pos, self.name);
            }
            ser.writeI32(buf, pos, self.count);
            if (version >= 2) {
                ser.writeCompactArrayLen(buf, pos, self.assignments.len);
            } else {
                ser.writeArrayLen(buf, pos, self.assignments.len);
            }
            for (self.assignments) |item| {
                item.serialize(buf, pos, version);
            }
            if (version >= 2) ser.writeEmptyTaggedFields(buf, pos);
        }

        pub fn deserialize(alloc: Allocator, buf: []const u8, pos: *usize, version: i16) !CreatePartitionsTopic {
            var result = CreatePartitionsTopic{};
            result.name = if (version >= 2)
                try ser.readCompactString(buf, pos)
            else
                try ser.readString(buf, pos);
            result.count = ser.readI32(buf, pos);
            const assignments_len: usize = if (version >= 2)
                (try ser.readCompactArrayLen(buf, pos)) orelse 0
            else
                (try ser.readArrayLen(buf, pos)) orelse 0;
            if (assignments_len > 0) {
                const assignments_items = try alloc.alloc(CreatePartitionsAssignment, assignments_len);
                for (assignments_items) |*item| {
                    item.* = try CreatePartitionsAssignment.deserialize(alloc, buf, pos, version);
                }
                result.assignments = assignments_items;
            }
            if (version >= 2) try ser.skipTaggedFields(buf, pos);
            return result;
        }

        pub fn calcSize(self: *const CreatePartitionsTopic, version: i16) usize {
            var size: usize = 0;
            if (version >= 2) {
                size += ser.compactStringSize(self.name);
            } else {
                size += ser.stringSize(self.name);
            }
            size += 4;
            if (version >= 2) {
                size += ser.unsignedVarintSize(self.assignments.len + 1);
            } else {
                size += 4;
            }
            for (self.assignments) |item| {
                size += item.calcSize(version);
            }
            if (version >= 2) size += 1;
            return size;
        }
    };

    /// Each topic that we want to create new partitions inside.
    /// Versions: 0+
    topics: []const CreatePartitionsTopic = &.{},
    /// The time in ms to wait for the partitions to be created.
    /// Versions: 0+
    timeout_ms: i32 = 0,
    /// If true, then validate the request, but don't actually increase the number of partitions.
    /// Versions: 0+
    validate_only: bool = false,

    pub fn serialize(self: *const CreatePartitionsRequest, buf: []u8, pos: *usize, version: i16) void {
        if (version >= 2) {
            ser.writeCompactArrayLen(buf, pos, self.topics.len);
        } else {
            ser.writeArrayLen(buf, pos, self.topics.len);
        }
        for (self.topics) |item| {
            item.serialize(buf, pos, version);
        }
        ser.writeI32(buf, pos, self.timeout_ms);
        ser.writeBool(buf, pos, self.validate_only);
        if (version >= 2) ser.writeEmptyTaggedFields(buf, pos);
    }

    pub fn deserialize(alloc: Allocator, buf: []const u8, pos: *usize, version: i16) !CreatePartitionsRequest {
        var result = CreatePartitionsRequest{};
        const topics_len: usize = if (version >= 2)
            (try ser.readCompactArrayLen(buf, pos)) orelse 0
        else
            (try ser.readArrayLen(buf, pos)) orelse 0;
        if (topics_len > 0) {
            const topics_items = try alloc.alloc(CreatePartitionsTopic, topics_len);
            for (topics_items) |*item| {
                item.* = try CreatePartitionsTopic.deserialize(alloc, buf, pos, version);
            }
            result.topics = topics_items;
        }
        result.timeout_ms = ser.readI32(buf, pos);
        result.validate_only = try ser.readBool(buf, pos);
        if (version >= 2) try ser.skipTaggedFields(buf, pos);
        return result;
    }

    pub fn calcSize(self: *const CreatePartitionsRequest, version: i16) usize {
        var size: usize = 0;
        if (version >= 2) {
            size += ser.unsignedVarintSize(self.topics.len + 1);
        } else {
            size += 4;
        }
        for (self.topics) |item| {
            size += item.calcSize(version);
        }
        size += 4;
        size += 1;
        if (version >= 2) size += 1;
        return size;
    }
};
