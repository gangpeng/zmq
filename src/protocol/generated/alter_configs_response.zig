// Auto-generated from AlterConfigsResponse.json
// API Key: 33, Type: response
// Valid Versions: 0-2, Flexible Versions: 2+
//
// DO NOT EDIT - generated by codegen_v2.py

const std = @import("std");
const ser = @import("../serialization.zig");
const Allocator = std.mem.Allocator;

pub const AlterConfigsResponse = struct {
    pub const AlterConfigsResourceResponse = struct {
        /// The resource error code.
        /// Versions: 0+
        error_code: i16 = 0,
        /// The resource error message, or null if there was no error.
        /// Versions: 0+
        error_message: ?[]const u8 = null,
        /// The resource type.
        /// Versions: 0+
        resource_type: i8 = 0,
        /// The resource name.
        /// Versions: 0+
        resource_name: ?[]const u8 = null,

        pub fn serialize(self: *const AlterConfigsResourceResponse, buf: []u8, pos: *usize, version: i16) void {
            ser.writeI16(buf, pos, self.error_code);
            if (version >= 2) {
                ser.writeCompactString(buf, pos, self.error_message);
            } else {
                ser.writeString(buf, pos, self.error_message);
            }
            ser.writeI8(buf, pos, self.resource_type);
            if (version >= 2) {
                ser.writeCompactString(buf, pos, self.resource_name);
            } else {
                ser.writeString(buf, pos, self.resource_name);
            }
            if (version >= 2) ser.writeEmptyTaggedFields(buf, pos);
        }

        pub fn deserialize(_: Allocator, buf: []const u8, pos: *usize, version: i16) !AlterConfigsResourceResponse {
            var result = AlterConfigsResourceResponse{};
            result.error_code = ser.readI16(buf, pos);
            result.error_message = if (version >= 2)
                try ser.readCompactString(buf, pos)
            else
                try ser.readString(buf, pos);
            result.resource_type = ser.readI8(buf, pos);
            result.resource_name = if (version >= 2)
                try ser.readCompactString(buf, pos)
            else
                try ser.readString(buf, pos);
            if (version >= 2) try ser.skipTaggedFields(buf, pos);
            return result;
        }

        pub fn calcSize(self: *const AlterConfigsResourceResponse, version: i16) usize {
            var size: usize = 0;
            size += 2;
            if (version >= 2) {
                size += ser.compactStringSize(self.error_message);
            } else {
                size += ser.stringSize(self.error_message);
            }
            size += 1;
            if (version >= 2) {
                size += ser.compactStringSize(self.resource_name);
            } else {
                size += ser.stringSize(self.resource_name);
            }
            if (version >= 2) size += 1;
            return size;
        }
    };

    /// Duration in milliseconds for which the request was throttled due to a quota violation, or zero if the request did not violate any quota.
    /// Versions: 0+
    throttle_time_ms: i32 = 0,
    /// The responses for each resource.
    /// Versions: 0+
    responses: []const AlterConfigsResourceResponse = &.{},

    pub fn serialize(self: *const AlterConfigsResponse, buf: []u8, pos: *usize, version: i16) void {
        ser.writeI32(buf, pos, self.throttle_time_ms);
        if (version >= 2) {
            ser.writeCompactArrayLen(buf, pos, self.responses.len);
        } else {
            ser.writeArrayLen(buf, pos, self.responses.len);
        }
        for (self.responses) |item| {
            item.serialize(buf, pos, version);
        }
        if (version >= 2) ser.writeEmptyTaggedFields(buf, pos);
    }

    pub fn deserialize(alloc: Allocator, buf: []const u8, pos: *usize, version: i16) !AlterConfigsResponse {
        var result = AlterConfigsResponse{};
        result.throttle_time_ms = ser.readI32(buf, pos);
        const responses_len: usize = if (version >= 2)
            (try ser.readCompactArrayLen(buf, pos)) orelse 0
        else
            (try ser.readArrayLen(buf, pos)) orelse 0;
        if (responses_len > 0) {
            const responses_items = try alloc.alloc(AlterConfigsResourceResponse, responses_len);
            for (responses_items) |*item| {
                item.* = try AlterConfigsResourceResponse.deserialize(alloc, buf, pos, version);
            }
            result.responses = responses_items;
        }
        if (version >= 2) try ser.skipTaggedFields(buf, pos);
        return result;
    }

    pub fn calcSize(self: *const AlterConfigsResponse, version: i16) usize {
        var size: usize = 0;
        size += 4;
        if (version >= 2) {
            size += ser.unsignedVarintSize(self.responses.len + 1);
        } else {
            size += 4;
        }
        for (self.responses) |item| {
            size += item.calcSize(version);
        }
        if (version >= 2) size += 1;
        return size;
    }
};
