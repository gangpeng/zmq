// Auto-generated from ProduceRequest.json
// API Key: 0, Type: request
// Valid Versions: 0-11, Flexible Versions: 9+
//
// DO NOT EDIT - generated by codegen_v2.py

const std = @import("std");
const ser = @import("../serialization.zig");
const Allocator = std.mem.Allocator;

pub const ProduceRequest = struct {
    pub const TopicProduceData = struct {
        pub const PartitionProduceData = struct {
            /// The partition index.
            /// Versions: 0+
            index: i32 = 0,
            /// The record data to be produced.
            /// Versions: 0+
            records: ?[]const u8 = null,

            pub fn serialize(self: *const PartitionProduceData, buf: []u8, pos: *usize, version: i16) void {
                ser.writeI32(buf, pos, self.index);
                if (version >= 9) {
                    ser.writeCompactBytes(buf, pos, self.records);
                } else {
                    ser.writeBytesBuf(buf, pos, self.records);
                }
                if (version >= 9) ser.writeEmptyTaggedFields(buf, pos);
            }

            pub fn deserialize(_: Allocator, buf: []const u8, pos: *usize, version: i16) !PartitionProduceData {
                var result = PartitionProduceData{};
                result.index = ser.readI32(buf, pos);
                result.records = if (version >= 9)
                    try ser.readCompactBytes(buf, pos)
                else
                    try ser.readBytes(buf, pos);
                if (version >= 9) try ser.skipTaggedFields(buf, pos);
                return result;
            }

            pub fn calcSize(self: *const PartitionProduceData, version: i16) usize {
                var size: usize = 0;
                size += 4;
                if (version >= 9) {
                    size += ser.compactBytesSize(self.records);
                } else {
                    size += ser.bytesSize(self.records);
                }
                if (version >= 9) size += 1;
                return size;
            }
        };

        /// The topic name.
        /// Versions: 0+
        name: ?[]const u8 = null,
        /// Each partition to produce to.
        /// Versions: 0+
        partition_data: []const PartitionProduceData = &.{},

        pub fn serialize(self: *const TopicProduceData, buf: []u8, pos: *usize, version: i16) void {
            if (version >= 9) {
                ser.writeCompactString(buf, pos, self.name);
            } else {
                ser.writeString(buf, pos, self.name);
            }
            if (version >= 9) {
                ser.writeCompactArrayLen(buf, pos, self.partition_data.len);
            } else {
                ser.writeArrayLen(buf, pos, self.partition_data.len);
            }
            for (self.partition_data) |item| {
                item.serialize(buf, pos, version);
            }
            if (version >= 9) ser.writeEmptyTaggedFields(buf, pos);
        }

        pub fn deserialize(alloc: Allocator, buf: []const u8, pos: *usize, version: i16) !TopicProduceData {
            var result = TopicProduceData{};
            result.name = if (version >= 9)
                try ser.readCompactString(buf, pos)
            else
                try ser.readString(buf, pos);
            const partition_data_len: usize = if (version >= 9)
                (try ser.readCompactArrayLen(buf, pos)) orelse 0
            else
                (try ser.readArrayLen(buf, pos)) orelse 0;
            if (partition_data_len > 0) {
                const partition_data_items = try alloc.alloc(PartitionProduceData, partition_data_len);
                for (partition_data_items) |*item| {
                    item.* = try PartitionProduceData.deserialize(alloc, buf, pos, version);
                }
                result.partition_data = partition_data_items;
            }
            if (version >= 9) try ser.skipTaggedFields(buf, pos);
            return result;
        }

        pub fn calcSize(self: *const TopicProduceData, version: i16) usize {
            var size: usize = 0;
            if (version >= 9) {
                size += ser.compactStringSize(self.name);
            } else {
                size += ser.stringSize(self.name);
            }
            if (version >= 9) {
                size += ser.unsignedVarintSize(self.partition_data.len + 1);
            } else {
                size += 4;
            }
            for (self.partition_data) |item| {
                size += item.calcSize(version);
            }
            if (version >= 9) size += 1;
            return size;
        }
    };

    /// The transactional ID, or null if the producer is not transactional.
    /// Versions: 3+
    transactional_id: ?[]const u8 = null,
    /// The number of acknowledgments the producer requires the leader to have received before considering a request complete. Allowed values: 0 for no acknowledgments, 1 for only the leader and -1 for the full ISR.
    /// Versions: 0+
    acks: i16 = 0,
    /// The timeout to await a response in milliseconds.
    /// Versions: 0+
    timeout_ms: i32 = 0,
    /// Each topic to produce to.
    /// Versions: 0+
    topic_data: []const TopicProduceData = &.{},

    pub fn serialize(self: *const ProduceRequest, buf: []u8, pos: *usize, version: i16) void {
        if (version >= 3) {
            if (version >= 9) {
                ser.writeCompactString(buf, pos, self.transactional_id);
            } else {
                ser.writeString(buf, pos, self.transactional_id);
            }
        }
        ser.writeI16(buf, pos, self.acks);
        ser.writeI32(buf, pos, self.timeout_ms);
        if (version >= 9) {
            ser.writeCompactArrayLen(buf, pos, self.topic_data.len);
        } else {
            ser.writeArrayLen(buf, pos, self.topic_data.len);
        }
        for (self.topic_data) |item| {
            item.serialize(buf, pos, version);
        }
        if (version >= 9) ser.writeEmptyTaggedFields(buf, pos);
    }

    pub fn deserialize(alloc: Allocator, buf: []const u8, pos: *usize, version: i16) !ProduceRequest {
        var result = ProduceRequest{};
        if (version >= 3) {
            result.transactional_id = if (version >= 9)
                try ser.readCompactString(buf, pos)
            else
                try ser.readString(buf, pos);
        }
        result.acks = ser.readI16(buf, pos);
        result.timeout_ms = ser.readI32(buf, pos);
        const topic_data_len: usize = if (version >= 9)
            (try ser.readCompactArrayLen(buf, pos)) orelse 0
        else
            (try ser.readArrayLen(buf, pos)) orelse 0;
        if (topic_data_len > 0) {
            const topic_data_items = try alloc.alloc(TopicProduceData, topic_data_len);
            for (topic_data_items) |*item| {
                item.* = try TopicProduceData.deserialize(alloc, buf, pos, version);
            }
            result.topic_data = topic_data_items;
        }
        if (version >= 9) try ser.skipTaggedFields(buf, pos);
        return result;
    }

    pub fn calcSize(self: *const ProduceRequest, version: i16) usize {
        var size: usize = 0;
        if (version >= 3) {
            if (version >= 9) {
                size += ser.compactStringSize(self.transactional_id);
            } else {
                size += ser.stringSize(self.transactional_id);
            }
        }
        size += 2;
        size += 4;
        if (version >= 9) {
            size += ser.unsignedVarintSize(self.topic_data.len + 1);
        } else {
            size += 4;
        }
        for (self.topic_data) |item| {
            size += item.calcSize(version);
        }
        if (version >= 9) size += 1;
        return size;
    }
};
