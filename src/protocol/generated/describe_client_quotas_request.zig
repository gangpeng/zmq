// Auto-generated from DescribeClientQuotasRequest.json
// API Key: 48, Type: request
// Valid Versions: 0-1, Flexible Versions: 1+
//
// DO NOT EDIT - generated by codegen_v2.py

const std = @import("std");
const ser = @import("../serialization.zig");
const Allocator = std.mem.Allocator;

pub const DescribeClientQuotasRequest = struct {
    pub const ComponentData = struct {
        /// The entity type that the filter component applies to.
        /// Versions: 0+
        entity_type: ?[]const u8 = null,
        /// How to match the entity {0 = exact name, 1 = default name, 2 = any specified name}.
        /// Versions: 0+
        match_type: i8 = 0,
        /// The string to match against, or null if unused for the match type.
        /// Versions: 0+
        match: ?[]const u8 = null,

        pub fn serialize(self: *const ComponentData, buf: []u8, pos: *usize, version: i16) void {
            if (version >= 1) {
                ser.writeCompactString(buf, pos, self.entity_type);
            } else {
                ser.writeString(buf, pos, self.entity_type);
            }
            ser.writeI8(buf, pos, self.match_type);
            if (version >= 1) {
                ser.writeCompactString(buf, pos, self.match);
            } else {
                ser.writeString(buf, pos, self.match);
            }
            if (version >= 1) ser.writeEmptyTaggedFields(buf, pos);
        }

        pub fn deserialize(_: Allocator, buf: []const u8, pos: *usize, version: i16) !ComponentData {
            var result = ComponentData{};
            result.entity_type = if (version >= 1)
                try ser.readCompactString(buf, pos)
            else
                try ser.readString(buf, pos);
            result.match_type = ser.readI8(buf, pos);
            result.match = if (version >= 1)
                try ser.readCompactString(buf, pos)
            else
                try ser.readString(buf, pos);
            if (version >= 1) try ser.skipTaggedFields(buf, pos);
            return result;
        }

        pub fn calcSize(self: *const ComponentData, version: i16) usize {
            var size: usize = 0;
            if (version >= 1) {
                size += ser.compactStringSize(self.entity_type);
            } else {
                size += ser.stringSize(self.entity_type);
            }
            size += 1;
            if (version >= 1) {
                size += ser.compactStringSize(self.match);
            } else {
                size += ser.stringSize(self.match);
            }
            if (version >= 1) size += 1;
            return size;
        }
    };

    /// Filter components to apply to quota entities.
    /// Versions: 0+
    components: []const ComponentData = &.{},
    /// Whether the match is strict, i.e. should exclude entities with unspecified entity types.
    /// Versions: 0+
    strict: bool = false,

    pub fn serialize(self: *const DescribeClientQuotasRequest, buf: []u8, pos: *usize, version: i16) void {
        if (version >= 1) {
            ser.writeCompactArrayLen(buf, pos, self.components.len);
        } else {
            ser.writeArrayLen(buf, pos, self.components.len);
        }
        for (self.components) |item| {
            item.serialize(buf, pos, version);
        }
        ser.writeBool(buf, pos, self.strict);
        if (version >= 1) ser.writeEmptyTaggedFields(buf, pos);
    }

    pub fn deserialize(alloc: Allocator, buf: []const u8, pos: *usize, version: i16) !DescribeClientQuotasRequest {
        var result = DescribeClientQuotasRequest{};
        const components_len: usize = if (version >= 1)
            (try ser.readCompactArrayLen(buf, pos)) orelse 0
        else
            (try ser.readArrayLen(buf, pos)) orelse 0;
        if (components_len > 0) {
            const components_items = try alloc.alloc(ComponentData, components_len);
            for (components_items) |*item| {
                item.* = try ComponentData.deserialize(alloc, buf, pos, version);
            }
            result.components = components_items;
        }
        result.strict = try ser.readBool(buf, pos);
        if (version >= 1) try ser.skipTaggedFields(buf, pos);
        return result;
    }

    pub fn calcSize(self: *const DescribeClientQuotasRequest, version: i16) usize {
        var size: usize = 0;
        if (version >= 1) {
            size += ser.unsignedVarintSize(self.components.len + 1);
        } else {
            size += 4;
        }
        for (self.components) |item| {
            size += item.calcSize(version);
        }
        size += 1;
        if (version >= 1) size += 1;
        return size;
    }
};
