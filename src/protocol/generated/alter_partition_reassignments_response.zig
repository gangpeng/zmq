// Auto-generated from AlterPartitionReassignmentsResponse.json
// API Key: 45, Type: response
// Valid Versions: 0, Flexible Versions: 0+
//
// DO NOT EDIT - generated by codegen_v2.py

const std = @import("std");
const ser = @import("../serialization.zig");
const Allocator = std.mem.Allocator;

pub const AlterPartitionReassignmentsResponse = struct {
    pub const ReassignableTopicResponse = struct {
        pub const ReassignablePartitionResponse = struct {
            /// The partition index.
            /// Versions: 0+
            partition_index: i32 = 0,
            /// The error code for this partition, or 0 if there was no error.
            /// Versions: 0+
            error_code: i16 = 0,
            /// The error message for this partition, or null if there was no error.
            /// Versions: 0+
            error_message: ?[]const u8 = null,

            pub fn serialize(self: *const ReassignablePartitionResponse, buf: []u8, pos: *usize, _: i16) void {
                ser.writeI32(buf, pos, self.partition_index);
                ser.writeI16(buf, pos, self.error_code);
                ser.writeCompactString(buf, pos, self.error_message);
                ser.writeEmptyTaggedFields(buf, pos);
            }

            pub fn deserialize(_: Allocator, buf: []const u8, pos: *usize, _: i16) !ReassignablePartitionResponse {
                var result = ReassignablePartitionResponse{};
                result.partition_index = ser.readI32(buf, pos);
                result.error_code = ser.readI16(buf, pos);
                result.error_message = try ser.readCompactString(buf, pos);
                try ser.skipTaggedFields(buf, pos);
                return result;
            }

            pub fn calcSize(self: *const ReassignablePartitionResponse, _: i16) usize {
                var size: usize = 0;
                size += 4;
                size += 2;
                size += ser.compactStringSize(self.error_message);
                size += 1;
                return size;
            }
        };

        /// The topic name
        /// Versions: 0+
        name: ?[]const u8 = null,
        /// The responses to partitions to reassign
        /// Versions: 0+
        partitions: []const ReassignablePartitionResponse = &.{},

        pub fn serialize(self: *const ReassignableTopicResponse, buf: []u8, pos: *usize, version: i16) void {
            ser.writeCompactString(buf, pos, self.name);
            ser.writeCompactArrayLen(buf, pos, self.partitions.len);
            for (self.partitions) |item| {
                item.serialize(buf, pos, version);
            }
            ser.writeEmptyTaggedFields(buf, pos);
        }

        pub fn deserialize(alloc: Allocator, buf: []const u8, pos: *usize, version: i16) !ReassignableTopicResponse {
            var result = ReassignableTopicResponse{};
            result.name = try ser.readCompactString(buf, pos);
            const partitions_len: usize = (try ser.readCompactArrayLen(buf, pos)) orelse 0;
            if (partitions_len > 0) {
                const partitions_items = try alloc.alloc(ReassignablePartitionResponse, partitions_len);
                for (partitions_items) |*item| {
                    item.* = try ReassignablePartitionResponse.deserialize(alloc, buf, pos, version);
                }
                result.partitions = partitions_items;
            }
            try ser.skipTaggedFields(buf, pos);
            return result;
        }

        pub fn calcSize(self: *const ReassignableTopicResponse, version: i16) usize {
            var size: usize = 0;
            size += ser.compactStringSize(self.name);
            size += ser.unsignedVarintSize(self.partitions.len + 1);
            for (self.partitions) |item| {
                size += item.calcSize(version);
            }
            size += 1;
            return size;
        }
    };

    /// The duration in milliseconds for which the request was throttled due to a quota violation, or zero if the request did not violate any quota.
    /// Versions: 0+
    throttle_time_ms: i32 = 0,
    /// The top-level error code, or 0 if there was no error.
    /// Versions: 0+
    error_code: i16 = 0,
    /// The top-level error message, or null if there was no error.
    /// Versions: 0+
    error_message: ?[]const u8 = null,
    /// The responses to topics to reassign.
    /// Versions: 0+
    responses: []const ReassignableTopicResponse = &.{},

    pub fn serialize(self: *const AlterPartitionReassignmentsResponse, buf: []u8, pos: *usize, version: i16) void {
        ser.writeI32(buf, pos, self.throttle_time_ms);
        ser.writeI16(buf, pos, self.error_code);
        ser.writeCompactString(buf, pos, self.error_message);
        ser.writeCompactArrayLen(buf, pos, self.responses.len);
        for (self.responses) |item| {
            item.serialize(buf, pos, version);
        }
        ser.writeEmptyTaggedFields(buf, pos);
    }

    pub fn deserialize(alloc: Allocator, buf: []const u8, pos: *usize, version: i16) !AlterPartitionReassignmentsResponse {
        var result = AlterPartitionReassignmentsResponse{};
        result.throttle_time_ms = ser.readI32(buf, pos);
        result.error_code = ser.readI16(buf, pos);
        result.error_message = try ser.readCompactString(buf, pos);
        const responses_len: usize = (try ser.readCompactArrayLen(buf, pos)) orelse 0;
        if (responses_len > 0) {
            const responses_items = try alloc.alloc(ReassignableTopicResponse, responses_len);
            for (responses_items) |*item| {
                item.* = try ReassignableTopicResponse.deserialize(alloc, buf, pos, version);
            }
            result.responses = responses_items;
        }
        try ser.skipTaggedFields(buf, pos);
        return result;
    }

    pub fn calcSize(self: *const AlterPartitionReassignmentsResponse, version: i16) usize {
        var size: usize = 0;
        size += 4;
        size += 2;
        size += ser.compactStringSize(self.error_message);
        size += ser.unsignedVarintSize(self.responses.len + 1);
        for (self.responses) |item| {
            size += item.calcSize(version);
        }
        size += 1;
        return size;
    }
};
