// Auto-generated from DescribeTopicPartitionsRequest.json
// API Key: 75, Type: request
// Valid Versions: 0, Flexible Versions: 0+
//
// DO NOT EDIT - generated by codegen_v2.py

const std = @import("std");
const ser = @import("../serialization.zig");
const Allocator = std.mem.Allocator;

pub const DescribeTopicPartitionsRequest = struct {
    pub const TopicRequest = struct {
        /// The topic name
        /// Versions: 0+
        name: ?[]const u8 = null,

        pub fn serialize(self: *const TopicRequest, buf: []u8, pos: *usize, _: i16) void {
            ser.writeCompactString(buf, pos, self.name);
            ser.writeEmptyTaggedFields(buf, pos);
        }

        pub fn deserialize(_: Allocator, buf: []const u8, pos: *usize, _: i16) !TopicRequest {
            var result = TopicRequest{};
            result.name = try ser.readCompactString(buf, pos);
            try ser.skipTaggedFields(buf, pos);
            return result;
        }

        pub fn calcSize(self: *const TopicRequest, _: i16) usize {
            var size: usize = 0;
            size += ser.compactStringSize(self.name);
            size += 1;
            return size;
        }
    };

    pub const Cursor = struct {
        /// The name for the first topic to process
        /// Versions: 0+
        topic_name: ?[]const u8 = null,
        /// The partition index to start with
        /// Versions: 0+
        partition_index: i32 = 0,

        pub fn serialize(self: *const Cursor, buf: []u8, pos: *usize, _: i16) void {
            ser.writeCompactString(buf, pos, self.topic_name);
            ser.writeI32(buf, pos, self.partition_index);
            ser.writeEmptyTaggedFields(buf, pos);
        }

        pub fn deserialize(_: Allocator, buf: []const u8, pos: *usize, _: i16) !Cursor {
            var result = Cursor{};
            result.topic_name = try ser.readCompactString(buf, pos);
            result.partition_index = ser.readI32(buf, pos);
            try ser.skipTaggedFields(buf, pos);
            return result;
        }

        pub fn calcSize(self: *const Cursor, _: i16) usize {
            var size: usize = 0;
            size += ser.compactStringSize(self.topic_name);
            size += 4;
            size += 1;
            return size;
        }
    };

    /// The topics to fetch details for.
    /// Versions: 0+
    topics: []const TopicRequest = &.{},
    /// The maximum number of partitions included in the response.
    /// Versions: 0+
    response_partition_limit: i32 = 2000,
    /// The first topic and partition index to fetch details for.
    /// Versions: 0+
    cursor: Cursor = null,

    pub fn serialize(self: *const DescribeTopicPartitionsRequest, buf: []u8, pos: *usize, version: i16) void {
        ser.writeCompactArrayLen(buf, pos, self.topics.len);
        for (self.topics) |item| {
            item.serialize(buf, pos, version);
        }
        ser.writeI32(buf, pos, self.response_partition_limit);
        self.cursor.serialize(buf, pos, version);
        ser.writeEmptyTaggedFields(buf, pos);
    }

    pub fn deserialize(alloc: Allocator, buf: []const u8, pos: *usize, version: i16) !DescribeTopicPartitionsRequest {
        var result = DescribeTopicPartitionsRequest{};
        const topics_len: usize = (try ser.readCompactArrayLen(buf, pos)) orelse 0;
        if (topics_len > 0) {
            const topics_items = try alloc.alloc(TopicRequest, topics_len);
            for (topics_items) |*item| {
                item.* = try TopicRequest.deserialize(alloc, buf, pos, version);
            }
            result.topics = topics_items;
        }
        result.response_partition_limit = ser.readI32(buf, pos);
        result.cursor = try Cursor.deserialize(alloc, buf, pos, version);
        try ser.skipTaggedFields(buf, pos);
        return result;
    }

    pub fn calcSize(self: *const DescribeTopicPartitionsRequest, version: i16) usize {
        var size: usize = 0;
        size += ser.unsignedVarintSize(self.topics.len + 1);
        for (self.topics) |item| {
            size += item.calcSize(version);
        }
        size += 4;
        size += self.cursor.calcSize(version);
        size += 1;
        return size;
    }
};
