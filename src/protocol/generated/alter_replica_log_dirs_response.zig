// Auto-generated from AlterReplicaLogDirsResponse.json
// API Key: 34, Type: response
// Valid Versions: 0-2, Flexible Versions: 2+
//
// DO NOT EDIT - generated by codegen_v2.py

const std = @import("std");
const ser = @import("../serialization.zig");
const Allocator = std.mem.Allocator;

pub const AlterReplicaLogDirsResponse = struct {
    pub const AlterReplicaLogDirTopicResult = struct {
        pub const AlterReplicaLogDirPartitionResult = struct {
            /// The partition index.
            /// Versions: 0+
            partition_index: i32 = 0,
            /// The error code, or 0 if there was no error.
            /// Versions: 0+
            error_code: i16 = 0,

            pub fn serialize(self: *const AlterReplicaLogDirPartitionResult, buf: []u8, pos: *usize, version: i16) void {
                ser.writeI32(buf, pos, self.partition_index);
                ser.writeI16(buf, pos, self.error_code);
                if (version >= 2) ser.writeEmptyTaggedFields(buf, pos);
            }

            pub fn deserialize(_: Allocator, buf: []const u8, pos: *usize, version: i16) !AlterReplicaLogDirPartitionResult {
                var result = AlterReplicaLogDirPartitionResult{};
                result.partition_index = ser.readI32(buf, pos);
                result.error_code = ser.readI16(buf, pos);
                if (version >= 2) try ser.skipTaggedFields(buf, pos);
                return result;
            }

            pub fn calcSize(_: *const AlterReplicaLogDirPartitionResult, version: i16) usize {
                var size: usize = 0;
                size += 4;
                size += 2;
                if (version >= 2) size += 1;
                return size;
            }
        };

        /// The name of the topic.
        /// Versions: 0+
        topic_name: ?[]const u8 = null,
        /// The results for each partition.
        /// Versions: 0+
        partitions: []const AlterReplicaLogDirPartitionResult = &.{},

        pub fn serialize(self: *const AlterReplicaLogDirTopicResult, buf: []u8, pos: *usize, version: i16) void {
            if (version >= 2) {
                ser.writeCompactString(buf, pos, self.topic_name);
            } else {
                ser.writeString(buf, pos, self.topic_name);
            }
            if (version >= 2) {
                ser.writeCompactArrayLen(buf, pos, self.partitions.len);
            } else {
                ser.writeArrayLen(buf, pos, self.partitions.len);
            }
            for (self.partitions) |item| {
                item.serialize(buf, pos, version);
            }
            if (version >= 2) ser.writeEmptyTaggedFields(buf, pos);
        }

        pub fn deserialize(alloc: Allocator, buf: []const u8, pos: *usize, version: i16) !AlterReplicaLogDirTopicResult {
            var result = AlterReplicaLogDirTopicResult{};
            result.topic_name = if (version >= 2)
                try ser.readCompactString(buf, pos)
            else
                try ser.readString(buf, pos);
            const partitions_len: usize = if (version >= 2)
                (try ser.readCompactArrayLen(buf, pos)) orelse 0
            else
                (try ser.readArrayLen(buf, pos)) orelse 0;
            if (partitions_len > 0) {
                const partitions_items = try alloc.alloc(AlterReplicaLogDirPartitionResult, partitions_len);
                for (partitions_items) |*item| {
                    item.* = try AlterReplicaLogDirPartitionResult.deserialize(alloc, buf, pos, version);
                }
                result.partitions = partitions_items;
            }
            if (version >= 2) try ser.skipTaggedFields(buf, pos);
            return result;
        }

        pub fn calcSize(self: *const AlterReplicaLogDirTopicResult, version: i16) usize {
            var size: usize = 0;
            if (version >= 2) {
                size += ser.compactStringSize(self.topic_name);
            } else {
                size += ser.stringSize(self.topic_name);
            }
            if (version >= 2) {
                size += ser.unsignedVarintSize(self.partitions.len + 1);
            } else {
                size += 4;
            }
            for (self.partitions) |item| {
                size += item.calcSize(version);
            }
            if (version >= 2) size += 1;
            return size;
        }
    };

    /// Duration in milliseconds for which the request was throttled due to a quota violation, or zero if the request did not violate any quota.
    /// Versions: 0+
    throttle_time_ms: i32 = 0,
    /// The results for each topic.
    /// Versions: 0+
    results: []const AlterReplicaLogDirTopicResult = &.{},

    pub fn serialize(self: *const AlterReplicaLogDirsResponse, buf: []u8, pos: *usize, version: i16) void {
        ser.writeI32(buf, pos, self.throttle_time_ms);
        if (version >= 2) {
            ser.writeCompactArrayLen(buf, pos, self.results.len);
        } else {
            ser.writeArrayLen(buf, pos, self.results.len);
        }
        for (self.results) |item| {
            item.serialize(buf, pos, version);
        }
        if (version >= 2) ser.writeEmptyTaggedFields(buf, pos);
    }

    pub fn deserialize(alloc: Allocator, buf: []const u8, pos: *usize, version: i16) !AlterReplicaLogDirsResponse {
        var result = AlterReplicaLogDirsResponse{};
        result.throttle_time_ms = ser.readI32(buf, pos);
        const results_len: usize = if (version >= 2)
            (try ser.readCompactArrayLen(buf, pos)) orelse 0
        else
            (try ser.readArrayLen(buf, pos)) orelse 0;
        if (results_len > 0) {
            const results_items = try alloc.alloc(AlterReplicaLogDirTopicResult, results_len);
            for (results_items) |*item| {
                item.* = try AlterReplicaLogDirTopicResult.deserialize(alloc, buf, pos, version);
            }
            result.results = results_items;
        }
        if (version >= 2) try ser.skipTaggedFields(buf, pos);
        return result;
    }

    pub fn calcSize(self: *const AlterReplicaLogDirsResponse, version: i16) usize {
        var size: usize = 0;
        size += 4;
        if (version >= 2) {
            size += ser.unsignedVarintSize(self.results.len + 1);
        } else {
            size += 4;
        }
        for (self.results) |item| {
            size += item.calcSize(version);
        }
        if (version >= 2) size += 1;
        return size;
    }
};
