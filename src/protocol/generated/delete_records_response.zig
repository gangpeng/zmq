// Auto-generated from DeleteRecordsResponse.json
// API Key: 21, Type: response
// Valid Versions: 0-2, Flexible Versions: 2+
//
// DO NOT EDIT - generated by codegen_v2.py

const std = @import("std");
const ser = @import("../serialization.zig");
const Allocator = std.mem.Allocator;

pub const DeleteRecordsResponse = struct {
    pub const DeleteRecordsTopicResult = struct {
        pub const DeleteRecordsPartitionResult = struct {
            /// The partition index.
            /// Versions: 0+
            partition_index: i32 = 0,
            /// The partition low water mark.
            /// Versions: 0+
            low_watermark: i64 = 0,
            /// The deletion error code, or 0 if the deletion succeeded.
            /// Versions: 0+
            error_code: i16 = 0,

            pub fn serialize(self: *const DeleteRecordsPartitionResult, buf: []u8, pos: *usize, version: i16) void {
                ser.writeI32(buf, pos, self.partition_index);
                ser.writeI64(buf, pos, self.low_watermark);
                ser.writeI16(buf, pos, self.error_code);
                if (version >= 2) ser.writeEmptyTaggedFields(buf, pos);
            }

            pub fn deserialize(_: Allocator, buf: []const u8, pos: *usize, version: i16) !DeleteRecordsPartitionResult {
                var result = DeleteRecordsPartitionResult{};
                result.partition_index = ser.readI32(buf, pos);
                result.low_watermark = ser.readI64(buf, pos);
                result.error_code = ser.readI16(buf, pos);
                if (version >= 2) try ser.skipTaggedFields(buf, pos);
                return result;
            }

            pub fn calcSize(_: *const DeleteRecordsPartitionResult, version: i16) usize {
                var size: usize = 0;
                size += 4;
                size += 8;
                size += 2;
                if (version >= 2) size += 1;
                return size;
            }
        };

        /// The topic name.
        /// Versions: 0+
        name: ?[]const u8 = null,
        /// Each partition that we wanted to delete records from.
        /// Versions: 0+
        partitions: []const DeleteRecordsPartitionResult = &.{},

        pub fn serialize(self: *const DeleteRecordsTopicResult, buf: []u8, pos: *usize, version: i16) void {
            if (version >= 2) {
                ser.writeCompactString(buf, pos, self.name);
            } else {
                ser.writeString(buf, pos, self.name);
            }
            if (version >= 2) {
                ser.writeCompactArrayLen(buf, pos, self.partitions.len);
            } else {
                ser.writeArrayLen(buf, pos, self.partitions.len);
            }
            for (self.partitions) |item| {
                item.serialize(buf, pos, version);
            }
            if (version >= 2) ser.writeEmptyTaggedFields(buf, pos);
        }

        pub fn deserialize(alloc: Allocator, buf: []const u8, pos: *usize, version: i16) !DeleteRecordsTopicResult {
            var result = DeleteRecordsTopicResult{};
            result.name = if (version >= 2)
                try ser.readCompactString(buf, pos)
            else
                try ser.readString(buf, pos);
            const partitions_len: usize = if (version >= 2)
                (try ser.readCompactArrayLen(buf, pos)) orelse 0
            else
                (try ser.readArrayLen(buf, pos)) orelse 0;
            if (partitions_len > 0) {
                const partitions_items = try alloc.alloc(DeleteRecordsPartitionResult, partitions_len);
                for (partitions_items) |*item| {
                    item.* = try DeleteRecordsPartitionResult.deserialize(alloc, buf, pos, version);
                }
                result.partitions = partitions_items;
            }
            if (version >= 2) try ser.skipTaggedFields(buf, pos);
            return result;
        }

        pub fn calcSize(self: *const DeleteRecordsTopicResult, version: i16) usize {
            var size: usize = 0;
            if (version >= 2) {
                size += ser.compactStringSize(self.name);
            } else {
                size += ser.stringSize(self.name);
            }
            if (version >= 2) {
                size += ser.unsignedVarintSize(self.partitions.len + 1);
            } else {
                size += 4;
            }
            for (self.partitions) |item| {
                size += item.calcSize(version);
            }
            if (version >= 2) size += 1;
            return size;
        }
    };

    /// The duration in milliseconds for which the request was throttled due to a quota violation, or zero if the request did not violate any quota.
    /// Versions: 0+
    throttle_time_ms: i32 = 0,
    /// Each topic that we wanted to delete records from.
    /// Versions: 0+
    topics: []const DeleteRecordsTopicResult = &.{},

    pub fn serialize(self: *const DeleteRecordsResponse, buf: []u8, pos: *usize, version: i16) void {
        ser.writeI32(buf, pos, self.throttle_time_ms);
        if (version >= 2) {
            ser.writeCompactArrayLen(buf, pos, self.topics.len);
        } else {
            ser.writeArrayLen(buf, pos, self.topics.len);
        }
        for (self.topics) |item| {
            item.serialize(buf, pos, version);
        }
        if (version >= 2) ser.writeEmptyTaggedFields(buf, pos);
    }

    pub fn deserialize(alloc: Allocator, buf: []const u8, pos: *usize, version: i16) !DeleteRecordsResponse {
        var result = DeleteRecordsResponse{};
        result.throttle_time_ms = ser.readI32(buf, pos);
        const topics_len: usize = if (version >= 2)
            (try ser.readCompactArrayLen(buf, pos)) orelse 0
        else
            (try ser.readArrayLen(buf, pos)) orelse 0;
        if (topics_len > 0) {
            const topics_items = try alloc.alloc(DeleteRecordsTopicResult, topics_len);
            for (topics_items) |*item| {
                item.* = try DeleteRecordsTopicResult.deserialize(alloc, buf, pos, version);
            }
            result.topics = topics_items;
        }
        if (version >= 2) try ser.skipTaggedFields(buf, pos);
        return result;
    }

    pub fn calcSize(self: *const DeleteRecordsResponse, version: i16) usize {
        var size: usize = 0;
        size += 4;
        if (version >= 2) {
            size += ser.unsignedVarintSize(self.topics.len + 1);
        } else {
            size += 4;
        }
        for (self.topics) |item| {
            size += item.calcSize(version);
        }
        if (version >= 2) size += 1;
        return size;
    }
};
