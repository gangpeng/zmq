// Auto-generated from ProduceResponse.json
// API Key: 0, Type: response
// Valid Versions: 0-11, Flexible Versions: 9+
//
// DO NOT EDIT - generated by codegen_v2.py

const std = @import("std");
const ser = @import("../serialization.zig");
const Allocator = std.mem.Allocator;

pub const ProduceResponse = struct {
    pub const TopicProduceResponse = struct {
        pub const PartitionProduceResponse = struct {
            pub const BatchIndexAndErrorMessage = struct {
                /// The batch index of the record that cause the batch to be dropped
                /// Versions: 8+
                batch_index: i32 = 0,
                /// The error message of the record that caused the batch to be dropped
                /// Versions: 8+
                batch_index_error_message: ?[]const u8 = null,

                pub fn serialize(self: *const BatchIndexAndErrorMessage, buf: []u8, pos: *usize, version: i16) void {
                    if (version >= 8) {
                        ser.writeI32(buf, pos, self.batch_index);
                    }
                    if (version >= 8) {
                        if (version >= 9) {
                            ser.writeCompactString(buf, pos, self.batch_index_error_message);
                        } else {
                            ser.writeString(buf, pos, self.batch_index_error_message);
                        }
                    }
                    if (version >= 9) ser.writeEmptyTaggedFields(buf, pos);
                }

                pub fn deserialize(_: Allocator, buf: []const u8, pos: *usize, version: i16) !BatchIndexAndErrorMessage {
                    var result = BatchIndexAndErrorMessage{};
                    if (version >= 8) {
                        result.batch_index = ser.readI32(buf, pos);
                    }
                    if (version >= 8) {
                        result.batch_index_error_message = if (version >= 9)
                            try ser.readCompactString(buf, pos)
                        else
                            try ser.readString(buf, pos);
                    }
                    if (version >= 9) try ser.skipTaggedFields(buf, pos);
                    return result;
                }

                pub fn calcSize(self: *const BatchIndexAndErrorMessage, version: i16) usize {
                    var size: usize = 0;
                    if (version >= 8) {
                        size += 4;
                    }
                    if (version >= 8) {
                        if (version >= 9) {
                            size += ser.compactStringSize(self.batch_index_error_message);
                        } else {
                            size += ser.stringSize(self.batch_index_error_message);
                        }
                    }
                    if (version >= 9) size += 1;
                    return size;
                }
            };

            pub const LeaderIdAndEpoch = struct {
                /// The ID of the current leader or -1 if the leader is unknown.
                /// Versions: 10+
                leader_id: i32 = -1,
                /// The latest known leader epoch
                /// Versions: 10+
                leader_epoch: i32 = -1,

                pub fn serialize(self: *const LeaderIdAndEpoch, buf: []u8, pos: *usize, version: i16) void {
                    if (version >= 10) {
                        ser.writeI32(buf, pos, self.leader_id);
                    }
                    if (version >= 10) {
                        ser.writeI32(buf, pos, self.leader_epoch);
                    }
                    if (version >= 9) ser.writeEmptyTaggedFields(buf, pos);
                }

                pub fn deserialize(_: Allocator, buf: []const u8, pos: *usize, version: i16) !LeaderIdAndEpoch {
                    var result = LeaderIdAndEpoch{};
                    if (version >= 10) {
                        result.leader_id = ser.readI32(buf, pos);
                    }
                    if (version >= 10) {
                        result.leader_epoch = ser.readI32(buf, pos);
                    }
                    if (version >= 9) try ser.skipTaggedFields(buf, pos);
                    return result;
                }

                pub fn calcSize(_: *const LeaderIdAndEpoch, version: i16) usize {
                    var size: usize = 0;
                    if (version >= 10) {
                        size += 4;
                    }
                    if (version >= 10) {
                        size += 4;
                    }
                    if (version >= 9) size += 1;
                    return size;
                }

                pub fn isDefault(self: *const LeaderIdAndEpoch) bool {
                    return self.leader_id == -1 and self.leader_epoch == -1;
                }
            };

            /// The partition index.
            /// Versions: 0+
            index: i32 = 0,
            /// The error code, or 0 if there was no error.
            /// Versions: 0+
            error_code: i16 = 0,
            /// The base offset.
            /// Versions: 0+
            base_offset: i64 = 0,
            /// The timestamp returned by broker after appending the messages. If CreateTime is used for the topic, the timestamp will be -1.  If LogAppendTime is used for the topic, the timestamp will be the broker local time when the messages are appended.
            /// Versions: 2+
            log_append_time_ms: i64 = -1,
            /// The log start offset.
            /// Versions: 5+
            log_start_offset: i64 = -1,
            /// The batch indices of records that caused the batch to be dropped
            /// Versions: 8+
            record_errors: []const BatchIndexAndErrorMessage = &.{},
            /// The global error message summarizing the common root cause of the records that caused the batch to be dropped
            /// Versions: 8+
            error_message: ?[]const u8 = null,
            /// The leader ID and epoch to use after NOT_LEADER_OR_FOLLOWER errors.
            /// Versions: 10+
            current_leader: LeaderIdAndEpoch = .{},

            pub fn serialize(self: *const PartitionProduceResponse, buf: []u8, pos: *usize, version: i16) void {
                ser.writeI32(buf, pos, self.index);
                ser.writeI16(buf, pos, self.error_code);
                ser.writeI64(buf, pos, self.base_offset);
                if (version >= 2) {
                    ser.writeI64(buf, pos, self.log_append_time_ms);
                }
                if (version >= 5) {
                    ser.writeI64(buf, pos, self.log_start_offset);
                }
                if (version >= 8) {
                    if (version >= 9) {
                        ser.writeCompactArrayLen(buf, pos, self.record_errors.len);
                    } else {
                        ser.writeArrayLen(buf, pos, self.record_errors.len);
                    }
                    for (self.record_errors) |item| {
                        item.serialize(buf, pos, version);
                    }
                }
                if (version >= 8) {
                    if (version >= 9) {
                        ser.writeCompactString(buf, pos, self.error_message);
                    } else {
                        ser.writeString(buf, pos, self.error_message);
                    }
                }
                if (version >= 9) {
                    const has_current_leader = version >= 10 and !self.current_leader.isDefault();
                    ser.writeUnsignedVarint(buf, pos, if (has_current_leader) 1 else 0);
                    if (has_current_leader) {
                        const tag_size = self.current_leader.calcSize(version);
                        ser.writeUnsignedVarint(buf, pos, 0);
                        ser.writeUnsignedVarint(buf, pos, tag_size);
                        self.current_leader.serialize(buf, pos, version);
                    }
                }
            }

            pub fn deserialize(alloc: Allocator, buf: []const u8, pos: *usize, version: i16) !PartitionProduceResponse {
                var result = PartitionProduceResponse{};
                errdefer if (result.record_errors.len > 0) alloc.free(result.record_errors);

                result.index = ser.readI32(buf, pos);
                result.error_code = ser.readI16(buf, pos);
                result.base_offset = ser.readI64(buf, pos);
                if (version >= 2) {
                    result.log_append_time_ms = ser.readI64(buf, pos);
                }
                if (version >= 5) {
                    result.log_start_offset = ser.readI64(buf, pos);
                }
                if (version >= 8) {
                    const record_errors_len: usize = if (version >= 9)
                        (try ser.readCompactArrayLen(buf, pos)) orelse 0
                    else
                        (try ser.readArrayLen(buf, pos)) orelse 0;
                    if (record_errors_len > 0) {
                        const record_errors_items = try alloc.alloc(BatchIndexAndErrorMessage, record_errors_len);
                        for (record_errors_items) |*item| {
                            item.* = try BatchIndexAndErrorMessage.deserialize(alloc, buf, pos, version);
                        }
                        result.record_errors = record_errors_items;
                    }
                }
                if (version >= 8) {
                    result.error_message = if (version >= 9)
                        try ser.readCompactString(buf, pos)
                    else
                        try ser.readString(buf, pos);
                }
                if (version >= 9) {
                    const tagged_fields = try ser.readTaggedFields(alloc, buf, pos);
                    defer if (tagged_fields.len > 0) alloc.free(tagged_fields);

                    var seen_current_leader = false;
                    for (tagged_fields) |field| {
                        if (field.tag == 0 and version >= 10) {
                            if (seen_current_leader) return error.DuplicateTaggedField;
                            seen_current_leader = true;

                            var tag_pos: usize = 0;
                            result.current_leader = try LeaderIdAndEpoch.deserialize(alloc, field.data, &tag_pos, version);
                            if (tag_pos != field.data.len) return error.InvalidTaggedField;
                        }
                    }
                }
                return result;
            }

            pub fn calcSize(self: *const PartitionProduceResponse, version: i16) usize {
                var size: usize = 0;
                size += 4;
                size += 2;
                size += 8;
                if (version >= 2) {
                    size += 8;
                }
                if (version >= 5) {
                    size += 8;
                }
                if (version >= 8) {
                    if (version >= 9) {
                        size += ser.unsignedVarintSize(self.record_errors.len + 1);
                    } else {
                        size += 4;
                    }
                    for (self.record_errors) |item| {
                        size += item.calcSize(version);
                    }
                }
                if (version >= 8) {
                    if (version >= 9) {
                        size += ser.compactStringSize(self.error_message);
                    } else {
                        size += ser.stringSize(self.error_message);
                    }
                }
                if (version >= 9) {
                    const has_current_leader = version >= 10 and !self.current_leader.isDefault();
                    size += ser.unsignedVarintSize(if (has_current_leader) 1 else 0);
                    if (has_current_leader) {
                        const tag_size = self.current_leader.calcSize(version);
                        size += ser.unsignedVarintSize(0);
                        size += ser.unsignedVarintSize(tag_size);
                        size += tag_size;
                    }
                }
                return size;
            }
        };

        /// The topic name
        /// Versions: 0+
        name: ?[]const u8 = null,
        /// Each partition that we produced to within the topic.
        /// Versions: 0+
        partition_responses: []const PartitionProduceResponse = &.{},

        pub fn serialize(self: *const TopicProduceResponse, buf: []u8, pos: *usize, version: i16) void {
            if (version >= 9) {
                ser.writeCompactString(buf, pos, self.name);
            } else {
                ser.writeString(buf, pos, self.name);
            }
            if (version >= 9) {
                ser.writeCompactArrayLen(buf, pos, self.partition_responses.len);
            } else {
                ser.writeArrayLen(buf, pos, self.partition_responses.len);
            }
            for (self.partition_responses) |item| {
                item.serialize(buf, pos, version);
            }
            if (version >= 9) ser.writeEmptyTaggedFields(buf, pos);
        }

        pub fn deserialize(alloc: Allocator, buf: []const u8, pos: *usize, version: i16) !TopicProduceResponse {
            var result = TopicProduceResponse{};
            result.name = if (version >= 9)
                try ser.readCompactString(buf, pos)
            else
                try ser.readString(buf, pos);
            const partition_responses_len: usize = if (version >= 9)
                (try ser.readCompactArrayLen(buf, pos)) orelse 0
            else
                (try ser.readArrayLen(buf, pos)) orelse 0;
            if (partition_responses_len > 0) {
                const partition_responses_items = try alloc.alloc(PartitionProduceResponse, partition_responses_len);
                for (partition_responses_items) |*item| {
                    item.* = try PartitionProduceResponse.deserialize(alloc, buf, pos, version);
                }
                result.partition_responses = partition_responses_items;
            }
            if (version >= 9) try ser.skipTaggedFields(buf, pos);
            return result;
        }

        pub fn calcSize(self: *const TopicProduceResponse, version: i16) usize {
            var size: usize = 0;
            if (version >= 9) {
                size += ser.compactStringSize(self.name);
            } else {
                size += ser.stringSize(self.name);
            }
            if (version >= 9) {
                size += ser.unsignedVarintSize(self.partition_responses.len + 1);
            } else {
                size += 4;
            }
            for (self.partition_responses) |item| {
                size += item.calcSize(version);
            }
            if (version >= 9) size += 1;
            return size;
        }
    };

    pub const NodeEndpoint = struct {
        /// The ID of the associated node.
        /// Versions: 10+
        node_id: i32 = 0,
        /// The node's hostname.
        /// Versions: 10+
        host: ?[]const u8 = null,
        /// The node's port.
        /// Versions: 10+
        port: i32 = 0,
        /// The rack of the node, or null if it has not been assigned to a rack.
        /// Versions: 10+
        rack: ?[]const u8 = null,

        pub fn serialize(self: *const NodeEndpoint, buf: []u8, pos: *usize, version: i16) void {
            if (version >= 10) {
                ser.writeI32(buf, pos, self.node_id);
            }
            if (version >= 10) {
                if (version >= 9) {
                    ser.writeCompactString(buf, pos, self.host);
                } else {
                    ser.writeString(buf, pos, self.host);
                }
            }
            if (version >= 10) {
                ser.writeI32(buf, pos, self.port);
            }
            if (version >= 10) {
                if (version >= 9) {
                    ser.writeCompactString(buf, pos, self.rack);
                } else {
                    ser.writeString(buf, pos, self.rack);
                }
            }
            if (version >= 9) ser.writeEmptyTaggedFields(buf, pos);
        }

        pub fn deserialize(_: Allocator, buf: []const u8, pos: *usize, version: i16) !NodeEndpoint {
            var result = NodeEndpoint{};
            if (version >= 10) {
                result.node_id = ser.readI32(buf, pos);
            }
            if (version >= 10) {
                result.host = if (version >= 9)
                    try ser.readCompactString(buf, pos)
                else
                    try ser.readString(buf, pos);
            }
            if (version >= 10) {
                result.port = ser.readI32(buf, pos);
            }
            if (version >= 10) {
                result.rack = if (version >= 9)
                    try ser.readCompactString(buf, pos)
                else
                    try ser.readString(buf, pos);
            }
            if (version >= 9) try ser.skipTaggedFields(buf, pos);
            return result;
        }

        pub fn calcSize(self: *const NodeEndpoint, version: i16) usize {
            var size: usize = 0;
            if (version >= 10) {
                size += 4;
            }
            if (version >= 10) {
                if (version >= 9) {
                    size += ser.compactStringSize(self.host);
                } else {
                    size += ser.stringSize(self.host);
                }
            }
            if (version >= 10) {
                size += 4;
            }
            if (version >= 10) {
                if (version >= 9) {
                    size += ser.compactStringSize(self.rack);
                } else {
                    size += ser.stringSize(self.rack);
                }
            }
            if (version >= 9) size += 1;
            return size;
        }
    };

    /// Each produce response
    /// Versions: 0+
    responses: []const TopicProduceResponse = &.{},
    /// The duration in milliseconds for which the request was throttled due to a quota violation, or zero if the request did not violate any quota.
    /// Versions: 1+
    throttle_time_ms: i32 = 0,
    /// Endpoints for current leaders enumerated in partition responses.
    /// Versions: 10+
    node_endpoints: []const NodeEndpoint = &.{},

    pub fn serialize(self: *const ProduceResponse, buf: []u8, pos: *usize, version: i16) void {
        if (version >= 9) {
            ser.writeCompactArrayLen(buf, pos, self.responses.len);
        } else {
            ser.writeArrayLen(buf, pos, self.responses.len);
        }
        for (self.responses) |item| {
            item.serialize(buf, pos, version);
        }
        if (version >= 1) {
            ser.writeI32(buf, pos, self.throttle_time_ms);
        }
        if (version >= 9) {
            const has_node_endpoints = version >= 10 and self.node_endpoints.len > 0;
            ser.writeUnsignedVarint(buf, pos, if (has_node_endpoints) 1 else 0);
            if (has_node_endpoints) {
                const tag_size = nodeEndpointsTagSize(self.node_endpoints, version);
                ser.writeUnsignedVarint(buf, pos, 0);
                ser.writeUnsignedVarint(buf, pos, tag_size);
                ser.writeCompactArrayLen(buf, pos, self.node_endpoints.len);
                for (self.node_endpoints) |item| {
                    item.serialize(buf, pos, version);
                }
            }
        }
    }

    pub fn deserialize(alloc: Allocator, buf: []const u8, pos: *usize, version: i16) !ProduceResponse {
        var result = ProduceResponse{};
        errdefer {
            for (result.responses) |topic| {
                for (topic.partition_responses) |partition| {
                    if (partition.record_errors.len > 0) alloc.free(partition.record_errors);
                }
                if (topic.partition_responses.len > 0) alloc.free(topic.partition_responses);
            }
            if (result.responses.len > 0) alloc.free(result.responses);
            if (result.node_endpoints.len > 0) alloc.free(result.node_endpoints);
        }

        const responses_len: usize = if (version >= 9)
            (try ser.readCompactArrayLen(buf, pos)) orelse 0
        else
            (try ser.readArrayLen(buf, pos)) orelse 0;
        if (responses_len > 0) {
            const responses_items = try alloc.alloc(TopicProduceResponse, responses_len);
            for (responses_items) |*item| {
                item.* = try TopicProduceResponse.deserialize(alloc, buf, pos, version);
            }
            result.responses = responses_items;
        }
        if (version >= 1) {
            result.throttle_time_ms = ser.readI32(buf, pos);
        }
        if (version >= 9) {
            const tagged_fields = try ser.readTaggedFields(alloc, buf, pos);
            defer if (tagged_fields.len > 0) alloc.free(tagged_fields);

            var seen_node_endpoints = false;
            for (tagged_fields) |field| {
                if (field.tag == 0 and version >= 10) {
                    if (seen_node_endpoints) return error.DuplicateTaggedField;
                    seen_node_endpoints = true;
                    result.node_endpoints = try readNodeEndpointsTag(alloc, field.data, version);
                }
            }
        }
        return result;
    }

    pub fn calcSize(self: *const ProduceResponse, version: i16) usize {
        var size: usize = 0;
        if (version >= 9) {
            size += ser.unsignedVarintSize(self.responses.len + 1);
        } else {
            size += 4;
        }
        for (self.responses) |item| {
            size += item.calcSize(version);
        }
        if (version >= 1) {
            size += 4;
        }
        if (version >= 9) {
            const has_node_endpoints = version >= 10 and self.node_endpoints.len > 0;
            size += ser.unsignedVarintSize(if (has_node_endpoints) 1 else 0);
            if (has_node_endpoints) {
                const tag_size = nodeEndpointsTagSize(self.node_endpoints, version);
                size += ser.unsignedVarintSize(0);
                size += ser.unsignedVarintSize(tag_size);
                size += tag_size;
            }
        }
        return size;
    }
};

fn nodeEndpointsTagSize(node_endpoints: []const ProduceResponse.NodeEndpoint, version: i16) usize {
    var size = ser.unsignedVarintSize(node_endpoints.len + 1);
    for (node_endpoints) |item| {
        size += item.calcSize(version);
    }
    return size;
}

fn readNodeEndpointsTag(alloc: Allocator, data: []const u8, version: i16) ![]const ProduceResponse.NodeEndpoint {
    var tag_pos: usize = 0;
    const node_endpoints_len: usize = (try ser.readCompactArrayLen(data, &tag_pos)) orelse return error.InvalidTaggedField;
    if (node_endpoints_len == 0) {
        if (tag_pos != data.len) return error.InvalidTaggedField;
        return &.{};
    }

    const node_endpoints = try alloc.alloc(ProduceResponse.NodeEndpoint, node_endpoints_len);
    errdefer alloc.free(node_endpoints);
    for (node_endpoints) |*item| {
        item.* = try ProduceResponse.NodeEndpoint.deserialize(alloc, data, &tag_pos, version);
    }
    if (tag_pos != data.len) return error.InvalidTaggedField;
    return node_endpoints;
}
