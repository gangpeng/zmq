// Auto-generated from EndQuorumEpochResponse.json
// API Key: 54, Type: response
// Valid Versions: 0-1, Flexible Versions: 1+
//
// DO NOT EDIT - generated by codegen_v2.py

const std = @import("std");
const ser = @import("../serialization.zig");
const Allocator = std.mem.Allocator;

pub const EndQuorumEpochResponse = struct {
    pub const TopicData = struct {
        pub const PartitionData = struct {
            /// The partition index.
            /// Versions: 0+
            partition_index: i32 = 0,
            /// 
            /// Versions: 0+
            error_code: i16 = 0,
            /// The ID of the current leader or -1 if the leader is unknown.
            /// Versions: 0+
            leader_id: i32 = 0,
            /// The latest known leader epoch
            /// Versions: 0+
            leader_epoch: i32 = 0,

            pub fn serialize(self: *const PartitionData, buf: []u8, pos: *usize, version: i16) void {
                ser.writeI32(buf, pos, self.partition_index);
                ser.writeI16(buf, pos, self.error_code);
                ser.writeI32(buf, pos, self.leader_id);
                ser.writeI32(buf, pos, self.leader_epoch);
                if (version >= 1) ser.writeEmptyTaggedFields(buf, pos);
            }

            pub fn deserialize(_: Allocator, buf: []const u8, pos: *usize, version: i16) !PartitionData {
                var result = PartitionData{};
                result.partition_index = ser.readI32(buf, pos);
                result.error_code = ser.readI16(buf, pos);
                result.leader_id = ser.readI32(buf, pos);
                result.leader_epoch = ser.readI32(buf, pos);
                if (version >= 1) try ser.skipTaggedFields(buf, pos);
                return result;
            }

            pub fn calcSize(_: *const PartitionData, version: i16) usize {
                var size: usize = 0;
                size += 4;
                size += 2;
                size += 4;
                size += 4;
                if (version >= 1) size += 1;
                return size;
            }
        };

        /// The topic name.
        /// Versions: 0+
        topic_name: ?[]const u8 = null,
        /// 
        /// Versions: 0+
        partitions: []const PartitionData = &.{},

        pub fn serialize(self: *const TopicData, buf: []u8, pos: *usize, version: i16) void {
            if (version >= 1) {
                ser.writeCompactString(buf, pos, self.topic_name);
            } else {
                ser.writeString(buf, pos, self.topic_name);
            }
            if (version >= 1) {
                ser.writeCompactArrayLen(buf, pos, self.partitions.len);
            } else {
                ser.writeArrayLen(buf, pos, self.partitions.len);
            }
            for (self.partitions) |item| {
                item.serialize(buf, pos, version);
            }
            if (version >= 1) ser.writeEmptyTaggedFields(buf, pos);
        }

        pub fn deserialize(alloc: Allocator, buf: []const u8, pos: *usize, version: i16) !TopicData {
            var result = TopicData{};
            errdefer if (result.partitions.len > 0) alloc.free(result.partitions);

            result.topic_name = if (version >= 1)
                try ser.readCompactString(buf, pos)
            else
                try ser.readString(buf, pos);
            const partitions_len: usize = if (version >= 1)
                (try ser.readCompactArrayLen(buf, pos)) orelse 0
            else
                (try ser.readArrayLen(buf, pos)) orelse 0;
            if (partitions_len > 0) {
                const partitions_items = try alloc.alloc(PartitionData, partitions_len);
                for (partitions_items) |*item| {
                    item.* = try PartitionData.deserialize(alloc, buf, pos, version);
                }
                result.partitions = partitions_items;
            }
            if (version >= 1) try ser.skipTaggedFields(buf, pos);
            return result;
        }

        pub fn calcSize(self: *const TopicData, version: i16) usize {
            var size: usize = 0;
            if (version >= 1) {
                size += ser.compactStringSize(self.topic_name);
            } else {
                size += ser.stringSize(self.topic_name);
            }
            if (version >= 1) {
                size += ser.unsignedVarintSize(self.partitions.len + 1);
            } else {
                size += 4;
            }
            for (self.partitions) |item| {
                size += item.calcSize(version);
            }
            if (version >= 1) size += 1;
            return size;
        }
    };

    pub const NodeEndpoint = struct {
        /// The ID of the associated node
        /// Versions: 1+
        node_id: i32 = 0,
        /// The node's hostname
        /// Versions: 1+
        host: ?[]const u8 = null,
        /// The node's port
        /// Versions: 1+
        port: u16 = 0,

        pub fn serialize(self: *const NodeEndpoint, buf: []u8, pos: *usize, version: i16) void {
            if (version >= 1) {
                ser.writeI32(buf, pos, self.node_id);
            }
            if (version >= 1) {
                if (version >= 1) {
                    ser.writeCompactString(buf, pos, self.host);
                } else {
                    ser.writeString(buf, pos, self.host);
                }
            }
            if (version >= 1) {
                ser.writeU16(buf, pos, self.port);
            }
            if (version >= 1) ser.writeEmptyTaggedFields(buf, pos);
        }

        pub fn deserialize(_: Allocator, buf: []const u8, pos: *usize, version: i16) !NodeEndpoint {
            var result = NodeEndpoint{};
            if (version >= 1) {
                result.node_id = ser.readI32(buf, pos);
            }
            if (version >= 1) {
                result.host = if (version >= 1)
                    try ser.readCompactString(buf, pos)
                else
                    try ser.readString(buf, pos);
            }
            if (version >= 1) {
                result.port = ser.readU16(buf, pos);
            }
            if (version >= 1) try ser.skipTaggedFields(buf, pos);
            return result;
        }

        pub fn calcSize(self: *const NodeEndpoint, version: i16) usize {
            var size: usize = 0;
            if (version >= 1) {
                size += 4;
            }
            if (version >= 1) {
                if (version >= 1) {
                    size += ser.compactStringSize(self.host);
                } else {
                    size += ser.stringSize(self.host);
                }
            }
            if (version >= 1) {
                size += 2;
            }
            if (version >= 1) size += 1;
            return size;
        }
    };

    /// The top level error code.
    /// Versions: 0+
    error_code: i16 = 0,
    /// 
    /// Versions: 0+
    topics: []const TopicData = &.{},
    /// Endpoints for all leaders enumerated in PartitionData.
    /// Versions: 1+
    node_endpoints: []const NodeEndpoint = &.{},

    pub fn serialize(self: *const EndQuorumEpochResponse, buf: []u8, pos: *usize, version: i16) void {
        ser.writeI16(buf, pos, self.error_code);
        if (version >= 1) {
            ser.writeCompactArrayLen(buf, pos, self.topics.len);
        } else {
            ser.writeArrayLen(buf, pos, self.topics.len);
        }
        for (self.topics) |item| {
            item.serialize(buf, pos, version);
        }
        if (version >= 1) {
            const has_node_endpoints = self.node_endpoints.len > 0;
            ser.writeUnsignedVarint(buf, pos, if (has_node_endpoints) 1 else 0);
            if (has_node_endpoints) {
                const tag_size = nodeEndpointsTagSize(self.node_endpoints, version);
                ser.writeUnsignedVarint(buf, pos, 0);
                ser.writeUnsignedVarint(buf, pos, tag_size);
                ser.writeCompactArrayLen(buf, pos, self.node_endpoints.len);
                for (self.node_endpoints) |item| {
                    item.serialize(buf, pos, version);
                }
            }
        }
    }

    pub fn deserialize(alloc: Allocator, buf: []const u8, pos: *usize, version: i16) !EndQuorumEpochResponse {
        var result = EndQuorumEpochResponse{};
        errdefer {
            for (result.topics) |topic| {
                if (topic.partitions.len > 0) alloc.free(topic.partitions);
            }
            if (result.topics.len > 0) alloc.free(result.topics);
            if (result.node_endpoints.len > 0) alloc.free(result.node_endpoints);
        }

        result.error_code = ser.readI16(buf, pos);
        const topics_len: usize = if (version >= 1)
            (try ser.readCompactArrayLen(buf, pos)) orelse 0
        else
            (try ser.readArrayLen(buf, pos)) orelse 0;
        if (topics_len > 0) {
            const topics_items = try alloc.alloc(TopicData, topics_len);
            for (topics_items) |*item| {
                item.* = try TopicData.deserialize(alloc, buf, pos, version);
            }
            result.topics = topics_items;
        }
        if (version >= 1) {
            const tagged_fields = try ser.readTaggedFields(alloc, buf, pos);
            defer if (tagged_fields.len > 0) alloc.free(tagged_fields);

            var seen_node_endpoints = false;
            for (tagged_fields) |field| {
                if (field.tag == 0) {
                    if (seen_node_endpoints) return error.DuplicateTaggedField;
                    seen_node_endpoints = true;
                    result.node_endpoints = try readNodeEndpointsTag(alloc, field.data, version);
                }
            }
        }
        return result;
    }

    pub fn calcSize(self: *const EndQuorumEpochResponse, version: i16) usize {
        var size: usize = 0;
        size += 2;
        if (version >= 1) {
            size += ser.unsignedVarintSize(self.topics.len + 1);
        } else {
            size += 4;
        }
        for (self.topics) |item| {
            size += item.calcSize(version);
        }
        if (version >= 1) {
            const has_node_endpoints = self.node_endpoints.len > 0;
            size += ser.unsignedVarintSize(if (has_node_endpoints) 1 else 0);
            if (has_node_endpoints) {
                const tag_size = nodeEndpointsTagSize(self.node_endpoints, version);
                size += ser.unsignedVarintSize(0);
                size += ser.unsignedVarintSize(tag_size);
                size += tag_size;
            }
        }
        return size;
    }
};

fn nodeEndpointsTagSize(node_endpoints: []const EndQuorumEpochResponse.NodeEndpoint, version: i16) usize {
    var size = ser.unsignedVarintSize(node_endpoints.len + 1);
    for (node_endpoints) |item| {
        size += item.calcSize(version);
    }
    return size;
}

fn readNodeEndpointsTag(alloc: Allocator, data: []const u8, version: i16) ![]const EndQuorumEpochResponse.NodeEndpoint {
    var tag_pos: usize = 0;
    const node_endpoints_len: usize = (try ser.readCompactArrayLen(data, &tag_pos)) orelse return error.InvalidTaggedField;
    if (node_endpoints_len == 0) {
        if (tag_pos != data.len) return error.InvalidTaggedField;
        return &.{};
    }

    const node_endpoints = try alloc.alloc(EndQuorumEpochResponse.NodeEndpoint, node_endpoints_len);
    errdefer alloc.free(node_endpoints);
    for (node_endpoints) |*item| {
        item.* = try EndQuorumEpochResponse.NodeEndpoint.deserialize(alloc, data, &tag_pos, version);
    }
    if (tag_pos != data.len) return error.InvalidTaggedField;
    return node_endpoints;
}
