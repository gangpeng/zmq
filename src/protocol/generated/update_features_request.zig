// Auto-generated from UpdateFeaturesRequest.json
// API Key: 57, Type: request
// Valid Versions: 0-1, Flexible Versions: 0+
//
// DO NOT EDIT - generated by codegen_v2.py

const std = @import("std");
const ser = @import("../serialization.zig");
const Allocator = std.mem.Allocator;

pub const UpdateFeaturesRequest = struct {
    pub const FeatureUpdateKey = struct {
        /// The name of the finalized feature to be updated.
        /// Versions: 0+
        feature: ?[]const u8 = null,
        /// The new maximum version level for the finalized feature. A value >= 1 is valid. A value < 1, is special, and can be used to request the deletion of the finalized feature.
        /// Versions: 0+
        max_version_level: i16 = 0,
        /// DEPRECATED in version 1 (see DowngradeType). When set to true, the finalized feature version level is allowed to be downgraded/deleted. The downgrade request will fail if the new maximum version level is a value that's not lower than the existing maximum finalized version level.
        /// Versions: 0
        allow_downgrade: bool = false,
        /// Determine which type of upgrade will be performed: 1 will perform an upgrade only (default), 2 is safe downgrades only (lossless), 3 is unsafe downgrades (lossy).
        /// Versions: 1+
        upgrade_type: i8 = 1,

        pub fn serialize(self: *const FeatureUpdateKey, buf: []u8, pos: *usize, version: i16) void {
            ser.writeCompactString(buf, pos, self.feature);
            ser.writeI16(buf, pos, self.max_version_level);
            ser.writeBool(buf, pos, self.allow_downgrade);
            if (version >= 1) {
                ser.writeI8(buf, pos, self.upgrade_type);
            }
            ser.writeEmptyTaggedFields(buf, pos);
        }

        pub fn deserialize(_: Allocator, buf: []const u8, pos: *usize, version: i16) !FeatureUpdateKey {
            var result = FeatureUpdateKey{};
            result.feature = try ser.readCompactString(buf, pos);
            result.max_version_level = ser.readI16(buf, pos);
            result.allow_downgrade = try ser.readBool(buf, pos);
            if (version >= 1) {
                result.upgrade_type = ser.readI8(buf, pos);
            }
            try ser.skipTaggedFields(buf, pos);
            return result;
        }

        pub fn calcSize(self: *const FeatureUpdateKey, version: i16) usize {
            var size: usize = 0;
            size += ser.compactStringSize(self.feature);
            size += 2;
            size += 1;
            if (version >= 1) {
                size += 1;
            }
            size += 1;
            return size;
        }
    };

    /// How long to wait in milliseconds before timing out the request.
    /// Versions: 0+
    timeout_ms: i32 = 60000,
    /// The list of updates to finalized features.
    /// Versions: 0+
    feature_updates: []const FeatureUpdateKey = &.{},
    /// True if we should validate the request, but not perform the upgrade or downgrade.
    /// Versions: 1+
    validate_only: bool = false,

    pub fn serialize(self: *const UpdateFeaturesRequest, buf: []u8, pos: *usize, version: i16) void {
        ser.writeI32(buf, pos, self.timeout_ms);
        ser.writeCompactArrayLen(buf, pos, self.feature_updates.len);
        for (self.feature_updates) |item| {
            item.serialize(buf, pos, version);
        }
        if (version >= 1) {
            ser.writeBool(buf, pos, self.validate_only);
        }
        ser.writeEmptyTaggedFields(buf, pos);
    }

    pub fn deserialize(alloc: Allocator, buf: []const u8, pos: *usize, version: i16) !UpdateFeaturesRequest {
        var result = UpdateFeaturesRequest{};
        result.timeout_ms = ser.readI32(buf, pos);
        const feature_updates_len: usize = (try ser.readCompactArrayLen(buf, pos)) orelse 0;
        if (feature_updates_len > 0) {
            const feature_updates_items = try alloc.alloc(FeatureUpdateKey, feature_updates_len);
            for (feature_updates_items) |*item| {
                item.* = try FeatureUpdateKey.deserialize(alloc, buf, pos, version);
            }
            result.feature_updates = feature_updates_items;
        }
        if (version >= 1) {
            result.validate_only = try ser.readBool(buf, pos);
        }
        try ser.skipTaggedFields(buf, pos);
        return result;
    }

    pub fn calcSize(self: *const UpdateFeaturesRequest, version: i16) usize {
        var size: usize = 0;
        size += 4;
        size += ser.unsignedVarintSize(self.feature_updates.len + 1);
        for (self.feature_updates) |item| {
            size += item.calcSize(version);
        }
        if (version >= 1) {
            size += 1;
        }
        size += 1;
        return size;
    }
};
