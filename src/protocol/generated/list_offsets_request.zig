// Auto-generated from ListOffsetsRequest.json
// API Key: 2, Type: request
// Valid Versions: 0-9, Flexible Versions: 6+
//
// DO NOT EDIT - generated by codegen_v2.py

const std = @import("std");
const ser = @import("../serialization.zig");
const Allocator = std.mem.Allocator;

pub const ListOffsetsRequest = struct {
    pub const ListOffsetsTopic = struct {
        pub const ListOffsetsPartition = struct {
            /// The partition index.
            /// Versions: 0+
            partition_index: i32 = 0,
            /// The current leader epoch.
            /// Versions: 4+
            current_leader_epoch: i32 = -1,
            /// The current timestamp.
            /// Versions: 0+
            timestamp: i64 = 0,
            /// The maximum number of offsets to report.
            /// Versions: 0
            max_num_offsets: i32 = 1,

            pub fn serialize(self: *const ListOffsetsPartition, buf: []u8, pos: *usize, version: i16) void {
                ser.writeI32(buf, pos, self.partition_index);
                if (version >= 4) {
                    ser.writeI32(buf, pos, self.current_leader_epoch);
                }
                ser.writeI64(buf, pos, self.timestamp);
                if (version == 0) {
                    ser.writeI32(buf, pos, self.max_num_offsets);
                }
                if (version >= 6) ser.writeEmptyTaggedFields(buf, pos);
            }

            pub fn deserialize(_: Allocator, buf: []const u8, pos: *usize, version: i16) !ListOffsetsPartition {
                var result = ListOffsetsPartition{};
                result.partition_index = ser.readI32(buf, pos);
                if (version >= 4) {
                    result.current_leader_epoch = ser.readI32(buf, pos);
                }
                result.timestamp = ser.readI64(buf, pos);
                if (version == 0) {
                    result.max_num_offsets = ser.readI32(buf, pos);
                }
                if (version >= 6) try ser.skipTaggedFields(buf, pos);
                return result;
            }

            pub fn calcSize(_: *const ListOffsetsPartition, version: i16) usize {
                var size: usize = 0;
                size += 4;
                if (version >= 4) {
                    size += 4;
                }
                size += 8;
                if (version == 0) {
                    size += 4;
                }
                if (version >= 6) size += 1;
                return size;
            }
        };

        /// The topic name.
        /// Versions: 0+
        name: ?[]const u8 = null,
        /// Each partition in the request.
        /// Versions: 0+
        partitions: []const ListOffsetsPartition = &.{},

        pub fn serialize(self: *const ListOffsetsTopic, buf: []u8, pos: *usize, version: i16) void {
            if (version >= 6) {
                ser.writeCompactString(buf, pos, self.name);
            } else {
                ser.writeString(buf, pos, self.name);
            }
            if (version >= 6) {
                ser.writeCompactArrayLen(buf, pos, self.partitions.len);
            } else {
                ser.writeArrayLen(buf, pos, self.partitions.len);
            }
            for (self.partitions) |item| {
                item.serialize(buf, pos, version);
            }
            if (version >= 6) ser.writeEmptyTaggedFields(buf, pos);
        }

        pub fn deserialize(alloc: Allocator, buf: []const u8, pos: *usize, version: i16) !ListOffsetsTopic {
            var result = ListOffsetsTopic{};
            result.name = if (version >= 6)
                try ser.readCompactString(buf, pos)
            else
                try ser.readString(buf, pos);
            const partitions_len: usize = if (version >= 6)
                (try ser.readCompactArrayLen(buf, pos)) orelse 0
            else
                (try ser.readArrayLen(buf, pos)) orelse 0;
            if (partitions_len > 0) {
                const partitions_items = try alloc.alloc(ListOffsetsPartition, partitions_len);
                for (partitions_items) |*item| {
                    item.* = try ListOffsetsPartition.deserialize(alloc, buf, pos, version);
                }
                result.partitions = partitions_items;
            }
            if (version >= 6) try ser.skipTaggedFields(buf, pos);
            return result;
        }

        pub fn calcSize(self: *const ListOffsetsTopic, version: i16) usize {
            var size: usize = 0;
            if (version >= 6) {
                size += ser.compactStringSize(self.name);
            } else {
                size += ser.stringSize(self.name);
            }
            if (version >= 6) {
                size += ser.unsignedVarintSize(self.partitions.len + 1);
            } else {
                size += 4;
            }
            for (self.partitions) |item| {
                size += item.calcSize(version);
            }
            if (version >= 6) size += 1;
            return size;
        }
    };

    /// The broker ID of the requester, or -1 if this request is being made by a normal consumer.
    /// Versions: 0+
    replica_id: i32 = 0,
    /// This setting controls the visibility of transactional records. Using READ_UNCOMMITTED (isolation_level = 0) makes all records visible. With READ_COMMITTED (isolation_level = 1), non-transactional and COMMITTED transactional records are visible. To be more concrete, READ_COMMITTED returns all data from offsets smaller than the current LSO (last stable offset), and enables the inclusion of the list of aborted transactions in the result, which allows consumers to discard ABORTED transactional records
    /// Versions: 2+
    isolation_level: i8 = 0,
    /// Each topic in the request.
    /// Versions: 0+
    topics: []const ListOffsetsTopic = &.{},

    pub fn serialize(self: *const ListOffsetsRequest, buf: []u8, pos: *usize, version: i16) void {
        ser.writeI32(buf, pos, self.replica_id);
        if (version >= 2) {
            ser.writeI8(buf, pos, self.isolation_level);
        }
        if (version >= 6) {
            ser.writeCompactArrayLen(buf, pos, self.topics.len);
        } else {
            ser.writeArrayLen(buf, pos, self.topics.len);
        }
        for (self.topics) |item| {
            item.serialize(buf, pos, version);
        }
        if (version >= 6) ser.writeEmptyTaggedFields(buf, pos);
    }

    pub fn deserialize(alloc: Allocator, buf: []const u8, pos: *usize, version: i16) !ListOffsetsRequest {
        var result = ListOffsetsRequest{};
        result.replica_id = ser.readI32(buf, pos);
        if (version >= 2) {
            result.isolation_level = ser.readI8(buf, pos);
        }
        const topics_len: usize = if (version >= 6)
            (try ser.readCompactArrayLen(buf, pos)) orelse 0
        else
            (try ser.readArrayLen(buf, pos)) orelse 0;
        if (topics_len > 0) {
            const topics_items = try alloc.alloc(ListOffsetsTopic, topics_len);
            for (topics_items) |*item| {
                item.* = try ListOffsetsTopic.deserialize(alloc, buf, pos, version);
            }
            result.topics = topics_items;
        }
        if (version >= 6) try ser.skipTaggedFields(buf, pos);
        return result;
    }

    pub fn calcSize(self: *const ListOffsetsRequest, version: i16) usize {
        var size: usize = 0;
        size += 4;
        if (version >= 2) {
            size += 1;
        }
        if (version >= 6) {
            size += ser.unsignedVarintSize(self.topics.len + 1);
        } else {
            size += 4;
        }
        for (self.topics) |item| {
            size += item.calcSize(version);
        }
        if (version >= 6) size += 1;
        return size;
    }
};
