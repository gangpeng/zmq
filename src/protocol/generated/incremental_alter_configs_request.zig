// Auto-generated from IncrementalAlterConfigsRequest.json
// API Key: 44, Type: request
// Valid Versions: 0-1, Flexible Versions: 1+
//
// DO NOT EDIT - generated by codegen_v2.py

const std = @import("std");
const ser = @import("../serialization.zig");
const Allocator = std.mem.Allocator;

pub const IncrementalAlterConfigsRequest = struct {
    pub const AlterConfigsResource = struct {
        pub const AlterableConfig = struct {
            /// The configuration key name.
            /// Versions: 0+
            name: ?[]const u8 = null,
            /// The type (Set, Delete, Append, Subtract) of operation.
            /// Versions: 0+
            config_operation: i8 = 0,
            /// The value to set for the configuration key.
            /// Versions: 0+
            value: ?[]const u8 = null,

            pub fn serialize(self: *const AlterableConfig, buf: []u8, pos: *usize, version: i16) void {
                if (version >= 1) {
                    ser.writeCompactString(buf, pos, self.name);
                } else {
                    ser.writeString(buf, pos, self.name);
                }
                ser.writeI8(buf, pos, self.config_operation);
                if (version >= 1) {
                    ser.writeCompactString(buf, pos, self.value);
                } else {
                    ser.writeString(buf, pos, self.value);
                }
                if (version >= 1) ser.writeEmptyTaggedFields(buf, pos);
            }

            pub fn deserialize(_: Allocator, buf: []const u8, pos: *usize, version: i16) !AlterableConfig {
                var result = AlterableConfig{};
                result.name = if (version >= 1)
                    try ser.readCompactString(buf, pos)
                else
                    try ser.readString(buf, pos);
                result.config_operation = ser.readI8(buf, pos);
                result.value = if (version >= 1)
                    try ser.readCompactString(buf, pos)
                else
                    try ser.readString(buf, pos);
                if (version >= 1) try ser.skipTaggedFields(buf, pos);
                return result;
            }

            pub fn calcSize(self: *const AlterableConfig, version: i16) usize {
                var size: usize = 0;
                if (version >= 1) {
                    size += ser.compactStringSize(self.name);
                } else {
                    size += ser.stringSize(self.name);
                }
                size += 1;
                if (version >= 1) {
                    size += ser.compactStringSize(self.value);
                } else {
                    size += ser.stringSize(self.value);
                }
                if (version >= 1) size += 1;
                return size;
            }
        };

        /// The resource type.
        /// Versions: 0+
        resource_type: i8 = 0,
        /// The resource name.
        /// Versions: 0+
        resource_name: ?[]const u8 = null,
        /// The configurations.
        /// Versions: 0+
        configs: []const AlterableConfig = &.{},

        pub fn serialize(self: *const AlterConfigsResource, buf: []u8, pos: *usize, version: i16) void {
            ser.writeI8(buf, pos, self.resource_type);
            if (version >= 1) {
                ser.writeCompactString(buf, pos, self.resource_name);
            } else {
                ser.writeString(buf, pos, self.resource_name);
            }
            if (version >= 1) {
                ser.writeCompactArrayLen(buf, pos, self.configs.len);
            } else {
                ser.writeArrayLen(buf, pos, self.configs.len);
            }
            for (self.configs) |item| {
                item.serialize(buf, pos, version);
            }
            if (version >= 1) ser.writeEmptyTaggedFields(buf, pos);
        }

        pub fn deserialize(alloc: Allocator, buf: []const u8, pos: *usize, version: i16) !AlterConfigsResource {
            var result = AlterConfigsResource{};
            result.resource_type = ser.readI8(buf, pos);
            result.resource_name = if (version >= 1)
                try ser.readCompactString(buf, pos)
            else
                try ser.readString(buf, pos);
            const configs_len: usize = if (version >= 1)
                (try ser.readCompactArrayLen(buf, pos)) orelse 0
            else
                (try ser.readArrayLen(buf, pos)) orelse 0;
            if (configs_len > 0) {
                const configs_items = try alloc.alloc(AlterableConfig, configs_len);
                for (configs_items) |*item| {
                    item.* = try AlterableConfig.deserialize(alloc, buf, pos, version);
                }
                result.configs = configs_items;
            }
            if (version >= 1) try ser.skipTaggedFields(buf, pos);
            return result;
        }

        pub fn calcSize(self: *const AlterConfigsResource, version: i16) usize {
            var size: usize = 0;
            size += 1;
            if (version >= 1) {
                size += ser.compactStringSize(self.resource_name);
            } else {
                size += ser.stringSize(self.resource_name);
            }
            if (version >= 1) {
                size += ser.unsignedVarintSize(self.configs.len + 1);
            } else {
                size += 4;
            }
            for (self.configs) |item| {
                size += item.calcSize(version);
            }
            if (version >= 1) size += 1;
            return size;
        }
    };

    /// The incremental updates for each resource.
    /// Versions: 0+
    resources: []const AlterConfigsResource = &.{},
    /// True if we should validate the request, but not change the configurations.
    /// Versions: 0+
    validate_only: bool = false,

    pub fn serialize(self: *const IncrementalAlterConfigsRequest, buf: []u8, pos: *usize, version: i16) void {
        if (version >= 1) {
            ser.writeCompactArrayLen(buf, pos, self.resources.len);
        } else {
            ser.writeArrayLen(buf, pos, self.resources.len);
        }
        for (self.resources) |item| {
            item.serialize(buf, pos, version);
        }
        ser.writeBool(buf, pos, self.validate_only);
        if (version >= 1) ser.writeEmptyTaggedFields(buf, pos);
    }

    pub fn deserialize(alloc: Allocator, buf: []const u8, pos: *usize, version: i16) !IncrementalAlterConfigsRequest {
        var result = IncrementalAlterConfigsRequest{};
        const resources_len: usize = if (version >= 1)
            (try ser.readCompactArrayLen(buf, pos)) orelse 0
        else
            (try ser.readArrayLen(buf, pos)) orelse 0;
        if (resources_len > 0) {
            const resources_items = try alloc.alloc(AlterConfigsResource, resources_len);
            for (resources_items) |*item| {
                item.* = try AlterConfigsResource.deserialize(alloc, buf, pos, version);
            }
            result.resources = resources_items;
        }
        result.validate_only = try ser.readBool(buf, pos);
        if (version >= 1) try ser.skipTaggedFields(buf, pos);
        return result;
    }

    pub fn calcSize(self: *const IncrementalAlterConfigsRequest, version: i16) usize {
        var size: usize = 0;
        if (version >= 1) {
            size += ser.unsignedVarintSize(self.resources.len + 1);
        } else {
            size += 4;
        }
        for (self.resources) |item| {
            size += item.calcSize(version);
        }
        size += 1;
        if (version >= 1) size += 1;
        return size;
    }
};
