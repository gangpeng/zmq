// Auto-generated from ListPartitionReassignmentsResponse.json
// API Key: 46, Type: response
// Valid Versions: 0, Flexible Versions: 0+
//
// DO NOT EDIT - generated by codegen_v2.py

const std = @import("std");
const ser = @import("../serialization.zig");
const Allocator = std.mem.Allocator;

pub const ListPartitionReassignmentsResponse = struct {
    pub const OngoingTopicReassignment = struct {
        pub const OngoingPartitionReassignment = struct {
            /// The index of the partition.
            /// Versions: 0+
            partition_index: i32 = 0,
            /// The current replica set.
            /// Versions: 0+
            replicas: []const i32 = &.{},
            /// The set of replicas we are currently adding.
            /// Versions: 0+
            adding_replicas: []const i32 = &.{},
            /// The set of replicas we are currently removing.
            /// Versions: 0+
            removing_replicas: []const i32 = &.{},

            pub fn serialize(self: *const OngoingPartitionReassignment, buf: []u8, pos: *usize, _: i16) void {
                ser.writeI32(buf, pos, self.partition_index);
                ser.writeCompactArrayLen(buf, pos, self.replicas.len);
                for (self.replicas) |item| {
                    ser.writeI32(buf, pos, item);
                }
                ser.writeCompactArrayLen(buf, pos, self.adding_replicas.len);
                for (self.adding_replicas) |item| {
                    ser.writeI32(buf, pos, item);
                }
                ser.writeCompactArrayLen(buf, pos, self.removing_replicas.len);
                for (self.removing_replicas) |item| {
                    ser.writeI32(buf, pos, item);
                }
                ser.writeEmptyTaggedFields(buf, pos);
            }

            pub fn deserialize(alloc: Allocator, buf: []const u8, pos: *usize, _: i16) !OngoingPartitionReassignment {
                var result = OngoingPartitionReassignment{};
                result.partition_index = ser.readI32(buf, pos);
                const replicas_len: usize = (try ser.readCompactArrayLen(buf, pos)) orelse 0;
                if (replicas_len > 0) {
                    const replicas_items = try alloc.alloc(i32, replicas_len);
                    for (replicas_items) |*item| {
                        item.* = ser.readI32(buf, pos);
                    }
                    result.replicas = replicas_items;
                }
                const adding_replicas_len: usize = (try ser.readCompactArrayLen(buf, pos)) orelse 0;
                if (adding_replicas_len > 0) {
                    const adding_replicas_items = try alloc.alloc(i32, adding_replicas_len);
                    for (adding_replicas_items) |*item| {
                        item.* = ser.readI32(buf, pos);
                    }
                    result.adding_replicas = adding_replicas_items;
                }
                const removing_replicas_len: usize = (try ser.readCompactArrayLen(buf, pos)) orelse 0;
                if (removing_replicas_len > 0) {
                    const removing_replicas_items = try alloc.alloc(i32, removing_replicas_len);
                    for (removing_replicas_items) |*item| {
                        item.* = ser.readI32(buf, pos);
                    }
                    result.removing_replicas = removing_replicas_items;
                }
                try ser.skipTaggedFields(buf, pos);
                return result;
            }

            pub fn calcSize(self: *const OngoingPartitionReassignment, _: i16) usize {
                var size: usize = 0;
                size += 4;
                size += ser.unsignedVarintSize(self.replicas.len + 1);
                size += self.replicas.len * 4;
                size += ser.unsignedVarintSize(self.adding_replicas.len + 1);
                size += self.adding_replicas.len * 4;
                size += ser.unsignedVarintSize(self.removing_replicas.len + 1);
                size += self.removing_replicas.len * 4;
                size += 1;
                return size;
            }
        };

        /// The topic name.
        /// Versions: 0+
        name: ?[]const u8 = null,
        /// The ongoing reassignments for each partition.
        /// Versions: 0+
        partitions: []const OngoingPartitionReassignment = &.{},

        pub fn serialize(self: *const OngoingTopicReassignment, buf: []u8, pos: *usize, version: i16) void {
            ser.writeCompactString(buf, pos, self.name);
            ser.writeCompactArrayLen(buf, pos, self.partitions.len);
            for (self.partitions) |item| {
                item.serialize(buf, pos, version);
            }
            ser.writeEmptyTaggedFields(buf, pos);
        }

        pub fn deserialize(alloc: Allocator, buf: []const u8, pos: *usize, version: i16) !OngoingTopicReassignment {
            var result = OngoingTopicReassignment{};
            result.name = try ser.readCompactString(buf, pos);
            const partitions_len: usize = (try ser.readCompactArrayLen(buf, pos)) orelse 0;
            if (partitions_len > 0) {
                const partitions_items = try alloc.alloc(OngoingPartitionReassignment, partitions_len);
                for (partitions_items) |*item| {
                    item.* = try OngoingPartitionReassignment.deserialize(alloc, buf, pos, version);
                }
                result.partitions = partitions_items;
            }
            try ser.skipTaggedFields(buf, pos);
            return result;
        }

        pub fn calcSize(self: *const OngoingTopicReassignment, version: i16) usize {
            var size: usize = 0;
            size += ser.compactStringSize(self.name);
            size += ser.unsignedVarintSize(self.partitions.len + 1);
            for (self.partitions) |item| {
                size += item.calcSize(version);
            }
            size += 1;
            return size;
        }
    };

    /// The duration in milliseconds for which the request was throttled due to a quota violation, or zero if the request did not violate any quota.
    /// Versions: 0+
    throttle_time_ms: i32 = 0,
    /// The top-level error code, or 0 if there was no error
    /// Versions: 0+
    error_code: i16 = 0,
    /// The top-level error message, or null if there was no error.
    /// Versions: 0+
    error_message: ?[]const u8 = null,
    /// The ongoing reassignments for each topic.
    /// Versions: 0+
    topics: []const OngoingTopicReassignment = &.{},

    pub fn serialize(self: *const ListPartitionReassignmentsResponse, buf: []u8, pos: *usize, version: i16) void {
        ser.writeI32(buf, pos, self.throttle_time_ms);
        ser.writeI16(buf, pos, self.error_code);
        ser.writeCompactString(buf, pos, self.error_message);
        ser.writeCompactArrayLen(buf, pos, self.topics.len);
        for (self.topics) |item| {
            item.serialize(buf, pos, version);
        }
        ser.writeEmptyTaggedFields(buf, pos);
    }

    pub fn deserialize(alloc: Allocator, buf: []const u8, pos: *usize, version: i16) !ListPartitionReassignmentsResponse {
        var result = ListPartitionReassignmentsResponse{};
        result.throttle_time_ms = ser.readI32(buf, pos);
        result.error_code = ser.readI16(buf, pos);
        result.error_message = try ser.readCompactString(buf, pos);
        const topics_len: usize = (try ser.readCompactArrayLen(buf, pos)) orelse 0;
        if (topics_len > 0) {
            const topics_items = try alloc.alloc(OngoingTopicReassignment, topics_len);
            for (topics_items) |*item| {
                item.* = try OngoingTopicReassignment.deserialize(alloc, buf, pos, version);
            }
            result.topics = topics_items;
        }
        try ser.skipTaggedFields(buf, pos);
        return result;
    }

    pub fn calcSize(self: *const ListPartitionReassignmentsResponse, version: i16) usize {
        var size: usize = 0;
        size += 4;
        size += 2;
        size += ser.compactStringSize(self.error_message);
        size += ser.unsignedVarintSize(self.topics.len + 1);
        for (self.topics) |item| {
            size += item.calcSize(version);
        }
        size += 1;
        return size;
    }
};
