// Auto-generated from OffsetForLeaderEpochResponse.json
// API Key: 23, Type: response
// Valid Versions: 0-4, Flexible Versions: 4+
//
// DO NOT EDIT - generated by codegen_v2.py

const std = @import("std");
const ser = @import("../serialization.zig");
const Allocator = std.mem.Allocator;

pub const OffsetForLeaderEpochResponse = struct {
    pub const OffsetForLeaderTopicResult = struct {
        pub const EpochEndOffset = struct {
            /// The error code 0, or if there was no error.
            /// Versions: 0+
            error_code: i16 = 0,
            /// The partition index.
            /// Versions: 0+
            partition: i32 = 0,
            /// The leader epoch of the partition.
            /// Versions: 1+
            leader_epoch: i32 = -1,
            /// The end offset of the epoch.
            /// Versions: 0+
            end_offset: i64 = -1,

            pub fn serialize(self: *const EpochEndOffset, buf: []u8, pos: *usize, version: i16) void {
                ser.writeI16(buf, pos, self.error_code);
                ser.writeI32(buf, pos, self.partition);
                if (version >= 1) {
                    ser.writeI32(buf, pos, self.leader_epoch);
                }
                ser.writeI64(buf, pos, self.end_offset);
                if (version >= 4) ser.writeEmptyTaggedFields(buf, pos);
            }

            pub fn deserialize(_: Allocator, buf: []const u8, pos: *usize, version: i16) !EpochEndOffset {
                var result = EpochEndOffset{};
                result.error_code = ser.readI16(buf, pos);
                result.partition = ser.readI32(buf, pos);
                if (version >= 1) {
                    result.leader_epoch = ser.readI32(buf, pos);
                }
                result.end_offset = ser.readI64(buf, pos);
                if (version >= 4) try ser.skipTaggedFields(buf, pos);
                return result;
            }

            pub fn calcSize(_: *const EpochEndOffset, version: i16) usize {
                var size: usize = 0;
                size += 2;
                size += 4;
                if (version >= 1) {
                    size += 4;
                }
                size += 8;
                if (version >= 4) size += 1;
                return size;
            }
        };

        /// The topic name.
        /// Versions: 0+
        topic: ?[]const u8 = null,
        /// Each partition in the topic we fetched offsets for.
        /// Versions: 0+
        partitions: []const EpochEndOffset = &.{},

        pub fn serialize(self: *const OffsetForLeaderTopicResult, buf: []u8, pos: *usize, version: i16) void {
            if (version >= 4) {
                ser.writeCompactString(buf, pos, self.topic);
            } else {
                ser.writeString(buf, pos, self.topic);
            }
            if (version >= 4) {
                ser.writeCompactArrayLen(buf, pos, self.partitions.len);
            } else {
                ser.writeArrayLen(buf, pos, self.partitions.len);
            }
            for (self.partitions) |item| {
                item.serialize(buf, pos, version);
            }
            if (version >= 4) ser.writeEmptyTaggedFields(buf, pos);
        }

        pub fn deserialize(alloc: Allocator, buf: []const u8, pos: *usize, version: i16) !OffsetForLeaderTopicResult {
            var result = OffsetForLeaderTopicResult{};
            result.topic = if (version >= 4)
                try ser.readCompactString(buf, pos)
            else
                try ser.readString(buf, pos);
            const partitions_len: usize = if (version >= 4)
                (try ser.readCompactArrayLen(buf, pos)) orelse 0
            else
                (try ser.readArrayLen(buf, pos)) orelse 0;
            if (partitions_len > 0) {
                const partitions_items = try alloc.alloc(EpochEndOffset, partitions_len);
                for (partitions_items) |*item| {
                    item.* = try EpochEndOffset.deserialize(alloc, buf, pos, version);
                }
                result.partitions = partitions_items;
            }
            if (version >= 4) try ser.skipTaggedFields(buf, pos);
            return result;
        }

        pub fn calcSize(self: *const OffsetForLeaderTopicResult, version: i16) usize {
            var size: usize = 0;
            if (version >= 4) {
                size += ser.compactStringSize(self.topic);
            } else {
                size += ser.stringSize(self.topic);
            }
            if (version >= 4) {
                size += ser.unsignedVarintSize(self.partitions.len + 1);
            } else {
                size += 4;
            }
            for (self.partitions) |item| {
                size += item.calcSize(version);
            }
            if (version >= 4) size += 1;
            return size;
        }
    };

    /// The duration in milliseconds for which the request was throttled due to a quota violation, or zero if the request did not violate any quota.
    /// Versions: 2+
    throttle_time_ms: i32 = 0,
    /// Each topic we fetched offsets for.
    /// Versions: 0+
    topics: []const OffsetForLeaderTopicResult = &.{},

    pub fn serialize(self: *const OffsetForLeaderEpochResponse, buf: []u8, pos: *usize, version: i16) void {
        if (version >= 2) {
            ser.writeI32(buf, pos, self.throttle_time_ms);
        }
        if (version >= 4) {
            ser.writeCompactArrayLen(buf, pos, self.topics.len);
        } else {
            ser.writeArrayLen(buf, pos, self.topics.len);
        }
        for (self.topics) |item| {
            item.serialize(buf, pos, version);
        }
        if (version >= 4) ser.writeEmptyTaggedFields(buf, pos);
    }

    pub fn deserialize(alloc: Allocator, buf: []const u8, pos: *usize, version: i16) !OffsetForLeaderEpochResponse {
        var result = OffsetForLeaderEpochResponse{};
        if (version >= 2) {
            result.throttle_time_ms = ser.readI32(buf, pos);
        }
        const topics_len: usize = if (version >= 4)
            (try ser.readCompactArrayLen(buf, pos)) orelse 0
        else
            (try ser.readArrayLen(buf, pos)) orelse 0;
        if (topics_len > 0) {
            const topics_items = try alloc.alloc(OffsetForLeaderTopicResult, topics_len);
            for (topics_items) |*item| {
                item.* = try OffsetForLeaderTopicResult.deserialize(alloc, buf, pos, version);
            }
            result.topics = topics_items;
        }
        if (version >= 4) try ser.skipTaggedFields(buf, pos);
        return result;
    }

    pub fn calcSize(self: *const OffsetForLeaderEpochResponse, version: i16) usize {
        var size: usize = 0;
        if (version >= 2) {
            size += 4;
        }
        if (version >= 4) {
            size += ser.unsignedVarintSize(self.topics.len + 1);
        } else {
            size += 4;
        }
        for (self.topics) |item| {
            size += item.calcSize(version);
        }
        if (version >= 4) size += 1;
        return size;
    }
};
